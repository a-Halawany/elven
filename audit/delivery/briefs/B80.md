# B80: shell and foundations (prepared first assignment of A3; proposal until the owner allocates A3)

**Stage.** B80, lane E. Completes F-P7-E-03 (tokens), F-P7-E-05 (component library) and F-P7-E-01 (the shell). Stage effort: 6–9.5 U. It depends on no other stage.

**What waits on it.** B81 → B82 → B84 → B83 / B85 / B86, B108 (accessibility software), B93/B94 through B84. **Mostly web**: a migration is needed only if persisted per-user navigation and workspace state (PR-05-002) goes into the database. That would use provisional `9002`, issued at start.

## Scope: the remaining clauses (from FEATURE_TRACKER.csv)

| Feature | Remaining clauses |
|---|---|
| **F-P7-E-03** tokens, themes, type, icons | Self-hosted Inter Variable and IBM Plex Mono; tabular-nums numeric tokens (TYP-001/002/013/014) · the remaining colour tokens (recommended, decided, withdrawn, classification) bound into components · forced-colours, print and fallback themes; the certified customer-theme mechanism with contrast validation and preview (PAT-41, UX-ADR-022) · the iconography set with semantic meaning (ch20) and design-system package versioning (ch5, ch68) |
| **F-P7-E-05** the component library | One governed package: Field, Text Area, Select, Combobox (ARIA 1.2), Date/Time with zones, Range, Unit input, File Intake · Data Table (sort, filter, group, select, export, sticky header, density), Tree Grid, Structured List, Metric, Comparison Matrix, Diff Viewer · the panels/drawers/dialogs contract and motion/loading/toast/skeleton/error summary · save-draft and bulk-selection patterns across every authoring flow · per-page inline styles replaced; component contract tests and a catalogue page |
| **F-P7-E-01** the shell | One Application Shell replacing the six duplicated layouts, with the persistent Global Context Bar (tenant, domain, role, objective, horizon, scenario, classification, effective time, policy) · context switch with impact preview (PAT-02); persisted per-user navigation state · Breadcrumb, Tab Set, History Navigator, verified return path (PAT-26) · deep links resolving exact object, version and as-of state (PAT-25) · the navigation-never-mutates guard and the degraded-context journey |

## Where the code is today

- Tokens: `packages/tokens/src/registry.ts`.
- Components: `apps/web/components/ui.tsx`, `observation.tsx`, `working-domain.tsx`.
- The six duplicated layouts: `apps/web/app/*/layout.tsx`. The web nav is an A1 hotspot: propose the shell's nav in a `/* B80 */` block.
- Browser suites: `apps/web/e2e/`. Every existing page must keep working, with browser-regression green.

## Isolation and rules

- Worktree `.claude/worktrees/a3-b80`; branch `e/b80-shell-foundations`.
- Databases `eye_verify_a3_*`; Redis :6395; vault `~/.eye-verify/a3/vault`; API :3413; web :3103.
- Browser suites run through `scripts/dev/heavy-slot.sh a3-b80 -- …`.
- Never touch :3000/:3401 or `eye_demo`.

## Demonstration: act-b80 on A3's copy, and the browser walk

- The NORDWERK theme is refused for the critical-state contrast, then accepted once corrected.
- The source-registration and decision-package forms share Field, validation and draft behavior.
- A bulk selection of Red Sea sources uses one pattern.
- `l.brandt` switches the context bar from the Regensburg objective to the corridor scenario and sees the impact preview before anything opens.
- A deep link opens an exact version as-of.

## Completion

The generic M1 conditions in STAGES.csv. The PR body lists the delivered and remaining clauses, the proposed row moves with evidence, and the nav/layout edits for A1.
