# B50: the knowledge core (prepared first assignment of A2; proposal until the owner allocates A2)

**Stage.** B50, lane A. Completes F-P3-01 through F-P3-05. Stage effort: 8.25–14.5 U (STAGES.csv).

**Why first.** B50 heads the longest precedence chain: B50 → B51 → B46 → B48 → B49 → B55 → B41. It depends on no other stage. B51 (ontology and graph revisions) and B54 (memory completion) wait on it.

**Verification.** F-P3-01's comprehensive schema-conformance verification is placed in H1; B50 builds and proves every clause.

## Scope: the remaining clauses (from FEATURE_TRACKER.csv; the spec refs are in each row)

| Feature | Remaining clauses (build all; demonstrate each) |
|---|---|
| **F-P3-01** canonical headers, identity, time | A generic writer populating confidence, uncertainty, quality, freshness, rights, residency, retention and `ontology_ref` instead of nulling them · mandatory-field validation per owning component before decision-active use · time values with zone, precision, source and uncertainty (unknown, approximate, recurring, conflicting) · identity declarations (issuing authority, namespace, aliases) and tenant/domain ids in telemetry and backups · lifecycle profiles per canonical type (RSK and MEM included) with typed re-entry transitions and causation · consolidated per-type schema-conformance fixtures |
| **F-P3-02** work-object lifecycle | One state machine with transition contracts (draft, reviewed, approved, published, corrected, withdrawn, archived, retired) · owner-only authoritative transitions; views, agents and imports submit proposals · rejection or compensation of invalid, duplicate, out-of-order or partial transitions · lifecycle replay |
| **F-P3-03** provenance and forward impact | A revision-diff query with downstream impact · backward explanation (models, agents, reviewers, approvals) and forward impact (warnings, briefings, commitments) · denied, abstained, challenged and failed paths in product lineage · generic "unfit" marking and the affected-product state classes · claim-to-evidence alignment through summaries and exports · custody-transfer exercises |
| **F-P3-04** timeline and as-of explorer | A timeline projection and an as-of explorer across object types (event, observation, validity, record time) · a source-to-product trace view exposing corrections, missing intervals and policy filtering · clock-skew fixtures and broken-lineage marking |
| **F-P3-05** correction-propagation closure | Per-consumer acknowledgement and bounded-exception closure before a correction case closes · the propagation-closure record · automatic publication freeze and escalation on stall · impact traversal into decisions, briefings, commitments, memory, evaluation datasets and exports · triggers for legal request, model incident, ontology correction and data-quality findings · the correction-propagation time metric · the challenge path |

## Where the code is today

- The 43-field canonical header and its CHECKs: `apps/api/migrations/0006_objects.sql`.
- The writers that currently null the header fields: `apps/api/src/intelligence/extraction/extraction.service.ts`, `…/review/review.service.ts`, `…/prediction/forecasting/forecasting.service.ts`, `…/prediction/scenarios/scenarios.service.ts`, `…/retention/import-package.ts`.
- Lineage: 0023. Impact walk: 0024, 0025, 0034.
- The correction agent: 0060, 0062.
- Subscriptions: 0063–0065 and 0083 (`apps/api/src/graph/subscriptions/`).
- The dependency graph: `apps/api/src/graph/`.

## Isolation and rules (DELIVERY_PLAN.md §6)

- Worktree `.claude/worktrees/a2-b50`; branch `a/b50-knowledge-core` from `main`.
- Databases `eye_verify_a2_*`; Redis :6394; vault `~/.eye-verify/a2/vault`; API :3412; web :3102.
- Heavy runs go through `scripts/dev/heavy-slot.sh a2-b50 -- …`.
- The provisional migration is `9001_b50_<slug>.sql`, issued by A1 at start (`MIGRATION_LEDGER.csv`). A1 freezes the final name at integration; after the rename, rebuild the disposable databases.
- Records and hotspots are A1's. Append PDP and refusal rows in `/* B50 */` blocks and list them in the PR body.

## Demonstration: act-b50, rehearsed on A2's own restored copy

- A late-August ETA is stored as an interval with its precision.
- The Red Sea assessment moves draft → reviewed → published; a stale concurrent publish is rejected.
- Correcting one port bulletin shows its forward impact: two claims, the corridor forecast, the warning and last week's briefing. The closure record shows every consumer's acknowledgement; a stalled consumer freezes publication.
- The executive scrubs the timeline to the day of the sourcing decision.

## Completion

The generic M1 conditions in STAGES.csv, plus the golden journey once B106 exists. The PR body lists the delivered and remaining clauses, the proposed tracker and requirement-row moves with evidence, and every shared-file edit.
