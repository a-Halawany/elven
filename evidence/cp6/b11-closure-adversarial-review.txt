=== The author's adversarial review of the B11 closure candidate (2026-09-14, before the commit): six find dimensions, two refuters per finding; then the corrections, then two judges per confirmed finding ===
=== Finders: locks (the advisory-lock design), cleanup (the executor's in-transaction cleanup), port (the re-declared ports), verifier (B11-F2), harness (the closure test file and the hold primitive), regressions (the suites, the scripts, the runbook). Refuters default to 'refuted' when they cannot demonstrate the defect; a finding is CONFIRMED unless both refuters refute it. ===

[1] verifier · MEDIUM · CONFIRMED
    Chain check digests a substituted [] for a non-array `excluded`, so an edited manifest.json verifies PACKAGE OK with the product's recorded digest
    at scripts/retention/verify-export.mjs:170
    claim: Line 170 (`const excluded = Array.isArray(manifest.excluded) ? manifest.excluded : [];`) feeds the chain at line 184 a substitute `[]` whenever the manifest's `excluded` member is not an array, whereas the product's rule the header claims to share (apps/api/src/retention/export-package.ts:42, `excluded: m.excluded`) digests the member as written. A manifest.json whose `excluded` was changed from `[]` to `null`, to a string, or to an object carrying arbitrary content therefore passes 'chain: sha256(JCS({format, package, authorization, gates, objects_digest, excluded, bound_to, statement})) = signature.package_digest' (the printed PASS is literally false for the file as written), passes 'authe
    refuter 1: stands — NOT REFUTED. The defect is real and reproducible on the corrected script; it is bounded and pre-existing, but the verifier provably proves less than it claims and diverges from the product's rule it claims to share.

What happens (code): scripts/retention/verify-export.mjs:170 coerces `manifest.excluded` to `[]` whenever it is not an array (`const excluded = Array.isArray(manifest.excluded) ? mani
    refuter 2: stands — Not refuted. The mechanism is exactly as claimed and I reproduced it independently with the product's own digest helper. scripts/retention/verify-export.mjs:170 substitutes `[]` for any non-array `manifest.excluded`, and :184 feeds that substitute (not the member as written) into the package-digest chain; the product's rule the header claims to share (apps/api/src/retention/export-package.ts:42, `

[2] verifier · LOW · REFUTED (both refuters)
    A zero-object package with a valid chain verifies PACKAGE OK although the product can never issue one
    at scripts/retention/verify-export.mjs:132
    claim: The 'objects is a list' check (line 132) accepts an empty list, the loop and the chain run over `[]`, `summary` is set (line 200) and the verdict (line 221) prints 'PACKAGE OK: 0 objects, 0 bytes, package digest …' with exit 0, ok:true, complete:true. The product refuses to record such a package: retention.export_packages has CHECK (object_count >= 1) (0070 line 279) and record_export_package raises when p_object_count differs from the approved scope's executable count (0070 lines 345–346), and buildExportPackage rolls back when anything was refused. So the verifier's OK on an empty directory-plus-manifest is a success by absence of the class B11-F2 named: it certifies as an export package s
    refuter 1: refuted — The behaviour is real (reproduced) but it is not a defect of the correction under any of the review's criteria.

1. It does not fail the frozen closure of B11-F2. The closure (audit/reviews/The_Eye_1fa3b08_Closure_and_B11_Bounded_Review.md:84) asks the verifier to reject a non-object manifest and an incomplete package WITHOUT A COMPLETED VALIDATION RESULT, return nonzero with ok:false, make text/J
    refuter 2: refuted — The behaviour is real (a synthetic zero-object manifest with a self-consistent chain prints "PACKAGE OK: 0 objects, 0 bytes", exit 0, ok:true, complete:true) but it is not a defect of the correction under any of the criteria a finding must meet.

1. It does not fail the frozen closure, and the finding concedes this. The B11-F2 closure (audit/reviews/The_Eye_1fa3b08_Closure_and_B11_Bounded_Review.m

[3] verifier · LOW · REFUTED (both refuters)
    The bound_to restatement check passes vacuously when the authorization block and bound_to both omit scope_digest/approval_id
    at scripts/retention/verify-export.mjs:189
    claim: Lines 189–190 compare `sig.bound_to.scope_digest === auth.scope_digest && sig.bound_to.approval_id === auth.approval_id` with plain strict equality, so `undefined === undefined` satisfies both; only action_id is guarded by `!== undefined`. A self-consistent package with `authorization: {}` and `bound_to: { action_id }` prints 'PASS chain: signature.bound_to restates the covered authorization (… scope_digest = authorization.scope_digest, approval_id = authorization.approval_id) — bound to action …, scope digest undefined, approval undefined' and PACKAGE OK, exit 0. The check's claim — that the signature block names the scope digest and the approval it is bound to — is not established. Impact 
    refuter 1: refuted — The observation is accurate (the both-undefined case does print PASS with "scope digest undefined, approval undefined" and exits 0 offline), but it is not a defect of the correction under the review's rules, and it is not a distinct weakness of the verifier.

1. Not a failure of the frozen closure conditions. B11-F2's frozen closure (review lines 74-84) is: reject a non-object manifest and an inco
    refuter 2: refuted — The observation is factually accurate but it is not a defect of the correction under any of the admissible criteria (fails frozen closure, loses/corrupts bytes, regresses behaviour, proves less than claimed).

1. Not a failure of the frozen closure. B11-F2's frozen closure (audit/reviews/The_Eye_1fa3b08_Closure_and_B11_Bounded_Review.md, §B11-F2) is: reject a non-object manifest and an incomplete 

[4] verifier · LOW · CONFIRMED
    A listed .bin that cannot be read as a file crashes the verifier with a stack trace: exit 1 but no verdict line and no JSON
    at scripts/retention/verify-export.mjs:145
    claim: `readFileSync(path)` at line 145 (and `readdirSync(dir)` at line 165, `statSync`/second `readFileSync` at line 118) run outside any try/catch. When a listed `<manifest_id>.bin` exists but is a directory (EISDIR) or unreadable (EACCES), the process dies on the uncaught exception: exit status 1, stdout empty — no 'PACKAGE FAILED' line in text mode and no JSON document in --json mode (a JSON consumer gets a parse error, not `ok:false`). The frozen closure asks that the text verdict, the JSON `ok` and the exit status always agree; here two of the three outcomes are absent rather than failing, and the failing check is not named. Fail-closed by accident (Node's exit code), not by the verdict logic
    refuter 1: stands — I could not refute the finding; it reproduces exactly as claimed and is a real, if low-severity, gap between what the corrected verifier claims and what it does.

What I established:
1. The core claim is accurate against the code. scripts/retention/verify-export.mjs:145 `const bytes = readFileSync(path);` runs outside any try/catch, inside the per-object loop, after only an `existsSync` guard at :
    refuter 2: refuted — The observable behaviour is real and I reproduced it independently: with a listed <manifest_id>.bin replaced by a directory (EISDIR) or made mode 000 (EACCES), scripts/retention/verify-export.mjs:145 `readFileSync(path)` throws outside any try/catch; the process exits 1 with empty stdout and a stack trace on stderr, in text mode and with --json alike. But this is not a way the correction fails its

[5] port · MEDIUM · CONFIRMED
    The manifest lock does not span the rollback cleanup when the executing backend is lost: the catch removes a copy another execution has already adopted, and committed evidence bytes are gone from both tiers
    at apps/api/src/retention/retention.service.ts:151
    claim: 0071's closure rests on the claim (migration header line 24, service comment lines 146-148) that 'an aborted transaction keeps its locks until it is rolled back', so removing the copies THIS execution created inside the catch is covered by the lock. That holds for a transaction that is aborted but open; it does not hold for one whose backend is gone (pg_terminate_backend, a server restart or failover, a dropped connection). PostgreSQL releases the advisory lock the moment the backend dies, while the Node process is still parked on the copy path with the file on disk and its locator in copiesMade. A second approved archive naming the same manifest then begins, finds the identical copy, adopts
    refuter 1: stands — The finding is a real, demonstrable defect and I could not refute it. The correction's safety argument for removing copiesMade inside the catch (retention.service.ts:146-148, echoed in the 0071 header) is that an aborted transaction keeps its advisory locks until ROLLBACK. That is true for an aborted-but-open transaction and false for a lost backend: PostgreSQL releases a transaction-scoped adviso
    refuter 2: stands — NOT REFUTED, but the finding stands only in a much narrower form than claimed; most of its reproduction and its stated triggers are refuted.

WHAT IS REFUTED. (1) The finder's reproduction is an artifact of vitest. Its probe terminates A's backend while A is parked at the b11.archive_after_copy_before_record hold with NO active statement. In that state pg's client receives the FATAL with no active

[6] harness · LOW · CONFIRMED
    disarm() cannot release a hold that has already fired; the comment's guarantee is false for the only case that matters
    at /Users/halawany/work/personal/mohammed/new_project/apps/api/src/observation/fault-injection.ts:148
    claim: `pause()` does `holds.delete(point)` before `await h.released`, so a fired hold is no longer in the map when `disarm()` runs its release loop (lines 115-117: 'A hold still pending when the test disarms is released, so the code under test never waits on a test that has moved on'). The loop only releases holds that were armed and never reached. When a closure case fails between `await held.reached` and `held.release()` (the normal failure mode of F1 cases 1 and 2), afterAll's `fault.disarm()` leaves execution A parked inside `copyBlob` (or the catch block of `execute`) with its transaction open, the advisory locks on its manifests held and a commit-pool connection checked out, until the worker
    refuter 1: refuted — The code reading is correct and I reproduced it independently, but the finding does not meet this review's admissibility bar and the guarantee it says is missing is already delivered by existing controls, so I refute it as a defect of the correction while conceding the comment is overstated.

What is true (confirmed, not disputed): apps/api/src/observation/fault-injection.ts:148 `holds.delete(poin
    refuter 2: stands — The finding stands; I could not refute it. The claimant reads the code correctly and the defect is demonstrable at two levels.

CODE: apps/api/src/observation/fault-injection.ts:146-150 — pause() does holds.get(point) → holds.delete(point) → h.reached() → await h.released, all synchronously up to the await. disarm() at :115-117 iterates holds.values() and calls h.release(). Consequently the releas

[7] harness · LOW · REFUTED (both refuters)
    The recorded evidence proves less than the migration header and the harness header claim: the before-run was a two-case filtered run and the after-runs predate the sixth case and the service's already-archived branch
    at /Users/halawany/work/personal/mohammed/new_project/apps/api/migrations/0071_b11_closure_manifest_locks.sql:30
    claim: Lines 30-31 say the interleaving was 'reproduced on the unfixed executor (evidence/cp6/b11-closure-repro-before.txt)'. That file does not exist in the tree (evidence/cp6 has no b11-closure file). The scratch repro-before.txt reports '5 tests | 1 failed | 3 skipped' with '1 passed': no `bail` is configured in vitest.int.config.ts, and vitest reports `-t`-filtered cases as skipped (verified: `-t "serial control"` gives '1 passed | 5 skipped (6)'), so only two of the five cases ran on the unfixed executor — the interleaving case, red at the archive retrieval with reason 'missing' under B's committed tier record (that IS the byte loss Codex named, not an artefact: re-applying 0070's lock-free be
    refuter 1: refuted — The finding measures the evidence against claims the headers do not make, and it omits the one after-run that covers the final tree.

1. What the migration header claims (apps/api/migrations/0071_b11_closure_manifest_locks.sql:30-31) is exactly "Codex's interleaving reproduced on the unfixed executor ... and closed here, with a second hold INSIDE the cleanup showing the lock still held". The befor
    refuter 2: refuted — The finding does not show the correction failing a frozen closure condition, losing bytes, regressing, deadlocking, or claiming more than its evidence supports; it misreads the evidence set and the scope of the claims.

1. What the frozen closure and the headers actually claim. B11-F1's frozen closure (audit/reviews/The_Eye_1fa3b08_Closure_and_B11_Bounded_Review.md:72) requires "reproduce this int

[8] harness · LOW · REFUTED (both refuters)
    Assertions in F1 case 1 that pass on the unfixed code, and the one property the harness does not pin (a stray archive removal after the rollback)
    at /Users/halawany/work/personal/mohammed/new_project/apps/api/test/int/phase6-retention-b11-closure.test.ts:145
    claim: Not load-bearing (they hold in the defective interleaving too, because they describe B's committed record and A's pause, both of which exist when the bytes are lost): line 145 (tier 'archive'), 148 (hot copy gone), 150 (B's bytes.removed), 152 (one tier record, B's), 156-162 (A's 409, pause row, revoked approvals, no execution rows, Q untouched). Load-bearing: 146-147 (the archive copy present and verifying), 149 (download verified), 151 (copy_created: true — B's own copy, not an adoption), 154 (bHeldBack). Case 4 (serial control) passes on unfixed code by design. The 2-second wait cannot pass by accident on lock-free SQL (the byte assertions fail first, shown by the pre-0071 run) and cannot
    refuter 1: refuted — The finding has two parts. (1) The list of case-1 assertions that "hold in the defective interleaving too" (closure test lines 145, 148, 150, 152, 156-162) is a description, not a defect: nothing is proven falsely by them, the load-bearing assertions (146-147, 149, 151, 154) are the ones that turned red on the unfixed executor, and no closure condition is failed or overstated by their presence. (2
    refuter 2: refuted — The finding's only claimed gap — "no case pins that NO archive removal happens after the rollback; a regression that re-adds the archive tombstone of copiesMade to the controller's post-rollback cleanup would still pass" — is already covered, deterministically, by an existing control in the correction's own evidence set, and the finding's premise about the closure file does not even hold empirical

[9] harness · LOW · REFUTED (both refuters)
    Temporary directories are created by the harness and never removed (hygiene, no correctness impact)
    at /Users/halawany/work/personal/mohammed/new_project/apps/api/test/int/phase6-retention-b11-closure.test.ts:46
    claim: `VAULT_DIR` (line 46) and every `packageDir` (line 270; seven per run: six F2 cases plus the control and the tampered copy) are created with mkdtempSync under os.tmpdir() and nothing removes them — there is no rmSync in the file, unlike the existing B11 harness which removes its export copies. Each run leaves one vault directory (12-40 KB) and seven package directories behind. Vault isolation itself is correct: the four roots are assigned before boot, config is loaded by the Nest factory at boot (no module in the import graph calls loadConfig() at import time; main.ts/run-bootstrap.ts/reconcile-degraded.ts are not imported), the upload path goes through the app's UploadController and VaultSe
    refuter 1: refuted — The factual core is accurate but it is not a defect under this review's criteria, and two of its supporting claims are wrong.

1. What is true: apps/api/test/int/phase6-retention-b11-closure.test.ts:46 creates VAULT_DIR with mkdtempSync and line 270 creates one package directory per packageDir() call; only mkdtempSync and writeFileSync are imported from node:fs (line 31), there is no rmSync anywhe
    refuter 2: refuted — The factual core of the finding is accurate — apps/api/test/int/phase6-retention-b11-closure.test.ts creates VAULT_DIR with mkdtempSync at line 46 and one package directory per packageDir() call at line 270, imports only { mkdtempSync, writeFileSync } from node:fs (line 31), and nothing removes them (the single `rm(` grep hit is `fault.disarm(` at line 122). But it is not a defect of the correctio

[10] regressions · MEDIUM · CONFIRMED
    demo-restart.sh honours EYE_DEMO_PORT only for the pid lookup and checks, not for the process it starts
    at scripts/ops/demo-restart.sh:18
    claim: PORT="${EYE_DEMO_PORT:-3401}" (line 18) drives api_pid, the kill, the readiness wait and verify(), but the start path (lines 58-61, 71) exports EYE_DB_NAME, EYE_DEGRADED_DIR, EYE_SCHEDULER_ENABLED and EYE_CONNECTOR_PER_SOURCE_CONCURRENCY only — never EYE_RUNTIME_PORT, which is the API's port setting (apps/api/src/config/config.ts:13 default 3401, :96 mapping) and which .eye-local/env does not set (grep count 0). Invoked with a non-default EYE_DEMO_PORT (the shape the author's own rehearsal uses for --verify-only, EYE_DEMO_PORT=3411 EYE_DEMO_DB_NAME=eye_demo_b12), the start path kills whatever listens on :3411, then starts node dist/main.js bound to :3401 with EYE_DB_NAME=$TARGET_DB and no EY
    refuter 1: stands — The finding stands; I could not refute it and demonstrated the defect on a faithful, isolated copy of the script without touching the live API, eye_demo or any Redis.

The code: scripts/ops/demo-restart.sh:18 sets PORT="${EYE_DEMO_PORT:-3401}" unconditionally (both modes), and PORT drives api_pid (line 31), the kill (66), the stop wait (67-68), the readiness wait (72) and verify() (40, 44). The st
    refuter 2: stands — The finding is accurate and I reproduced it on a verbatim copy of the script without touching the demonstration. scripts/ops/demo-restart.sh derives PORT from EYE_DEMO_PORT (line 18) and uses it for the pid lookup (31), the kill (66), the readiness wait (72) and verify() (40, 44), but the start path exports only EYE_DB_NAME, EYE_DEGRADED_DIR, EYE_SCHEDULER_ENABLED and EYE_CONNECTOR_PER_SOURCE_CONC

[11] regressions · LOW · REFUTED (both refuters)
    0071 header claims a detected deadlock pauses the execution as infrastructure; it answers a bare 500 and leaves the action approved
    at apps/api/migrations/0071_b11_closure_manifest_locks.sql:36
    claim: Lines 35-38 say a deadlock PostgreSQL detects 'fails that execution as an infrastructure pause and is retried'. A 40P01 raised inside begin_execution (where the locks are taken, before the state moved) is not a RetentionExecutionRolledBack and does not match the controller's only other pause branch, the /^retention execution rejected \((rights_changed|scope_changed|references_changed)\)/ regex (apps/api/src/retention/retention.controller.ts:140-152, which otherwise rethrows), and asObservationRefusal maps only ['23514','23503','22023','23505','42501','2F002','P0R01','P0R02'] (apps/api/src/observation/observation-errors.ts:453) — no 40P01 — so the route answers 500 with no pause, no failure c
    refuter 1: refuted — The finding's reading of the code is accurate but it does not describe a defect of any kind the review admits (a failed frozen closure condition, lost or corrupted bytes, a regression, a deadlock, or a proof claiming more than it shows). It is, by its own author's classification, "a records inaccuracy in the migration header rather than a reachable loss" about a scenario the finding itself establi
    refuter 2: refuted — The finding reads the code correctly but does not identify a defect in the correction. Verified chain: retention.service.ts:129 calls cap.beginExecution OUTSIDE the executor's try (opened at :133), so an error raised inside begin_execution propagates as-is; retention.controller.ts:140-152 catches only RetentionExecutionRolledBack and the /^retention execution rejected \((rights_changed|scope_chang

[12] regressions · LOW · CONFIRMED
    Runbook §4 step 2 migrate command fails as written: the migrator does not read .eye-local/env
    at docs/ops/DEMONSTRATION_RUNBOOK.md:51
    claim: Step 2 tells the operator to run `cd apps/api && EYE_DB_NAME=eye_demo node scripts/migrate.mjs` with the note '(the migrate credentials come from .eye-local/env)'. apps/api/scripts/migrate.mjs reads process.env only (lines 19-32) and exits 1 when EYE_DB_MIGRATE_PASSWORD is absent; it never loads the env file. The runbook's own restart script sources the file first (demo-restart.sh:57 `set -a; . .eye-local/env; set +a`) but the migrate step omits that, so an operator following §4 literally gets a refusal at the step that must precede the restart of a migrated batch.
    refuter 1: stands — The finding reads the code correctly and the defect is demonstrable, so I cannot refute it. apps/api/scripts/migrate.mjs (lines 19-25) reads process.env only and exits 1 before any connection when EYE_DB_MIGRATE_PASSWORD is absent; it has no dotenv/--env-file/local-env loader (grep for env-file/local-env in migrate.mjs, apps/api/package.json and package.json finds only the error-message text). pnp
    refuter 2: stands — The finding is accurate and demonstrable; none of the refutation grounds apply.

1. It does not misread the code. apps/api/scripts/migrate.mjs reads process.env only: lines 19–25 exit 1 when EYE_DB_MIGRATE_PASSWORD is unset (before pg.Client is constructed at line 27 and before client.connect() at line 62). Nothing in the file imports dotenv or scripts/local-env.mjs. `pnpm db:migrate` is a bare `n

[13] regressions · LOW · CONFIRMED
    demo-restart.sh env parsing splits on spaces and prints truncated vault roots
    at scripts/ops/demo-restart.sh:35
    claim: proc_env pipes `ps -E -p <pid> -o command=` through `tr ' ' '\n'` (line 35), so any value containing a space is cut at the space. A vault root such as /tmp/with space/evidence is reported as EYE_VAULT_EVIDENCE_ROOT=/tmp/with — the operator is told the process runs with a root it does not have. The verdict is unaffected (only EYE_DB_NAME, which cannot contain a space, decides pass/fail), but the roots are exactly what §7 tells the operator to read from this output, and an archive moves bytes under them.
    refuter 1: stands — The finding is a real defect in the script as it was raised (scripts/ops/demo-restart.sh:35 with `ps -E -p <pid> -o command= | tr ' ' '\n'`), and I reproduced it exactly: macOS `ps -E` joins environ entries with single spaces (od dump shows no other delimiter), so `tr ' ' '\n'` cuts any value at its first space; a throwaway node process started with EYE_VAULT_EVIDENCE_ROOT='/tmp/with space/evidenc
    refuter 2: stands — The finding is a real defect against the line it cites and I could not refute it; it reproduces exactly as claimed. Against the reviewed version of scripts/ops/demo-restart.sh (proc_env, line 35: `ps -E -p "$pid" -o command= | tr ' ' '\n'`), a process started with EYE_VAULT_EVIDENCE_ROOT="/tmp/with space/evidence" is reported as `EYE_VAULT_EVIDENCE_ROOT=/tmp/with`, and the remainder (`space/eviden

[14] regressions · LOW · REFUTED (both refuters)
    Stale comment on the upgrade proof's ledger count after the 0071 bump
    at scripts/phase1/verify-0022-upgrade.mjs:198
    claim: The diff moves 'public.schema_migrations' from 49 to 50 (line 199) but leaves the line-198 comment 'one ledger line per migration applied above the ceiling (0022–0070)'; 50 lines are 0022–0071. The ledger claim under test is now described by a range that yields 49, contradicting the value beside it; the next migration's author cannot tell which is authoritative.
    refuter 1: refuted — The factual observation is accurate but it is not a defect under this review's definition. The finding is a comment inconsistency only: scripts/phase1/verify-0022-upgrade.mjs:198 still reads "(0022–0070)" while line 199 was bumped to 50 — and line 199 itself carries the inline note "// + 0071 (the B11 closure: the manifests' locks)". Read together, the pair says 0022–0070 (49) + 0071 = 50, which i
    refuter 2: refuted — The finding is a stale-comment observation, not a defect under the review's terms (fails a frozen closure condition, loses/corrupts bytes, regresses behaviour, deadlocks, or proves less than claimed). The only thing the proof consumes is the numeric value at line 199 (verify-0022-upgrade.mjs:386-393 compares `INTENDED_ADDITIONS[t]` to the observed row additions and goes red on a mismatch); the lin

[15] regressions · LOW · CONFIRMED
    The cited after-evidence predates the executor's 'already archived' branch and the sixth harness case
    at apps/api/src/retention/retention.service.ts:208
    claim: The evidence files named for the correction are of an earlier state of the same uncommitted change: closure-after.txt (5 tests, started 21:01:59) and b11-suites-after.txt (39 tests, 21:02:07) were produced before retention.service.ts and phase6-retention-b11-closure.test.ts were last modified (both 21:11:26 — the ALREADY ARCHIVED branch at line 208 ff. and the 'OVERLAP THAT SUCCEEDS' case). The full-suite run in the same directory (int-all-1.txt, 990/990, started 21:03:33) likewise predates that branch, so no full-suite evidence covers the executor as it now stands; only closure-and-b11-after-2.txt (45 tests, 21:11:42) does. The claim 'green after' is therefore proven for the current code on
    refuter 1: stands — The finding is factually correct and demonstrable, and it is a "proves less than it claims" defect: the after-evidence the correction cites was produced on an earlier state of the same uncommitted change. closure-after.txt (Start 21:01:59, 5 tests) and b11-suites-after.txt (21:02:07, 39 tests) were written before retention.service.ts and phase6-retention-b11-closure.test.ts were changed at 21:11:2
    refuter 2: refuted — The finding's observation is accurate, but it is not a defect of the correction: it shows no failure of a frozen closure condition, no byte loss, no regression, no deadlock, and nothing in the repository binds a claim to the stale files. The orchestrator handed reviewers scratchpad working files; the correction itself (code, 0071, harness, verifier, runbook, requirement rows) makes no claim on the

[16] locks · MEDIUM · CONFIRMED
    The manifest lock ends with the backend, not at the cleanup boundary: a terminated session lets B adopt A's copy, then A's in-transaction cleanup removes B's committed bytes
    at apps/api/src/retention/retention.service.ts:146
    claim: 0071's closure rests on 'an aborted transaction keeps its locks until it is rolled back' (apps/api/migrations/0071_b11_closure_manifest_locks.sql:24), but a transaction-scoped advisory lock lives exactly as long as the backend. When A's session is terminated while A is between the copy and the record (PostgreSQL 18 here: idle_in_transaction_session_timeout, transaction_timeout, a failover, pg_terminate_backend — all FATAL 57P01), the lock is gone while A's Node-side execution is still in flight. B then starts, finds A's archive copy (copy_created: false), records the move, commits and removes the hot copy. A resumes, its next statement fails on the dead connection, and execute()'s catch (ret
    refuter 1: stands — The finding stands against the correction as reviewed. At my first read (working tree at 21:11), apps/api/src/retention/retention.service.ts:146-155 removed every locator in copiesMade unconditionally (`for (const l of copiesMade) await this.vault.tombstone('archive', scope, l)`) inside execute()'s catch, and the controller was HEAD's version (only `e instanceof RetentionExecutionRolledBack` → pau
    refuter 2: stands — NOT REFUTED. The finding is a real defect of the correction as reviewed, and it fails the frozen closure condition literally ("Scope any locking or ownership mechanism through the relevant filesystem cleanup boundary; a transaction lock that expires before cleanup alone is insufficient", audit/reviews/The_Eye_1fa3b08_Closure_and_B11_Bounded_Review.md:72).

What the correction relied on and why it 

[17] locks · MEDIUM · REFUTED (both refuters)
    A deletion now takes one lock-table slot per manifest with an unbounded source-selector scope: a large approved deletion can never execute (out of shared memory at begin_execution) and a merely big one starves the cluster's lock table for its whole run
    at apps/api/migrations/0071_b11_closure_manifest_locks.sql:87
    claim: begin_execution (0071:87–89) takes pg_advisory_xact_lock for every executable manifest of an archive or a deletion. Archive/export scopes are capped at 200 ids (retention.service.ts:75), but a deletion's source selector is unbounded: resolve_scope's SELECT DISTINCT over observation.blob_manifests by source_id (0070:546–562) has no LIMIT and admits every corrected/superseded/withdrawn manifest of the source. Each advisory lock is a heavyweight lock-table entry; the table is cluster-wide and sized max_locks_per_transaction × (max_connections + max_prepared_transactions) = 64 × 100 = 6,400 on this cluster. A deletion over ~6,000+ retired manifests therefore fails at begin_execution with 'out of
    refuter 1: refuted — The finding misreads the correction as it stands in the working tree. begin_execution in apps/api/migrations/0071_b11_closure_manifest_locks.sql:117–131 does NOT take one advisory lock per executable manifest unconditionally: it first counts the executable manifest items (line 118) and, for an archive or a deletion with v_n > 256, takes ONLY the domain's movers lock exclusively (line 119–120) and 
    refuter 2: refuted — The finding misreads the correction: it was measured against an earlier draft of 0071 that no longer exists in the working tree. The current apps/api/migrations/0071_b11_closure_manifest_locks.sql (lines 117–131) counts the executable manifest items first (v_n) and, for an archive or a deletion naming MORE THAN 256 manifests, takes ONE transaction-scoped advisory lock — the domain's movers lock, e

[18] cleanup · MEDIUM · CONFIRMED
    In-transaction cleanup removes archive copies without verifying the transaction (and its manifest locks) still exists; a session ended server-side during the copy window loses another action's committed bytes
    at apps/api/src/retention/retention.service.ts:148
    claim: The correction's B11-F1 invariant — 'the lock spans the filesystem cleanup' — holds only while the executing transaction is still alive when the catch block runs. Nothing in execute()'s catch (lines 148–152) checks that; it removes every locator in copiesMade unconditionally, on the JS side, whether the PostgreSQL transaction is open, aborted-but-open, or already gone. Between the tier read and the port call the transaction is IDLE in transaction for the whole duration of vault.copyBlob (read, write, fsync, rename, dir fsync, read-back — seconds for a large evidence file). If the backend ends in that window (pg_terminate_backend by an operator, idle_in_transaction_session_timeout on the serv
    refuter 1: refuted — The finding misreads the current working tree: it describes a catch block that "removes every locator in copiesMade unconditionally", but apps/api/src/retention/retention.service.ts:148–162 as it stands does not. Line 156 asks `cap.holdsManifestLock(...)` on the transaction's OWN connection before each removal and removes only when the answer is `true` or the aborted-transaction error 25P02 (a liv
    refuter 2: stands — The finding as literally written is stale, but the defect it names — the in-transaction cleanup removing an archive copy under a manifest lock the transaction does not actually hold, so a concurrent action's committed bytes are lost — is present in the CURRENT working tree and I reproduced it through the governed path. So I cannot refute it.

WHAT IS STALE. The finding's harness and log are from 2

[19] cleanup · LOW · REFUTED (both refuters)
    When the pipeline's ROLLBACK itself fails (dead connection), the RetentionExecutionRolledBack — its failure class and the export's package-directory cleanup — is replaced by the rollback error: no pause is recorded and the action stays 'approved'
    at apps/api/src/retention/retention.controller.ts:140
    claim: Kysely's transaction().execute rethrows the ROLLBACK's error, not the handler's, when the rollback fails. After a connection loss the executor's catch still throws RetentionExecutionRolledBack (retention.service.ts:158) carrying the failure class and, for a customer export, the cleanup that removes the package directory; the pipeline's rollback on the dead client then throws 'Client has encountered a connection error and is not queryable', pipeline.write rethrows that, and the controller's branch at line 140 does not match — the route ends in a bare 500 with no pause_action, the action remains 'approved' with failure_class null (observed in the same run for action A), and an export's package
    refuter 1: refuted — The finding describes the HEAD (44b01bb) shape of the controller, not the uncommitted correction under review. Its premise about Kysely is correct — in kysely 0.29.4 `dist/kysely.js:685-690` a throwing `rollbackTransaction(connection)` rejects before `throw error`, so the ROLLBACK's error replaces the handler's RetentionExecutionRolledBack — but the working-tree controller already handles exactly 
    refuter 2: refuted — The finding describes a defect of an intermediate state of the working tree, not of the correction under review. Its evidence run (scratchpad/b12/review/ixc/lockloss-run.txt) is stamped 21:23:18; apps/api/src/retention/retention.controller.ts and retention.service.ts were both modified at 21:33:21, after that run, and the uncommitted diff of the controller (git diff HEAD) adds exactly the connecti

=== 19 findings; 10 confirmed, 9 refuted ===

=== THE CORRECTIONS (applied to the same unapplied migration and tree before the demonstration) ===
  · the lock ends with the backend (locks/cleanup/port findings) — FIRST correction (round 1): the executor proved the manifest's lock on the transaction's own connection before each removal and read 25P02 as held; the controller kept the executor's verdict when the ROLLBACK itself failed; every pool client got an 'error' listener (shared/db.ts); the harness gained the dead-backend case. THE RE-JUDGEMENT REFUTED THE PREMISE: PostgreSQL releases a transaction's locks at the abort itself (a cancelled statement, a timeout, a deadlock), so 25P02 means the lock is GONE; and a check-then-act window remained between the proof and the unlink on a lost backend. SECOND correction (round 2): OWNERSHIP replaces the lock at the cleanup boundary — an archive copy is STAGED under its execution ATTEMPT's own name (<locator>.staging-<attempt_id>) and published under the locator only after the commit that recorded the move; a rollback removes only its own attempt's file; readArchived finds a recorded-but-unpublished copy under the manifest's digest; a failed publish keeps the hot copy and a pending residual (the retry publishes any staged copy under the digest); a deletion retires staged copies; the execution runs in a subtransaction so a cancelled statement leaves the transaction usable for the failure record; the item branches roll back to the item savepoint only while it exists; the round-2 judges' residual — a second attempt of the same action sharing a per-action name — closed by the per-attempt name and a harness case (a lost backend, then a second execute of the same action).
  · one lock per manifest for an unbounded scope (locks): the domain's MOVERS advisory lock (shared) plus per-manifest locks up to 256 executable manifests; above that the movers lock exclusively and no manifest lock; every execution takes the domain lock first (no wait cycle).
  · a deadlock at the start answered a bare 500 (regressions): 40P01 and 55P03 raised before the state moved pause the action infrastructure for a retry.
  · the verifier digested a substituted [] for a non-list `excluded` (verifier): `excluded` must be a list and enters the chain as listed; an unreadable listed file or an unlistable directory is a failed check, not a crash.
  · disarm() could not release a fired hold (harness): fired holds are tracked and released by disarm().
  · demo-restart.sh (regressions): the started process receives EYE_RUNTIME_PORT; the macOS environment is split before each ' NAME=' token, not at every space.
  · the runbook's migrate step (regressions): the handoff is sourced before the migrator (it reads the process environment only).
  · the upgrade proof's comment (regressions): 0022–0071.
  · the harness's temp directories (harness): removed in afterAll; every evidence file re-run at the final tree.
  · an empty package with a valid chain verifies OK, and bound_to's vacuous restatement when both sides omit the fields (verifier, both refuted): left as observations — the product never records an empty package; the binding's fields are always written.

=== THE RE-JUDGEMENT of every confirmed finding against the corrected tree (two judges each; CLOSED only when both say closed) ===
  #1 CLOSED
    judge1: closed — The defect was that scripts/retention/verify-export.mjs coerced a non-array `excluded` to `[]` before digesting the chain, so a manifest whose recorded `excluded` was `[]` could have that member replaced by null, an object, a string, or deleted, and still print PASS chain / PASS authenticity / PACKAGE OK with exit 0 against the product's recorded package digest. In the working tree (uncommitted, g
    judge2: closed — The defect the finding names — the verifier feeding a substitute `[]` into the package-digest chain whenever `manifest.excluded` is not an array, so an edited manifest verifies PACKAGE OK against the product's recorded digest — can no longer occur in the working tree.

Code (scripts/retention/verify-export.mjs, uncommitted diff vs HEAD 44b01bb): the coercion at old line 170 is replaced at line 178
  #2 CLOSED
    judge2: closed — The defect the finding names — an uncaught fs exception on a listed <manifest_id>.bin that is a directory (EISDIR) or unreadable (EACCES), killing the process before the verdict block so that text mode prints no PACKAGE FAILED line and --json mode prints no document — can no longer occur in the working-tree verifier. Every filesystem call in /Users/halawany/work/personal/mohammed/new_project/scrip
    judge1: closed — The defect the finding names — an uncaught filesystem exception at the per-object read (or the directory listing) that kills the verifier before the verdict block, leaving exit 1 with an empty stdout (no PACKAGE FAILED line, no JSON document, no named failing check) — can no longer occur in the working tree.

Code (scripts/retention/verify-export.mjs, working tree, per `git diff HEAD`): the per-ob
  #3 CLOSED
    judge2: closed — The defect named — a lost backend releases the manifest's transaction-scoped advisory lock while the executor still has its copy in copiesMade, the catch then removes that copy unconditionally (a copy another execution has meanwhile adopted and committed), and the action is left 'approved' with no pause because the failed ROLLBACK's error replaces the executor's verdict — can no longer occur on th
    judge1: closed — Judged on the working tree as it stood at 23:18 (the tree moved twice during this review; the state judged is pinned by md5: apps/api/src/retention/retention.service.ts f9d11803cd23d3f44dbf9669db2aa536, retention.controller.ts 11d4068c2e12359a60ab7c9fb43e5d3a, retention.capabilities.ts identical to HEAD, apps/api/src/shared/db.ts f7af5ce7767c260ff2f3851c4040246a, apps/api/src/observation/vault/vau
  #4 CLOSED
    judge2: closed — The defect the finding names — pause() removed the hold from the map before awaiting it, so disarm()'s release loop could only ever reach holds that were armed and never fired, leaving a parked execution (its transaction open, its manifest locks held, a commit-pool client checked out) untouched by afterAll — can no longer occur in the working tree. apps/api/src/observation/fault-injection.ts now k
    judge1: closed — The defect the finding names — a hold that has fired is removed from `holds` before the code under test parks on `await h.released`, so `disarm()`'s release loop can never reach it and the comment's guarantee ("the code under test never waits on a test that has moved on") was false for the fired case — can no longer occur on the working tree. In apps/api/src/observation/fault-injection.ts (uncommi
  #5 CLOSED
    judge1: closed — The defect was that PORT (from EYE_DEMO_PORT, default 3401) drove api_pid, the kill, the readiness wait and verify(), while the process the script launched never received EYE_RUNTIME_PORT — the only setting the API's port comes from — so the launch and the verification were decoupled whenever an override or an inherited EYE_RUNTIME_PORT was in play. The working-tree script (scripts/ops/demo-restar
    judge2: closed — The defect the finding names — the start path exporting EYE_DB_NAME etc. but never EYE_RUNTIME_PORT, so the process the script launches binds whatever the shell carries (default 3401) while the kill, the readiness wait and verify() use $PORT — can no longer occur. In the working tree, scripts/ops/demo-restart.sh line 62 now reads `export EYE_RUNTIME_PORT="$PORT"`, placed unconditionally on the sta
  #6 CLOSED
    judge2: closed — The defect the finding names — an operator following §4 step 2 literally from a shell that has not sourced .eye-local/env is refused by the migrator — can no longer occur through the runbook's text. docs/ops/DEMONSTRATION_RUNBOOK.md lines 52–54 now state the reason ("The migrator reads the process environment only, so the local secret handoff is sourced first") and give the step as `set -a; . .eye
    judge1: closed — The finding named a documentation defect: the runbook's §4 step 2 told the operator to run the migrator bare (`cd apps/api && EYE_DB_NAME=eye_demo node scripts/migrate.mjs`) with a parenthetical implying the migrator loads `.eye-local/env` itself, while `apps/api/scripts/migrate.mjs` reads `process.env` only and exits 1 before connecting when `EYE_DB_MIGRATE_PASSWORD` is absent. The correction is 
  #7 CLOSED
    judge1: closed — The defect the finding names — the macOS branch of proc_env piping `ps -E -p <pid> -o command=` through `tr ' ' '\n'` so any value with a space is cut at the space and the operator is shown a root the process does not have — can no longer occur in the working tree. The on-disk scripts/ops/demo-restart.sh (untracked; the cited line 35 is now the comment, the pipeline is lines 37-39) splits the ps -
    judge2: closed — The defect the finding names — the Darwin branch of proc_env piping `ps -E -p <pid> -o command=` through `tr ' ' '\n'` so any root containing a space is reported cut at the space — can no longer occur in the working tree. scripts/ops/demo-restart.sh (untracked, mtime 2026-09-14 21:35:11) now splits the ps -E line before every ' NAME=' token (`sed -E 's/ ([A-Za-z_][A-Za-z0-9_]*=)/\<newline>\1/g'`, 
  #8 OPEN
    judge1: closed — The defect the finding names is that the after-evidence the correction cites was produced on an earlier state of the same uncommitted change, so no cited (and no full-suite) evidence covered the executor's ALREADY ARCHIVED branch and the harness as they stand. That is no longer the case. The tree's last code edits are 0071 at 21:32:38, retention.service/.capabilities/.controller and fault-injectio
    judge2: open — The correction as the prompt describes it WAS adequate for the tree it describes, but the tree moved again while I judged, and the defect the finding names — after-evidence produced on an earlier state of the same uncommitted change than the executor and the harness as they now stand — has recurred; the tree as it stands is moreover red on its own newest case, so no "green after" claim can be true
  #9 OPEN
    judge2: open — The demonstrated path of the finding is closed, but the defect its title names — a terminated session lets B adopt A's copy and A's in-transaction cleanup then removes B's committed bytes — can still occur on the current tree, in a narrower window that the correction's mechanism cannot reach.

WHAT IS CLOSED (verified on a fresh database, eye_verify_judge_j2f9, migrated 0022–0071 from the working 
    judge1: closed — Verdict pinned to the working tree as it stood from 23:05:46 to 23:09:00 and unchanged through my runs (23:10–23:13): retention.service.ts f9d11803cd23, retention.controller.ts 11d4068c2e12, retention.capabilities.ts 02b758999560, vault.service.ts 779715bd3977, evidence.service.ts 748195bcbbff, 0071 ebc696c1a82d, phase6-retention-b11-closure.test.ts b9d9daeebff1, db.ts f7af5ce7767c (md5 prefixes; 
  #10 OPEN
    judge1: open — The defect the finding names is "the in-transaction cleanup removes a copy under a manifest lock the executing transaction no longer holds, so another action's committed bytes are lost" — the residue the frozen closure of B11-F1 forbids ("a transaction lock that expires before cleanup alone is insufficient ... after A fails, B's committed evidence must still retrieve and verify"). The correction c
    judge2: open — Partly corrected, not closed. The terminated-backend variant the title names is closed: retention.service.ts:156 now asks retention.holds_manifest_lock on the transaction's own connection before each removal, a lost connection yields a code-less pg error, the copy is LEFT IN PLACE and named in the pause reason, and the repository harness (phase6-retention-b11-closure.test.ts:257) proves it; both r
=== re-judged: 7 closed, 3 open of 10 ===

=== ROUND 2 — against the staged-copy tree: two judges on each of the two findings left open by the re-judgement, then three finders (crash windows, the readers, the contracts) with two refuters each on the staged-copy design ===
  judge1:the cleanup removes without verifying th: OPEN — THE NAMED INTERLEAVING IS CLOSED. Under the staged-copy design, action B's copyBlob reads only the PUBLISHED locator (vault.service.ts copyBlob: `readFile(published)`; it never looks at a `.staging-*` file), so B cannot adopt A's uncommitted copy; A's cleanup calls `removeStaged(scope, locator, a.actionId)`, which removes only `<locator>.staging-<A.action_id>` (retention.service.ts execute catch, lines 156–163); the 25P02-as-held premise and holdsManifestLock are gone from the executor (grep: no 25P02/holdsManifestLock in retention.service.ts; holds_manifest_lock is used only inside archive_bl
  judge1:the lock ends with the backend: OPEN — The staged-copy design closes the interleaving the finding literally names (two DIFFERENT actions): a copy is written as <locator>.staging-<action_id>, copyBlob adopts only a copy already PUBLISHED under the locator (readFile(published)), archive_blob records only under the lock, the controller publishes (rename) only after the commit, and execute()'s catch removes only removeStaged(scope, locator, a.actionId) — a file bearing its own action id. Nothing depends on the lock surviving to the removal, so the lost backend and the top-level abort no longer let action A remove action B's file: the r
  judge2:the cleanup removes without verifying th: OPEN — The staged-copy design closes the finding for OVERLAPPING DISTINCT ACTIONS, which is the interleaving the finding and Codex's frozen closure describe: a copy is staged as <locator>.staging-<action_id> and published under the locator only after the commit that recorded the move (retention.controller.ts execute → publishCopies after pipeline.write returns); copyBlob's idempotent early return reads only the PUBLISHED locator, so another action never adopts an uncommitted staged file for its own copy (B makes its own copy — copy_created:true in both the BACKEND and ABORT harness cases); the tier==
  judge2:the lock ends with the backend: OPEN — JUDGE 2 verdict on confirmed-findings[8] ("the lock ends with the backend"): NOT CLOSED — narrowed, not eliminated.

WHAT THE STAGED-COPY DESIGN CLOSES. The literal A/B interleaving of the finding is closed. An archive copy is now staged as `<locator>.staging-<action_id>` (vault.service.ts copyBlob/stagingPathFor), published under the locator only by the controller after the commit (publishCopies → publishArchiveCopy), and a rollback removes only `removeStaged(scope, locator, a.actionId)` — its own name (retention.service.ts:156–163). copyBlob never adopts a staged file of another action (it c
  (both judges of each open finding named the same residual — the staged name was per ACTION, so a second attempt of the same action, admitted after the first's backend was lost, shared it and the first's late cleanup removed the second's file — corrected by the per-ATTEMPT name and the harness case 'a SECOND ATTEMPT of the same action'; the author judged the correction on the harness, 9/9, and the round-2 finders' findings below against the corrected tree)
  [R2-1] readers · MEDIUM · CONFIRMED
      A deletion's staged archive copies are invisible to verification and unreachable by the retry route: DeletionVerified can be published with a complete copy of the bytes still on disk
      at /Users/halawany/work/personal/mohammed/new_project/apps/api/src/retention/retention.service.ts:414
      claim: The deletion executor retires a manifest's bytes from evidence, archive and the staged names (line 208, stagedToo: true), but (a) retryBytes' deletion branch (lines 414-415) builds its entries as evidence + archive with NO stagedToo, so a refused staged-copy removal is never retried, and (b) observeForVerification (lines 463, 470) computes bytes_present = exists(evidence) || exists(archive) on the published locator only, so a surviving `<locator>.staging-<attempt>` file is invisible. retention.verify_action (0070 §6, line 877) then passes the item on bytes_present=false, moves the pending resi
      refuter 1: stands — NOT REFUTED. The finding correctly describes the tree it reviewed, and the precondition state is reachable through the governed path.

What I confirmed by reading the reviewed version (retention.service.ts 7eaf8bed…, vault.service.ts 5d85e275… — the hashes matched at the start of my session): (a) re
      refuter 2: stands — NOT refuted. The finding was accurate for the reviewed tree and its unsafe outcome is still demonstrable, in a narrower form, on the tree as it stands now.

1. The reviewed code (retention.service.ts 7eaf8bed…, vault.service.ts 5d85e275… — the hashes I read at the start of this check) matched every 
  [R2-2] readers · LOW · CONFIRMED
      restore.sh's blob verification reads only <tier>/<locator>: a live archived manifest whose publish is pending is counted ABSENT (strict check fails on eye_demo; boundary reconciliation names it 'lost') although its bytes are on disk twice
      at /Users/halawany/work/personal/mohammed/new_project/scripts/ops/restore.sh:807
      claim: verify-blobs.mjs (embedded at lines 798-812) resolves each live manifest to join(vaultRoot, vault, locator) where vault = observation.manifest_tier(m) (tier_vault_expr, lines 820-824). In the pending-publish state that 0071 deliberately creates — the move recorded (tier = archive), the copy at `<archive>/<locator>.staging-<attempt>`, the hot copy KEPT at `<evidence>/<locator>` until the publish succeeds — the verifier looks only at `<archive>/<locator>`, finds nothing, and counts the manifest absent. For a database in STRICT_BLOB_DBS (default eye_demo, line 79) the check at lines 833-834 fails
      refuter 1: stands — NOT REFUTED. The pending-publish state is a designed, durable state of the governed path, and restore.sh's verifier does not know it exists.

The state is real and reachable through the governed path. 0071's header (apps/api/migrations/0071_b11_closure_manifest_locks.sql lines 24-38) says the copy i
      refuter 2: stands — Not refuted: the defect is real, reachable through the governed path, and not covered by any existing control. (1) The code reads as claimed: restore.sh's embedded verify-blobs.mjs (lines 798-812) reads only join(vaultRoot, vault, locator), where vault comes from tier_vault_expr (lines 820-824, `cas
  [R2-3] readers · LOW · CONFIRMED
      After a failed publish whose staged copy does not verify, the hot copy kept for exactly this case is read by no reader and re-copied by no route: intact evidence is unretrievable and the next identical poll admits a duplicate
      at /Users/halawany/work/personal/mohammed/new_project/apps/api/src/observation/vault/vault.service.ts:436
      claim: The controller keeps the hot copy when publishArchiveCopy fails (retention.controller.ts, the unpublished filter before removeBytes) and records a residual for the retry. readArchived (line 436) consults the published locator and the staged names only — never the evidence root — even though the tier ledger's 'archive' is, in this state, a record whose served copy is not yet there. If the staged copy is unusable (publishArchiveCopy answered 'corrupt' on the staged digest, or the post-rename read-back was corrupt so the published copy exists with the wrong digest), then: download → readArchived 
      refuter 1: refuted — Verdict against the tree as it stands at the time of this check (vault.service.ts mtime 23:40:02, retention.service.ts 23:40:16 — both rewritten AFTER the finding's scratch run at 23:35 and after my own first read of the same functions; the orchestrator's evidence files closure-after-7/b11-suites-af
      refuter 2: stands — NOT REFUTED. The finding is an accurate reading of the reviewed tree and the state it describes is reachable through the governed executor; the only reason it no longer reproduces end-to-end is that the working tree was edited DURING this refutation to correct it.

1. Code, as of the reviewed tree (
  [R2-4] readers · LOW · CONFIRMED
      The archive root is never reconciled: staged copies orphaned by a lost process or a failed cleanup persist indefinitely when the action is withdrawn; the sweeper walks the evidence root only
      at /Users/halawany/work/personal/mohammed/new_project/apps/api/src/observation/sweeper/sweeper.service.ts:215
      claim: reconcileOrphanCandidates lists join(rootFor('evidence'), tenant, domain) only; no code path lists the archive root except per-locator (readArchived, stagedCopies, removeAllStaged). A staged file outlives its attempt when the API process dies between copyBlob and the executor's cleanup, or when removeStaged is refused (retention.service.ts:165, recorded in the pause reason only). The header and the code say such a file is 'retired with the bytes by a deletion, or by the publish of another attempt's copy' (retention.service.ts:158); neither happens when the paused action is withdrawn and the ma
      refuter 1: stands — The finding stands; I could not refute it. (1) The code read confirms every cited fact: sweeper.service.ts:215 walks only join(rootFor('evidence'), tenant, domain); a grep of apps/api/src for readdir finds the archive root listed nowhere except per-locator (vault.service.ts stagedCopies/removeAllSta
      refuter 2: stands — Not refuted. The finding is accurate on the code and I reproduced it through the governed path on a fresh database and an isolated vault; it is low-severity, bounded, and — importantly for the judge — a gap the tree's own records already declare rather than a hidden defect of the correction.

What t
  [R2-5] contracts · MEDIUM · CONFIRMED
      A second archive of an already-recorded manifest removes the hot copy while the archive copy is only staged; that action can never verify by its own retry
      at apps/api/src/retention/retention.service.ts:262
      claim: The already-archived branch (tierNow.tier === 'archive', line 229) treats a tier record as proof of a published copy and still pushes the manifest's evidence locator for post-commit removal (line 262). When the recording action A committed but its publish failed (a pending residual on A, the hot copy deliberately kept, the copy staged under A's attempt), a second approved archive B of the same manifest — approved while it was hot, as A9 allows — executes 'already archived', commits, and its controller removes the hot copy. The manifest then exists only as A's staged file. This contradicts 0071
      refuter 1: stands — NOT REFUTED against the tree the finding was filed on; the defect is real, contained (no byte loss), and a correction for it landed in the working tree while this review ran.

(1) The reviewed code (the hashes the c2 probe bound — vault.service.ts 4374643188285b8c1b5bdbd377dc2857, retention.service.
  [R2-6] contracts · LOW · REFUTED (both refuters)
      The retry route's response reports nothing failed when the publish (and the new re-copy) fails again
      at apps/api/src/retention/retention.controller.ts:129
      claim: For an executed archive with a pending residual, retryBytes (retention.service.ts lines 411–424) swallows a failed publishArchiveCopy (.catch(() => undefined)) and a failed re-copy (.then(..., () => false)); when the archive copy still does not verify it simply pushes no removal entry. removeBytes then reports removed:[] failed:[], and the controller answers {retried:true, pending:1, bytes:{removed:[], failed:[]}} — the operator is told the retry ran with no failure while nothing was published or removed and the residual is still pending. The failure reason is not carried anywhere in the respo
      refuter 1: refuted — Refuted against the tree as it stands now — the finding described the previous shape of retryBytes, which the working tree no longer contains.

What the finding says: retryBytes swallows a failed publish and a failed re-copy, pushes no removal entry, and the controller answers {retried:true, pending
      refuter 2: refuted — The finding describes a response contract that is no longer in the tree. Its evidence (probe-run-6-x1.txt, 23:42:15) and the code it quotes (retryBytes pushing no entry when the archive copy could not be published, so removeBytes answered removed:[] failed:[]) match the version of apps/api/src/reten
  [R2-7] contracts · LOW · CONFIRMED
      0071's >256-manifest rule and begin_execution's own comment are false for a customer export: an export naming more than 256 manifests holds no manifest locks and no exclusive movers lock
      at apps/api/migrations/0071_b11_closure_manifest_locks.sql:122
      claim: The header (line 9) says 'an execution naming more than 256 manifests takes the domain's movers lock EXCLUSIVELY instead', and the lock block's comment (line 116) says 'an archive or a deletion of the same manifest waits for the export to commit'. The code takes the exclusive movers lock only for a.kind IN ('archive','deletion') AND v_n > 256 (line 122); a customer_export with v_n > 256 takes the shared movers lock and, because of IF v_n <= 256 (line 126), no manifest locks at all. An export can exceed 256 manifests: 0070's resolve_scope sets v_preserving := a.kind IN ('review','archive','cust
      refuter 1: stands — The finding stands. Code reading: 0071's lock block (apps/api/migrations/0071_b11_closure_manifest_locks.sql lines 120-131) takes the domain's movers lock EXCLUSIVELY only for `a.kind IN ('archive','deletion') AND v_n > 256`; every other case takes the movers lock SHARED, and the per-manifest loop r
  [R2-8] contracts · LOW · CONFIRMED
      The closure harness's ABORT case cannot fail on the lock claim it names: bHeldBackThroughAsCleanup is guaranteed by the sleeping trigger on B's own record
      at apps/api/test/int/phase6-retention-b11-closure.test.ts:337
      claim: The case creates a BEFORE INSERT trigger on retention.executions that sleeps 12 s for every row (line 311) and drops it only in finally after pB is awaited. B's own recordExecution therefore sleeps 12 s, whereas A's 409 arrives sub-second after the cancel (rollback to savepoint, staged removal, pipeline rollback, pause write). bDone sampled right after await pA (line 324) is false whether or not A's locks survived the abort — with no lock at all B would have reached its insert within ~100 ms of starting and still be sleeping. Nothing checks holdersOf(P) after pg_cancel_backend; the only lock a
      refuter 1: stands — Not refuted. The finding is correct on its central claim and on both side claims.

1. The assertion is vacuous with respect to the lock. The trigger the case creates (lines 310–311) is unconditional on retention.executions and is dropped only in `finally` after `await pB`, so B's own `recordExecutio
  [R2-9] contracts · LOW · REFUTED (both refuters)
      A9's spy assertion is vacuous after 0071 and the header's description of the already-committed-copy control no longer matches the path taken
      at apps/api/test/int/phase6-retention-b11.test.ts:449
      claim: A9 spies on vault.tombstone and asserts no archive-root call ([]), but since 0071 the rollback cleanup never calls vault.tombstone at all (execute's catch calls removeStaged by attempt name), so the assertion would pass even if the cleanup removed every file under the locator by another means. The control's substance survives only through the later vault.exists('archive', P) and download checks. Further, A2 no longer 'finds the copy in place' through copyBlob: because A1's record committed, A2 takes the already-archived branch (retention.service.ts line 229) and never calls copyBlob for P, so 
      refuter 1: refuted — The finding misreads what A9's spy guards, what the header claims, and the reachability of copyBlob's idempotent return; and the tree it was filed against has since moved (the harness and the 0071 header were edited at 23:49:40, after the finding), so both halves are moot in the current tree as well
  [R2-10] contracts · LOW · UNJUDGED
      0071 header (b): the subtransaction does not leave the transaction 'usable for the record of the failure' — no failure record is written in it, and the service's own doc still states the removed premise
      at apps/api/migrations/0071_b11_closure_manifest_locks.sql:33
      claim: After rollbackToSavepoint('retention_execution') (retention.service.ts line 162) the executor only removes staged files on the filesystem and throws; the pipeline's transaction().execute rolls the whole transaction back, the handler-failure audit is written by recordHandlerFailure in its own transaction, and the pause is the controller's separate write. The savepoint's only observable effect is that the manifest locks remain held during the staged-file removal (and that a cancelled statement does not leave the client with an aborted top-level transaction before its ROLLBACK), which the header 
  [R2-11] contracts · LOW · UNJUDGED
      readArchived's new evidence-root fallback reports staged:true and the custody record says tier 'archive' for bytes served from the hot root
      at apps/api/src/observation/vault/vault.service.ts:452
      claim: When neither the published locator nor a staged copy exists, readArchived now returns the EVIDENCE-root bytes tagged staged:true (line 452). EvidenceService.download chooses readArchived from the tier record and then records custody.retrieved with details.tier = tier.tier (evidence.service.ts line 331) and answers tier 'archive'/availability 'archived' — so a read actually served from the hot root is recorded as an archive-tier read, and the flag meant to say 'found under a staging name' is set for a hot read. This is a records-truthfulness gap introduced by the mid-review change; it is reacha
  [R2-12] contracts · LOW · UNJUDGED
      retryBytes now copies into and publishes under the archive root from a read-capability route with no manifest lock, so it can interleave with a deletion's post-commit removal
      at apps/api/src/retention/retention.service.ts:415
      claim: The retry route runs under pipeline.consequentialRead with RetentionCapability.read (retention.controller.ts lines 126–129): no begin_execution, no movers or manifest lock. retryBytes now performs publishArchiveCopy(null) (line 411) and, when nothing verifies, copyBlob('evidence','archive') + publishArchiveCopy (line 415) — writes into the archive root. A deletion of the same manifest holds the manifest lock only inside its transaction and removes bytes after its commit (removeBytes: tombstone('archive') then removeAllStaged). Interleaving the retry's rename/publish between those two removals,
  [R2-13] crash-windows · MEDIUM · UNJUDGED
      Crash (or lost commit acknowledgement) between the commit and the controller's post-commit work leaves an executed archive with an unpublished staged copy and its hot copy, and no route ever publishes or removes them
      at apps/api/src/retention/retention.service.ts:400
      claim: The retry route is keyed on residual rows only (`retryBytes`: `const pending = ...residual_inventory... status = 'pending'`; the loop at line 400 is the only thing that publishes or removes). Those rows are written by the SAME process, in a separate pipeline.write AFTER publishCopies/removeBytes (retention.controller.ts lines 179-190). If the process dies after the transaction committed and before that write — or the COMMIT's acknowledgement is lost (the controller's catch rethrows a generic error at line 174 while the server has committed), or the residual write itself fails — the action is '
  [R2-14] crash-windows · MEDIUM · UNJUDGED
      The post-commit byte movers (publish, the retry's publish/copy, a deletion's removal) run outside the manifest lock; a deletion and a publish of the same manifest can interleave so the published copy survives the tombstone with no route to remove it
      at apps/api/src/retention/retention.controller.ts:179
      claim: 0071 serialises the two EXECUTIONS on a manifest, but everything that moves bytes after the commit takes no lock: publishCopies (controller line 179) and removeBytes (line 181) run after pipeline.write returned; retryBytes runs inside a consequentialRead transaction that holds nothing (retention.service.ts lines 395-421, now including a copy-from-hot fallback). A deletion D of P that waited on an archive A's lock proceeds the instant A commits; D's post-commit removal is rm(evidence) → rm(archive locator) → readdir + rm each staged (removeBytes → tombstone then removeAllStaged, vault.service.t
  [R2-15] crash-windows · LOW · UNJUDGED
      The already-archived branch removes the hot copy on the strength of a STAGED copy: after a first archive whose publish failed, a second archive of the same manifest leaves the manifest with a staged file as its only copy
      at apps/api/src/retention/retention.service.ts:229
      claim: When the tier is already 'archive' the executor calls readArchived (line 231), which returns `staged: true` for a recorded-but-unpublished copy, then discards that flag (line 232: `copy = { ..., created: false }`) and still pushes the evidence locator for removal (line 262). 0071's header (d) says the hot copy stays 'until the publish succeeds' and the first action's pending residual keeps it for exactly that reason; the second action removes it anyway. Between the second action's commit and the first action's retry the manifest exists only as `<locator>.staging-<attempt>` — retrievable throug
  [R2-16] crash-windows · LOW · UNJUDGED
      A transaction that fails after the handler returned (POL/AUD commit, outbox enqueue, or the COMMIT itself) leaves the attempt's staged copies as orphans with the action back to approved and no pause
      at apps/api/src/retention/retention.service.ts:160
      claim: The removal of an attempt's staged files by name runs only in execute's catch (line 160, `if (kind === 'archive' && copiesMade.length > 0)`), i.e. only for a failure raised inside executeItems. A failure after the handler returned — commitPolicy/commitAudit/outbox enqueue in pipeline.write (pipeline.service.ts 176-186), or a COMMIT that fails for real — rolls the transaction back (state back to 'approved'), the controller's catch reaches `throw e` (controller line 174: not a RolledBack, not a lock code, not a rejected message) so no pause is recorded and no cleanup runs, and `<locator>.staging
  [R2-17] crash-windows · LOW · UNJUDGED
      A deletion's failed removal is recorded twice for the same locator, so its residual note reads 'refused again' on the first failure and loses the locator
      at apps/api/src/retention/retention.controller.ts:185
      claim: locatorsToRemove holds two entries per deleted manifest (evidence, and archive with stagedToo); the residual loop at line 185 filters by locator and therefore calls recordBytesResidual twice for the same manifest ref. The second call takes record_bytes_residual's UPDATE branch (0067 line 161), overwriting the note with 'the vault refused the removal again: the vault refused the removal after the record committed' and dropping the '; locator …' suffix the INSERT branch writes. Records only; the retry and verification behave correctly (verified on the current tree: the deletion's retry now retir
=== round 2: 17 finding(s) on the staged-copy design; the refuters of 8 of them were cut off by the session limit (the workflow's own record) — the author judged those on the corrected tree: the actionable ones corrected (the already-archived branch publishes a staged copy under the lock and schedules the hot removal only once a published copy stands; the retry works from the executed items, skips a tombstoned manifest, re-copies from the kept hot copy and reports what it could not publish; a transaction failing after the executor returned has its attempt's staged copies removed; one residual per locator; the download's custody row says where the bytes were served from; an execution above 256 manifests takes the movers lock exclusively whatever its kind; the ABORT case asserts the lock inside the cleanup; A9 spies the removals the cleanup could make; a listing failure counts as bytes present and fails a deletion's cleanup; restore.sh's verifier looks where the readers look), the two structural ones recorded as follow-ups (the post-commit byte movers run outside the manifest lock — a deletion's verification and retry catch a copy that survives; the archive root is never reconciled) ===
