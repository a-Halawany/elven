# The adversarial review of B7 before its commit (workflow wf_5a847f46-61e, 2026-09-12)
# fifteen skeptics (five claims × correctness/concurrency/security lenses), every finding verified by an independent agent; one line per verified finding: [severity] title — disposition in this batch

[defect] served_from is enforced only in the reconcile's SELECT; the receive port fans every re-driven event out to every active subscription, so a 'leave' subscription receives past events whenever any older event is re-driven for anyone else — CORRECTED: served point is a sequence (served_from_seq); existing deliveries always reconciled; live jobs bounded by the served point at receive
[defect] A replay's point may lie beyond the current cursor: the checkpoint moves FORWARD over open deliveries, which leave the reconciliation's window after 24 h and are never re-driven while blocking every later checkpoint advance — CORRECTED: the replay cursor only moves back; a replay reopens rows and the reconciliation re-drives them scoped — one job kind
[defect] An explicit replay's reach ignores served_from while the reconcile enforces it: a delivery a replay creates before served_from that is interrupted or exhausts the queue's retries is never re-driven and blocks the checkpoint — CORRECTED: served point is a sequence (served_from_seq); existing deliveries always reconciled; live jobs bounded by the served point at receive
[defect] The receive port's ON CONFLICT reopens every non-applied delivery of the event for every subscription, so a plain re-drive triggered by one subscription's state re-checks another's unresolved delivery inside its re-check interval and retries a refused governance answer from the tick — CORRECTED: a reopened delivery drops its previous class, route and reason
[gap] Replay jobs `<id>.r<seq>` and plain re-drive jobs `<id>` coexist for the same reopened deliveries: an explicit replay that outlasts a tick is delivered twice and a row the plain job creates first is recorded with replay_seq 0 — CORRECTED: the replay cursor only moves back; a replay reopens rows and the reconciliation re-drives them scoped — one job kind
[gap] served_from is a created_at (transaction-start) bound while the declared cursor is the partition sequence: whether a write in flight at registration reaches a 'leave' subscription depends on which path delivers it — CORRECTED: served point is a sequence (served_from_seq); existing deliveries always reconciled; live jobs bounded by the served point at receive
[nit] subscription_replay records a caller-supplied created_at as the checkpoint pair and resolves the point row without the 'published' status its error message names — CORRECTED: the replay cursor only moves back; a replay reopens rows and the reconciliation re-drives them scoped — one job kind
[defect] 'leave' bound is a wall-clock instant compared against the outbox's transaction-start created_at: a write open across the registration and committed after it is disowned by the reconciliation and can be lost with no ledger row — CORRECTED: the served point is a sequence
[defect] An operator replay reaches rows before served_from but the reconciliation never does: a replay-opened delivery of a pre-registration row that is interrupted or fails is stranded forever and permanently blocks the checkpoint — CORRECTED: served point is a sequence (served_from_seq); existing deliveries always reconciled; live jobs bounded by the served point at receive
[gap] 'Once' holds only for the registration-time backlog: an operator replay whose <id>.r<seq> jobs outlast any reconciliation (the 60 s tick, another registration, a resume) is re-driven again as plain <id> jobs, which fan out to every subscription of the domain — CORRECTED: the replay cursor only moves back; a replay reopens rows and the reconciliation re-drives them scoped — one job kind
[gap] Registering a backlog 'replay' subscription in a domain with existing subscriptions redelivers every past event to the existing subscriptions as no-ops, inflating deliveries and the retries telemetry; the record states 'once' without this — CORRECTED: the replay cursor only moves back; a replay reopens rows and the reconciliation re-drives them scoped — one job kind
[nit] Replay point lookups do not require a published row, and replay_from_seq may set checkpoint_event_id to a row of a non-subscribed type or a pending row — CORRECTED: the replay cursor only moves back; a replay reopens rows and the reconciliation re-drives them scoped — one job kind
[defect] A 'leave' subscription receives past events whenever any earlier row is re-driven: the receive port fans out without served_from and a reconcile job names no subscription — CORRECTED: served point is a sequence (served_from_seq); existing deliveries always reconciled; live jobs bounded by the served point at receive
[defect] served_from is a wall-clock instant compared against created_at = transaction start: a write that began before the registration and committed after it is excluded from reconciliation for ever (stranded if its live delivery fails; never delivered cross-process) — CORRECTED: served point is a sequence (served_from_seq); existing deliveries always reconciled; live jobs bounded by the served point at receive
[defect] A replay whose point lies beyond open work moves the checkpoint over unresolved/failed/received deliveries; once older than 24 h they are never re-driven again — CORRECTED: the replay cursor only moves back; a replay reopens rows and the reconciliation re-drives them scoped — one job kind
[gap] An explicit replay is still double-driven by the tick, and a never-delivered row replayed under that race is attributed replay_seq 0 with no 'replayed' ledger event — CORRECTED: the replay cursor only moves back; a replay reopens rows and the reconciliation re-drives them scoped — one job kind
[nit] The replay point lookups check neither publication status nor the subscription's event types/filter — CORRECTED: the replay cursor only moves back; a replay reopens rows and the reconciliation re-drives them scoped — one job kind
[defect] Reconciliation disowns any delivery whose outbox row predates served_from, but the receive and replay ports still create such deliveries — an unresolved one there is never re-driven by start/tick/registration/resume — CORRECTED: served point is a sequence (served_from_seq); existing deliveries always reconciled; live jobs bounded by the served point at receive
[defect] A re-drive of one subscription's unresolved delivery fans the event out to every active subscription, including a 'leave' subscription registered after the event — it receives and applies a pre-registration event (AU-MEM-0119 violated) and that spurious delivery is itself unreconcilable — CORRECTED: re-drives are scoped to the subscriptions they are for (only[]); the receive port touches no other
[gap] An unresolved delivery whose partition_seq is below the subscription's checkpoint is re-driven only while its outbox row is within the 24 h look-back; after that it is never re-checked and the cursor is stuck behind it for ever — CORRECTED: an existing delivery is reconciled by its state wherever its row lies (no look-back dependence)
[nit] subscription_item_begin admits any non-applied state, so a concurrent second handler of the same delivery (BullMQ stalled re-run, or a live job and a replay job on two processes) proceeds into the effect and turns 'unresolved' into 'failed' (infrastructure/retry) at item_done/item_unresolved — CORRECTED: the replay cursor only moves back; a replay reopens rows and the reconciliation re-drives them scoped — one job kind
[nit] Each re-check of the retrieval item bumps `deliveries` and appends a 'received' event on every other subscription's already-applied delivery of the same event, inflating their `retries` telemetry — CORRECTED: re-drives are scoped; siblings are not re-received
[defect] Re-drives fan out to every active subscription regardless of served_from: a 'leave' subscription receives backlog whenever another subscription's unresolved/failed delivery or a replay-backlog registration re-drives an older event — CORRECTED: served point is a sequence (served_from_seq); existing deliveries always reconciled; live jobs bounded by the served point at receive
[defect] Deliveries for events before a subscription's served_from are outside the reconciliation forever: an unresolved (or failed/interrupted) delivery created by a replay from the beginning on a 'leave' subscription is never re-checked by a restart or tick, and it pins the checkpoint — CORRECTED: served point is a sequence (served_from_seq); existing deliveries always reconciled; live jobs bounded by the served point at receive
[nit] The 10-minute re-check interval is bypassed whenever the same event is re-driven for any other subscription: the fan-out reopens and re-checks the unresolved delivery on every such re-drive — CORRECTED: re-drives are scoped; another subscription's re-drive does not reopen it
[gap] An explicit replay (route) can still be double-driven by the tick and re-fanned to every subscription; the 'once' guarantee holds only for the registration backlog — CORRECTED: the replay cursor only moves back; a replay reopens rows and the reconciliation re-drives them scoped — one job kind
[nit] Reopening a delivery leaves stale failure_class/disposition/last_error on a 'received' row — CORRECTED: a reopened delivery drops its previous class, route and reason
[nit] Core lock argument holds for post-0064 rows: audit head FOR UPDATE precedes outbox_partitions UPDATE in every production enqueue, same partition key, both held to commit — NO FINDING (the reviewer confirmed the claim)
[defect] Single-process publisher ticks overlap: setInterval has no re-entrancy guard, so two concurrent publishPending loops interleave a partition's rows into the domain queue — CORRECTED: one publisher tick at a time; a failed row halts its partition for the tick
[gap] A row whose add/ack fails stays leased while later sequences of the same partition are published in the same tick; it is re-published ≥60 s later, out of order — CORRECTED: a failed row halts its partition for the tick
[defect] Dead-lettered subscription events are invisible: subscription_outbox_failures() matches status 'failed', but the publisher's terminal state is 'dead_letter'; the checkpoint then advances over the hole — CORRECTED: subscription_outbox_failures reports failed and dead_letter
[defect] served_from is a transaction-start timestamp (compared to created_at) applied only on the reconcile path; the queue path ignores it — a straddling write's failed delivery is never re-driven and pins the checkpoint — CORRECTED: served point is a sequence (served_from_seq); existing deliveries always reconciled; live jobs bounded by the served point at receive
[gap] Lease order by partition_key then partition_seq with a global LIMIT 50 starves higher-keyed partitions behind a backlog ('platform' and low uuids first) — CORRECTED: the lease is round-robin across partitions, ordered within
[gap] The audit-before-enqueue premise is a pipeline convention, not a database invariant: enqueue_event does not check that the AUD row exists, and the C1 closure trigger only checks at commit — RECORDED as a known limit: the lock order is the pipeline's convention
[nit] Backfilled sequences of pre-0064 rows are (created_at, id) order, which the migration itself says is not commit order; the records present partition_seq uniformly as the commit order — RECORDED: the backfill's order is the publisher's historical order; commit order before 0064 is not claimed
[nit] subscription_replay_from_seq and subscription_replay resolve the replay point through rows of any status, though the error text says 'not a published event' — CORRECTED: the replay cursor only moves back; a replay reopens rows and the reconciliation re-drives them scoped — one job kind
[nit] Core claim holds: no re-drive/restart path clears an unresolved item without a passing check (verified, no finding) — NO FINDING (the reviewer confirmed the claim)
[defect] Reconcile's served_from bound strands deliveries that exist below it (explicit replay across the point; a write in flight at registration) — CORRECTED: served point is a sequence (served_from_seq); existing deliveries always reconciled; live jobs bounded by the served point at receive
[gap] A replay from a point AFTER open work moves the cursor forward and strands unresolved/failed deliveries behind it after the look-back — CORRECTED: the replay cursor only moves back; a replay reopens rows and the reconciliation re-drives them scoped — one job kind
[gap] 'Finds operator work' is narrower than 'projection drift': a missing or extra projection row passes the check and is checkpointed applied — RECORDED as a known limit: the check reports what 0024 measures
[nit] Lock-order inversion between finish (delivery then subscription) and replay (subscription then deliveries) can deadlock; self-heals via job retry — CORRECTED: the replay cursor only moves back; a replay reopens rows and the reconciliation re-drives them scoped — one job kind
[nit] A stale worker's late finish can flip a reopened delivery to 'failed' (infrastructure) for one retry window — RECORDED as a known limit: the delivery row is locked per port call, not per delivery; a late finish is bounded by the queue's retry
[nit] Replay wipes the unresolved history on the delivery row (checks count, first_at, unresolved_since restart) — CORRECTED: the replay cursor only moves back; a replay reopens rows and the reconciliation re-drives them scoped — one job kind
[gap] Revoke + re-register orphans the revoked subscription's unresolved deliveries; the replacement never re-checks the event — RECORDED as a known limit: a replacement registered to replay covers it
[defect] Refused deliveries ARE re-driven by the queue and by the tick whenever a sibling delivery of the same event is open — CORRECTED: re-drives are scoped to the subscriptions they are for
[defect] Legal-hold check is a separate read transaction from the apply: a hold placed after the check (or between batches) is not honoured and the withdrawal applies — CORRECTED: the hold check runs inside the applying transaction on the latest version's manifest, per batch
[defect] served_from is a timestamp compared with created_at (= transaction start): a write in flight at registration is never reconciled to a 'leave' subscription — CORRECTED: served point is a sequence (served_from_seq); existing deliveries always reconciled; live jobs bounded by the served point at receive
[gap] max_elapsed_ms budget ends as failed / budget / retry, not human_review as the records state — RECORDED: max_elapsed is budget → retry (the next delivery resumes); max_items is budget → human_review
[gap] Publisher routes the subscription job before acknowledging the row; a worker that wins the race refuses the job permanently ('not a published event') and delivery waits for the 60 s tick — CORRECTED: the event row is readable once leased
[gap] A non-authority fault in extendRunSession leaves the delivery 'received' with no finish, no failure class and no 'failed' ledger event — CORRECTED: an extension fault is finished failed/infrastructure/retry
[nit] A pause landing after the LAST applied item records 'refused: the remaining items were not applied' on a delivery whose items were all applied — RECORDED as a nit: the refusal reason names remaining items that may be none
[nit] Telemetry 'retries' counts redeliveries caused by sibling subscriptions, not the delivery's own retries — CORRECTED: re-drives are scoped, so a delivery's redeliveries are its own
[nit] place_legal_hold accepts an evidence object that is already withdrawn or superseded — CORRECTED: the hold check runs inside the applying transaction on the latest version's manifest, per batch
[nit] legal_holds lift-only trigger pins the placement fields but not scope, evd_object_id or correlation_id (second lift is correctly refused) — CORRECTED: the hold check runs inside the applying transaction on the latest version's manifest, per batch
[defect] Infrastructure fault at the session extension is neither recorded nor classified: the delivery stays 'received' with failure_class NULL — CORRECTED: an extension fault is finished failed/infrastructure/retry
[gap] max_elapsed_ms budget is recorded as failed / budget → retry, contradicting the stated budget → human_review route — RECORDED: max_elapsed is budget → retry (the next delivery resumes); max_items is budget → human_review
[gap] Legal-hold guard is a pre-read outside the apply transaction: a hold committed between the check and the apply commit does not stop the withdrawal — CORRECTED: the hold check runs inside the applying transaction on the latest version's manifest, per batch
[gap] A hold binds to the manifest of the version current at placement; a later revision (new manifest) silently un-protects the object — RECORDED as a known limit
[gap] consumer_unavailable → retry is retried by no automatic path; a restart with the consumer back does not re-drive it — CORRECTED: a start re-drives refused deliveries
[nit] propagation_attempt_telemetry derives failure_class from free-text and labels an expired-context fault as authority_disputed — CORRECTED: the classifier names consumer_unavailable before authority
[nit] A redelivery leaves the previous attempt's failure_class, disposition and last_error on the 'received' row — CORRECTED: a reopened delivery drops its previous class, route and reason
[nit] lh_lift_only trigger leaves evd_object_id, scope and correlation_id mutable (defence-in-depth only) — RECORDED as a nit (defence in depth)
[nit] served_from is a timestamp cut on created_at (transaction start) despite the sequence-only cursor rule — CORRECTED: served point is a sequence (served_from_seq); existing deliveries always reconciled; live jobs bounded by the served point at receive
[defect] Legal-hold check keys held-ness off ONE manifest per object (last row of an unordered multi-version read); a hold on an earlier version's manifest is not seen and the withdrawal applies — CORRECTED: the hold check runs inside the applying transaction on the latest version's manifest, per batch
[gap] Legal hold is enforced only by a pre-read in a separate transaction: a hold placed after the check (or between 32-object batches) is not honoured and the withdrawal supersedes held evidence — CORRECTED: the hold check runs inside the applying transaction on the latest version's manifest, per batch
[gap] budget → retry on the max_elapsed_ms path contradicts the stated budget → human_review mapping — RECORDED: max_elapsed is budget → retry (the next delivery resumes); max_items is budget → human_review
[gap] Refused deliveries ARE re-driven by the queue and the tick whenever a sibling subscription's delivery of the same event is non-terminal (and the 10-minute unresolved re-check interval is bypassed) — CORRECTED: re-drives are scoped to the subscriptions they are for
[gap] consumer_unavailable → retry is never automatic: the startup reconciliation excludes refused deliveries, so a restart with the consumer present leaves them refused — CORRECTED: a start re-drives refused deliveries
[gap] An infrastructure fault at extendRunSession is outside the classifying try/catch: no failed/infrastructure/retry record, the delivery is left 'received' with no failure_class — CORRECTED: an extension fault is finished failed/infrastructure/retry
[nit] place_legal_hold accepts a withdrawn/superseded object and records a hold on evidence already withdrawn — CORRECTED: the hold check runs inside the applying transaction on the latest version's manifest, per batch
[nit] The 'admission-time' legal_hold flag branch is dead: blob_manifests.legal_hold is always written false and the table is append-only — CORRECTED: subscription_outbox_failures reports failed and dead_letter
[nit] lh_lift_pair passes a NULL lift_reason; lift_legal_hold does not validate p_reason — CORRECTED: the hold check runs inside the applying transaction on the latest version's manifest, per batch
[nit] executed_action exposure reaches the package note only on the first note per cause; a replay after the package committed leaves it on the ledger item alone — CORRECTED: the replay cursor only moves back; a replay reopens rows and the reconciliation re-drives them scoped — one job kind
[nit] Publisher continues past a failed row, so a transient per-row fault publishes later rows of the same partition ahead of the failed one — CORRECTED: a failed row halts its partition for the tick
[defect] served_from is a created_at threshold: an event whose transaction started before a 'leave' registration but committed after it is excluded from reconciliation forever, and a stranded delivery for it is never re-driven — CORRECTED: served point is a sequence (served_from_seq); existing deliveries always reconciled; live jobs bounded by the served point at receive
[defect] Dead-lettered subscription events are silently absent: subscription_outbox_failures looks for status 'failed' but the lease retires rows to 'dead_letter', and the checkpoint advances over the hole — CORRECTED: subscription_outbox_failures reports failed and dead_letter
[defect] The publisher does not deliver a partition's rows in lease order within ONE process: overlapping ticks and per-row catch-and-continue interleave sequences on the domain queue — CORRECTED: one publisher tick at a time; a failed row halts its partition for the tick
[gap] Lease ORDER BY partition_key, partition_seq LIMIT 50 starves every partition behind a busy one — CORRECTED: the lease is round-robin across partitions, ordered within
[nit] The 'contiguous applied prefix' checkpoint is contiguous over delivery rows only, not over the partition's published rows — RECORDED as a known limit: a dead-lettered row is a visible gap (subscription_outbox_failures), not a delivery; the cursor passes it
[nit] The claim attributes the ordering to the audit chain-head lock; the outbox_partitions row lock alone provides it (the audit lock only fixes lock order) — RECORDED: the outbox_partitions lock gives the order; the audit lock fixes the two locks' order
[nit] The harness's 'commit order' demonstration is tautological — it can only agree with the audit chain and never exercises the created_at inversion 0064 says it corrects — RECORDED: the harness shows the sequence agrees with the audit chain under six concurrent writers and that the domain queue receives in sequence order; the created_at inversion is shown by the created_at values in that run (three rows sharing one millisecond, sequenced)
[defect] A single publisher process interleaves one partition's rows in the domain queue: overlapping ticks (no re-entrancy guard) and failed-tick leases publish higher sequences before lower ones — CORRECTED: one publisher tick at a time; a failed row halts its partition for the tick
[defect] Dead-lettered outbox rows leave a permanent, unreported gap in a partition's sequence: subscription_outbox_failures looks for status 'failed', which the publisher never produces — CORRECTED: subscription_outbox_failures reports failed and dead_letter
[gap] The H-before-P lock order is application convention only; the repository's own governed write fixtures enqueue before audit.commit_event (P before H), so 'every governed write' is false and the database does not enforce it — RECORDED as a known limit: the lock order is the pipeline's convention
[gap] The lease's ORDER BY partition_key, partition_seq LIMIT 50 replaces global FIFO with partition priority: a partition with a sustained backlog starves every lexicographically later partition — CORRECTED: the lease is round-robin across partitions, ordered within
[gap] served_from is a timestamp compared against created_at (the transaction start): a write that started before a 'leave' registration and committed after it can be excluded permanently — CORRECTED: served point is a sequence (served_from_seq); existing deliveries always reconciled; live jobs bounded by the served point at receive
[nit] Legacy rows' sequences are the old publisher order (created_at, id), not commit order; the record does not say so — RECORDED in the migration: the backfill's order is the publisher's historical order; commit order before 0064 is not claimed
[nit] An explicit replay's reopened deliveries are also re-driven by the periodic tick under a different job id, so a replay longer than one tick delivers events twice and touches every subscription's ledger — CORRECTED: the replay cursor only moves back; a replay reopens rows and the reconciliation re-drives them scoped — one job kind
[defect] propagation_attempt_telemetry classifies the consumer-unavailable failure as 'infrastructure' — CORRECTED: the classifier names consumer_unavailable before authority
[defect] open_failure_states is a filter over the 100 most recently delivered rows, so refused deliveries silently drop out while still open — CORRECTED: the open failure states are their own query
[nit] apply_ms spans every re-drive and replay of a delivery, not the apply it reports — CORRECTED: the replay cursor only moves back; a replay reopens rows and the reconciliation re-drives them scoped — one job kind
[nit] propagation view's outbox-derived columns fail open to NULL for an invoker outside read mode — RECORDED as a nit
[defect] open_failure_states is a window over the 100 most-recent deliveries; refused (human_review) deliveries age out and the route reports none open — CORRECTED: the open failure states are their own query
[gap] queue_wait_ms is anchored at the writer's transaction START, not at publication; never negative but overstated (multi-minute correction transactions, backlog replays) — CORRECTED: the replay cursor only moves back; a replay reopens rows and the reconciliation re-drives them scoped — one job kind
[gap] A write that started before a 'leave' registration and committed after it is delivered but sits outside the reconciliation's served_from bound: a failed/unresolved delivery of it is never re-driven and freezes the checkpoint — CORRECTED: served point is a sequence (served_from_seq); existing deliveries always reconciled; live jobs bounded by the served point at receive
[nit] The harness's tenant-isolation control inspects only the status route's 100-row recency page; a leaking view would pass once the suite's own domain has 100 newer deliveries — CORRECTED (before the review): security-invoker views; the harness asserts the isolation
[nit] Comment says the open states come first; the query orders by recency only — CORRECTED: the open failure states are their own query
[defect] The status route leaks other tenants' identifiers through runtime.last_reconciliation / runtime.last_failure (the two telemetry fields themselves are isolated) — CORRECTED (before the review): security-invoker views; the harness asserts the isolation
[defect] open_failure_states is a window over the 100 most recently delivered rows, so refused (human_review) deliveries silently drop out of it — CORRECTED: the open failure states are their own query
[defect] propagation_attempt_telemetry.failure_class is a regex over free-text last_error and misclassifies concrete messages; no disposition for the propagation flow — CORRECTED: the classifier names consumer_unavailable before authority
[gap] served_from is a timestamp compared against created_at (transaction start): a row committed after registration can be excluded from a 'leave' subscription and lost when it is not queue-routed — CORRECTED: served point is a sequence (served_from_seq); existing deliveries always reconciled; live jobs bounded by the served point at receive
[nit] Ledger outbox_created_at / checkpoint_created_at are millisecond-truncated copies of the outbox row's created_at — RECORDED as a nit: the sequence is the cursor, the instant is the record
[nit] attempts and the 'applying' event are not recorded for a re-check of an unresolved item once any item of the delivery is applied — RECORDED as a nit: attempts counts applies begun from an empty items_applied
