# CP-6 author evidence (2026-09-12)

The author's own demonstrations for the checkpoint recorded in `PHASE6_REPORT.md` §19 — one class of evidence,
kept apart from Codex's focused checks (quoted in §19) and from the hosted results (GitHub Actions, the only chain
that verifies a harness unit). Every file is the unedited output of the named command on the author's machine
(the vitest configuration warning lines and blank lines stripped from the suite logs).

| File | What it is |
|---|---|
| `repro-b1.mjs`, `repro-b1-before.txt`, `repro-b1-after.txt` | Codex finding 1 reproduced on the actual function `graph.propagations_to_reconcile()` (a rolled-back transaction pointing one attempt at each state) before and after migration 0062 |
| `repro-b4-before.txt`, `repro-b4-before-summary.md`, `repro-b4-after.txt` | Codex finding 2 reproduced on a scratch copy of the tracked audit before and after the evidence-backed summariser |
| `b1-suite-run1.txt`, `b1-suite-run2.txt` | `phase6-propagation-consumer` (18 cases) at the 0062 head, two consecutive runs on real Redis and the real outbox |
| `b6-run7.txt`, `b6-run8.txt` | `phase6-graph-subscriptions` (13 cases) at the 0063 head, two consecutive runs on `eye_verify3_20260912` (created and migrated 0001–0063 for this checkpoint) |
| `b6-regress-1.txt`, `b6-regress-2.txt` | the suites whose write paths gained the GraphChanged/MemoryCorrected emitters, re-run at the 0063 head (81/81, 98/98) |
| `upgrade-0063.txt` | the tail of `scripts/phase1/verify-0022-upgrade.mjs` at the 0063 head (63 files; roles +25; migrations +42; 275/275 on the upgraded data; virgin and upgraded schema digests equal) |
| `act-b6.txt` | `scripts/phase6/register-subscriptions.mjs` on the NORDWERK demonstration (`eye_demo` migrated 0062–0063) — cited as `act-b6.log` at `fcbdefc`/`cb014a9`, where the repository's `*.log` ignore rule kept it out of the commit; the retained file, renamed |
| `hosted-fcbdefc-build-test-summary.txt` | the totals lines of the hosted `build-test` job at `fcbdefc` (ci run 34663012651): unit, the integration suite 852/852 in 49 files with `phase6-graph-subscriptions` 13/13, the upgrade proof PASS — the hosted result itself is GitHub's, this is its extract |
| `repro-retrieval-before.txt`, `repro-retrieval-after.txt` | Codex finding 3 reproduced at the database/queue boundary by `apps/api/test/int/phase6-repro-retrieval-retry.test.ts` — before (code at `fcbdefc`, database at 0063: `failed` → re-drive → `applied`, one check) and after (0064: `unresolved` → re-drive → `unresolved`, a second check) |
| `b7-run16.txt`, `b7-run15.txt` | `phase6-graph-subscriptions-2` (14 cases) at the 0064 head on `eye_verify8_20260912` (created and migrated 0001–0064 from the final migration file) — run 16 alone with console output (the telemetry measurement); run 15 beside `phase6-graph-subscriptions`, the reproduction file, `phase6-propagation-consumer` and `gate22-outbox-hardening` (50/50) |
| `b7-regress-b1.txt` | `phase6-propagation-consumer`, `gate22-outbox-hardening`, `phase1-acceptance` at an earlier 0064 state (68/68) |
| `b7-int-all-4.txt`, `upgrade-0064.txt` | the full integration suite and the upgrade proof on a database created and migrated 0001–0064 from the final migration file |
| `b7-adversarial-review.txt` | the independent adversarial review of the batch before its commit (a workflow of fifteen skeptics over five claims, every finding verified independently): the distinct findings and what each became |
| `b7-telemetry-measurement.txt` | the one operating measurement AU-MEM-0041 asks for, on the local profile (a retrieval delivery: queue wait, apply time, end-to-end age, retries) and the tenant's open failure states as the status route reports them |
| `act-b7.txt` | the same act re-run on the NORDWERK demonstration after `eye_demo` was migrated through 0064 (the six subscriptions kept; the correction's MemoryCorrected and the retraction's GraphChanged delivered and verified — the ledger route's events now read through `outbox_event_id`) |
| `hosted-a852c65-build-test-summary.txt` | the totals lines of the hosted `build-test` job at `a852c65` (ci run 34693808238): unit, the integration suite 867/867 in 51 files with `phase6-graph-subscriptions-2` 14/14 and the reproduction file, the upgrade proof PASS with the declared column additions — the hosted result itself is GitHub's, this is its extract |
| `repro-event-delivery-before.txt`, `repro-event-delivery-after.txt` | Codex's two B7 event-delivery findings reproduced at the database/queue boundary by `apps/api/test/int/phase6-repro-event-delivery.test.ts` — before (code at the reproduction head `6fc52c9` = `a852c65` plus the test-runtime publish-fault hook and the configurable lease, database `eye_verify8_20260912` at 0064: B:1, A:50, A:51 published ahead of the failed A:1, the halted rows charged an attempt each; a replay from the beginning re-driving 1 of 3, the served point unmoved, the skipped history never delivered) and after (0065 on `eye_verify15_20260912`: A waits behind A:1 and publishes in sequence, each row tried once; the replay re-drives 3, the served point 0, the skipped history delivered once each) |
| `b8-run8.txt`, `b8-run9.txt`, `b8-run10.txt` | `phase6-graph-subscriptions-3` (19 cases) at the B8 head — run 8 on `eye_verify13_20260912` before the adversarial review, run 9 on `eye_verify16_20260912` after its corrections, run 10 on `eye_verify18_20260912` (with the reproduction file, 23/23) after the last of them; each on a database created and migrated 0001–0065: the replay recorded with no server, the queue lost and the process restarted; two application contexts on one database and Redis (the claim, the hand-over, the take-over, routing from the process that does not serve, two publishers with the fault at the head); the partition telemetry; the two remaining AU-MEM-0039 conditions with their positive controls; the four flows' telemetry; the interface register; the retention floor; the edge reassessment on evidence and model change; a warning invalidated and a briefing re-flagged by a correction walk |
| `b8-flow-telemetry-measurement.txt` | the one operating measurement per flow AU-MEM-0041 asks for, on the local profile (forecast, warning, reconciliation, simulation), as the telemetry route reports it |
| `b8-interface-register.txt` | the register's binding counts as the harness read them through the route (50 interfaces: 19 bound, 25 partial, 6 unbound after the review's correction) |
| `b8-int-all-2.txt`, `b8-int-all-3.txt`, `b8-int-all-4.txt`, `upgrade-0065.txt` | the full integration suite (890/890 in 53 files: before the adversarial review on `eye_verify15`, after its corrections on `eye_verify17`, on the final file on `eye_verify19`) and the upgrade proof on databases created and migrated 0001–0065 from the final migration file, alone on the machine; the first full run (with the upgrade proof beside it, before two corrections) had eight failures — recorded in `PHASE6_REPORT.md` §21.3 |
| `b8-regress-2.txt`, `b8-regress-3.txt` | B6, B7, the reproductions, the propagation consumer and the outbox suites at an earlier 0065 state (57/57); the four files that failed in the first full run, re-run after the corrections (51/51) |
| `b8-adversarial-review.txt` | the independent adversarial review of the batch before its commit (fifteen skeptics over five claims, every finding verified independently): the distinct findings and what each became |
| `act-b8.txt` | `scripts/phase6/register-subscriptions.mjs` on the NORDWERK demonstration at `661c2fb` after `eye_demo` was migrated through 0065: §0 the demonstration database's 0064 reconciled — it had been migrated through a development iteration of 0064 (digest `9da00200…`), the migrator refused 0065; `scripts/phase6/demo-reconcile-0064.sql` (guarded, one transaction, the exact difference copied verbatim from the committed file, rehearsed on a restored copy) applied and the recorded digest set to the committed file's, 0065 then through the migrator (a first migrator attempt without the migrate credential refused, recorded); the API restarted on the B8 build; the decisions and memory-mappings subscriptions revoked and registered anew (their consumers' methods changed; the replacements served from the row after the revoked cursor, the replay re-driving nothing because the rows between were ObservationRecorded), the act's two events delivered to all six within the tick, and per event WHICH deliveries did non-empty work (retrieval) and which found nothing of theirs (the other five); the serving holder, the partition and the open failure states as the status route reports them; §6 the database read back |
| `hosted-661c2fb-build-test-summary.txt` | the hosted `ci` run 34705325289 at `661c2fb` (build-test job 103584230879): unit 2147/2147, the acceptance suite 58/58 with 0065 applied, the integration suite 890/890 in 53 files on a fresh database (phase6-graph-subscriptions-3 19/19, phase6-repro-event-delivery 4/4, B6/B7 and the outbox suites), the upgrade proof PASS (65 files, schema digests equal), the accounting controls and `--check`; C19 lifecycle 34705325299 green; browser-regression and supply-chain green |
| `repro-serving-lifecycle-before.txt` | Codex's B8-F1/B8-F2 reproduced at the hook head `a081001` (`phase6-repro-serving-lifecycle`, 5 cases on a fresh 0001–0065 database with a second application context): the two controls pass, the three defect cases fail as the findings say |
| `repro-serving-lifecycle-after.txt` | the same five cases after 0066 §1 (the generation, the fence, the detached worker close) on `eye_verify46` migrated 0001–0066: 5/5 |
| `b9-run31.txt` | `phase6-graph-subscriptions-4` (0066 §2–§8, 23 cases) on `eye_verify44` migrated 0001–0066: 23/23 (24/24 with §11's case; 25/25 with 0067's) |
| `b9-run29.txt` | `phase6-executive-requests` (0066 §9, 7 cases) on `eye_verify40`: 7/7 (9/9 with 0067's cases) |
| `b9-int-all-2.txt` | the full integration suite at the head before §11 on `eye_verify45` (fresh, 0001–0066): 925/925 in 56 files |
| `b9-int-all-3.txt` | the full integration suite at the final 0066 on `eye_verify48` (fresh): 926/926 in 56 files, 418 s |
| `b9-unit.txt` | the unit and gate suites at the candidate: 2148/2148 and the hermetic meta suite 9/9 |
| `upgrade-0066.txt` | the post-C18 upgrade proof with 0066: 45 migrations, 31 roles, 28 registry rows added exactly as declared; 275/275 on the upgraded data |
| `act-b9.txt` | the B9 act on the NORDWERK demonstration (2026-09-12T22:30–22:41Z): `eye_demo` migrated with 0066 (backup taken), the API restarted, the re-driven correction that had failed ten times completing (§11), then the seven scenes — every scene with its effect (PHASE6_REPORT §22.5) |
| `b9-int-all-4.txt`, `upgrade-0067.txt` | the full integration suite at the corrected candidate on `eye_verify50` (fresh, 0001–0067): 929/929 in 56 files; the upgrade proof with 0067 (46 migrations) |
| `hosted-36ce748-build-test-summary.txt` | the totals lines of the hosted `ci` run 34726253751 at `36ce748` (build-test job 103640765238): unit 2148/2148, the acceptance suite 58/58, the integration suite 929/929 in 56 files on a fresh database, the upgrade proof with 46 migrations, C18 612/612 — the run that moves the seven B9 units to `verified:ci` |
| `b9-int-all-5.txt` | the full integration suite at the B9-F1 closure candidate on `eye_verify52` (fresh, 0001–0067): 932/932 in 56 files |
| `closure-b9-f1.txt` | Codex's B9-F1 closed through the real HTTP path on the demonstration (`scripts/phase6/closure-b9-f1.mjs`): v1 internal, v2 restricted, v3 audience-role-only; the analyst's historical v1 read printed whole — nothing of v2 or v3; the access ledger naming version 1 |
| `b9-adversarial-review.txt` | the six-dimension adversarial review of the candidate `c93cdad` with two refuters per finding: the confirmed defects and the observations; corrected by 0067 (PHASE6_REPORT §22.7) || `act-b10.txt` | the B10 act on the NORDWERK demonstration (2026-09-13T08:13–08:14Z, head `0cee439`; rehearsed three times on a restored copy — the first rehearsal found the review-scope defect corrected as 0068 §6): `eye_demo` migrated with 0068 (backup taken), the API restarted on the B10 build, then four scenes — the workspace's routes with the withdrawal (409 EYE-STA-003 printed whole), the briefing agent's retrieval (one item read, two hidden, the ledger row by the agent), B9-F2 on the live `stocks` edge (refused → unresolved → re-driven without a duplicate cause → the vocabulary extended → re-derived; G2's run asserting nothing twice), B9-F3 (a review of current evidence verified by its preservation contract; no DeletionVerified) — PHASE6_REPORT §23.3 |
| `b10-unit.txt`, `b10-upgrade-proof.txt`, `b10-int-all-1.txt` | at the B10 tree: the unit suite 2151/2151 in 40 files; the upgrade proof with 0022–0068 (47 migrations; 297/297 and 275/275 on the upgraded data); the full integration suite on `eye_verify61` (fresh, 0001–0068): 937/937 in 56 files |
| `hosted-0cee439-build-test-summary.txt` | the totals lines of the hosted `ci` run 34747248517 at `0cee439` (build-test job 103697312257): unit 2151/2151, the acceptance suite 58/58, the integration suite 937/937 in 56 files on a fresh database (the two B10 files 32/32 and 10/10), the upgrade proof with 47 migrations, C18 612/612; C19 34747248530 green — the run the B10 clauses of AU-DP-0176, AU-MEM-0059/-0061 and AU-MEM-0065 are bound to |
| `b10-closure-repro-before.txt` | Codex's B10-F1/F2/F3 and the author-found B10-F4 REPRODUCED at the governed boundary on the unfixed code (`phase6-briefing-memory.test.ts` against `c04f6b1`'s sources on a fresh database): the restricted statement in the plain executive's briefing, the object read serving the payload, two digests, the item gone at the earlier cutoff — 5 of 5 red as the findings say |
| `closure-b10.txt` | the closure on the demonstration through the HTTP path (`scripts/phase6/closure-b10.mjs`, 2026-09-13T10:30–10:32Z; `eye_demo` backed up and migrated with 0069, the API restarted on the corrected build): F1 (the complete briefing response of a reader outside the audience — nothing of the item; withheld with the reason), F4 (the object route's headers, content withheld; evidence served as before), F2 (one digest, three distinct accesses), F3 (v1 at the cutoff after v2; the availability naming the supersession); the first attempt's domain-wide composition refused by the fold, recorded — PHASE6_REPORT §23.6 |
| `b10-closure-adversarial-review.txt` | the author's adversarial review of the closure candidate (six dimensions, two refuters per finding, 38 agents): 13 confirmed (five distinct defects, all corrected before the push — the claim and evidence routes, the narrative, the withdrawn-item slots and the version window, the correlation-id binding), 3 refuted |
| `b10-closure-int-all-1.txt`, `b10-closure-upgrade-proof.txt` | the full integration suite at the closure candidate on `eye_verify69` (fresh, 0001–0069): 943/943 in 57 files; the upgrade proof with 0022–0069 (48 migrations) |
| `hosted-1fa3b08-build-test-summary.txt` | the totals of the hosted `ci` run 34752156335 at `1fa3b08` (build-test job 103710395626): unit 2151/2151, the acceptance suite 58/58, the integration suite 943/943 in 57 files (`phase6-briefing-memory` 6/6), the upgrade proof through 0069, C18 612/612; C19 34752156341 green |
| `act-b11.txt` | the B11 act on the NORDWERK demonstration (2026-09-13T14:15Z; rehearsed on a restored copy with its own vault copy first): `eye_demo` migrated with 0070 (backup taken), the API restarted, the six REST agents re-provisioned for the connector's new digest and a proof run on `ecb-eurusd`; the UN Comtrade act up to the key the owner binds; the archive of the current `eu-sanctions-rss` evidence (the tier ledger, the host's paths, the verification, retrieval from the archive tier); the customer export of two NORDWERK records verified offline by the customer's verifier (`PACKAGE OK`) and revoked; the safe scope — a deletion paused on a live review case, decided, resolved again, executed, DeletionVerified — PHASE6_REPORT §24.3 |
| `b11-adversarial-review.txt` | the author's adversarial review of the B11 retention candidate (six dimensions, two refuters per finding): 29 confirmed of 31, the corrections (the same unapplied migration and tree, before the demonstration) and their re-judgement (two judges per finding: 29 closed, 0 open) |
| `b11-int-all-1.txt`, `b11-unit.txt`, `b11-upgrade-proof.txt`, `b11-interface-register.txt` | at the B11 tree: the full integration suite on `eye_verify75` (fresh, 0001–0070): 984/984 in 60 files; the unit suite 2156/2156 in 40 files; the upgrade proof with 0022–0070 (49 migrations, 29 registry rows); the interface register after 0070 (26/24/0; L3-I04's binding) |
| `hosted-1e3e4be-build-test-summary.txt` | the totals of the hosted `ci` run 34869384873 at `1e3e4be` (build-test job 104061230792): unit 2156/2156, the acceptance suite 58/58, the integration suite 985/985 in 60 files on a fresh database (`phase6-retention-b11` 35/35, `-archive-poll` 4/4, `phase6-source-credentials` 3/3), the upgrade proof with 49 migrations, C18 612/612; supply-chain and browser-regression green; C19 34869384887 green — the run that moves AU-MEM-0059 and AU-MEM-0061 to `verified:ci` (the first push, `dc1a60a`, was red on the gitleaks scanners over a fixture literal and one `-4` case's withdraw action — amended before any review, PHASE6_REPORT §24.5) |
| `b11-closure-repro-before.txt` | Codex's B11-F1 REPRODUCED at the governed boundary on the UNFIXED executor (the B11 tree's sources, migrations 0001–0070 on the fresh `eye_verify_b11c0`, an isolated vault): the new harness's interleaving case — A's copy of P adopted by B, B committed, A rolled back and cleaned up — RED with the retrieval of P from the archive tier answering `missing`; the serial control green |
| `closure-b11.txt` | the B11 closure on the demonstration through the HTTP path (`scripts/phase6/closure-b11.mjs`; rehearsed first on a restored copy with its own vault copy and Redis): the running target verified by the runbook's script, `eye_demo` backed up and migrated with 0071, the API restarted by the script with its target verified; two approved archive actions on one NORDWERK record executed CONCURRENTLY — the lock orders them, one mover and one finder, one tier record, the bytes present, both verified, the download served from the archive tier, no advisory lock outliving its transaction; the same two records exported and verified by the corrected verifier, the SHIPPED verifier's exit 0 on a `null` and a `[]` manifest shown on the host beside the corrected verifier's exit 1, the export revoked; the Comtrade readiness and the exact binding step — PHASE6_REPORT §25.5 |
| `b11-closure-rehearsal.txt` | the rehearsals of the same act on the restored copy `eye_demo_b12` (:3411, its own vault copy and its own Redis on :6392) at the corrected tree, before the demonstration — scenes 1 and 2 (archive overlap, export/verifier) on the isolated-vault copy every time, scene 3 (readiness) resilient to the freshly-restored process's context-GUC 500 |
| `b11-closure-adversarial-review.txt` | the author's adversarial review of the closure candidate in TWO ROUNDS: round 1 (six dimensions, two refuters per finding — 19 findings, 10 confirmed), the corrections, and the re-judgement (two judges per confirmed finding: 7 closed, 3 open); round 2 against the staged-copy tree (the two open findings re-judged — all four judges named the per-action-name residual, closed by the per-attempt name — and three finders with two refuters attacking the staged copy: the actionable ones corrected, two structural residuals recorded as follow-ups) — PHASE6_REPORT §25.3 |
| `b11-closure-int-all-1.txt`, `b11-closure-unit.txt`, `b11-closure-upgrade-proof.txt` | at the closure tree: the full integration suite on a fresh database (0001–0071) — 994/994 in 61 files, the closure harness's 9 cases included; the unit suite 2156/2156 and the meta suite 9/9; the upgrade proof with 0022–0071 (50 migrations, 29 registry rows) |
| `hosted-6b93a27-build-test-summary.txt` | the totals of the hosted `ci` run 34968358484 at `6b93a27` (PR #48 → main; build-test job 104378198417): unit 2156/2156, the meta suite 9/9, acceptance 58/58, the integration suite 994/994 in 61 files on a fresh database (`phase6-retention-b11-closure` 9/9), the upgrade proof through 0071 (50 migrations), C18 612/612; supply-chain and browser-regression green; C19 lifecycle 34968358342 green — the run this closure is bound to |
| `b12-harness.txt` | the B12 harness `phase6-retention-b12` (12 cases: the restore port and executor, its contract and retrieval, the overlap that succeeds, the restore window and the ordering, the budget, retries and escalation, the observable state, the sweeper's walk, the retry route) at the B12 tree on a fresh database (0001–0072) with an isolated vault — 12/12 (the first run 10/12 on two harness expectations corrected) |
| `b12-neighbouring-suites.txt` | the suites the B12 changes touch at the B12 tree on a fresh database: `phase6-retention-b11`, `-archive-poll`, `phase1-fault-injection` (the sweeper), `phase1-acceptance` (the vault), `phase6-graph-subscriptions-4` — 160/160 in 5 files; `phase6-retention-b11-closure` 9/9 after the retitling of four cases to the per-attempt ownership design |
| `b12-int-all-1.txt`, `b12-upgrade-proof.txt`, `b12-unit.txt` | at the B12 tree: the full integration suite on a fresh database (0001–0072) — 1006/1006 in 62 files; the upgrade proof with 0022–0072 (51 migrations above the ceiling); the unit suite |
| `b12-rehearsal.txt` | the B12 act rehearsed on a restored copy of `eye_demo` (`eye_demo_b12` on :3411 with its own vault copy and its own Redis) before the demonstration — every scene producing its effect |
| `act-b12.txt` | the B12 act on the NORDWERK demonstration (2026-09-15T17:31–17:43Z): `eye_demo` backed up and migrated with 0072, the API restarted on the B12 build (the restart script's capture defect found and corrected mid-act, the restart repeated by the corrected script), the two archived NORDWERK records RESTORED to the hot tier (the host, the ledger, custody, the contract, the retrieval), the tier policy declared, an archive refused at admission by the byte budget and retried, an action escalated by age, the schedule evaluated oldest-due first with deferral, the restored record re-archived by the schedule after its restore window (the full cycle in one ledger), the sweeper's cleanup of the archive root, the policy re-declared at sane values |
| `hosted-97576c3-build-test-summary.txt` | the totals of the hosted `ci` run 35004457633 at `97576c3` (PR #49 → main; build-test job 104500625123): unit 2156/2156, the meta suite 9/9, acceptance 58/58, the integration suite 1006/1006 in 62 files on a fresh database (`phase6-retention-b12` 12/12), the upgrade proof with 0022–0072 (+51 rows, 72 files), C18 612/612 + 44; supply-chain and browser-regression green; C19 lifecycle 35004457741 green |
| `b13-harness.txt` | the B13 harness `phase6-retention-b13` (10 cases: the schedule retirement, the signing key, the signed export, the archive and the download, the destinations, the transfer-station delivery and its gates, the https delivery, the reads and the PDP) at the B13 tree on a fresh database (0001–0073) with an isolated vault, an isolated transfer station and two generated Ed25519 pairs bound by reference — 10/10 (the first run 6/10 on one fixture defect corrected) |
| `b13-neighbouring-suites.txt` | the suites the B13 changes touch at the B13 tree on a fresh database: `phase6-retention-b11`, `-closure`, `phase6-retention-b12`, `phase1-acceptance` (the vault, the egress), `phase6-graph-subscriptions-4` — 134/134 in 5 files |
| `b13-int-all-1.txt`, `b13-upgrade-proof.txt`, `b13-unit.txt` | at the B13 tree: the full integration suite on a fresh database (0001–0073) — 1016/1016 in 63 files; the upgrade proof with 0022–0073 (52 migrations above the ceiling); the unit suite |
| `b13-rehearsal.txt` | the B13 act rehearsed on a restored copy of `eye_demo` (`eye_demo_b13` on :3411 with its own vault copy, its own rehearsal signing key and transfer station, and its own Redis) before the demonstration — every scene producing its effect |
| `act-b13.txt` | the B13 act on the NORDWERK demonstration (2026-09-15T22:36Z): the operator's preparation (the demonstration key by reference in the local handoff — never printed; the station directory), `eye_demo` backed up and migrated with 0073, the API restarted; the B12-declared archive schedule RETIRED through the route with its history kept; the demonstration key declared (purpose demonstration); the transfer station and the https destination declared; the customer export signed with the key, verified, DOWNLOADED as one tar and verified offline (a flipped byte failing); DELIVERED to the transfer station, the demonstration recipient's receipt collected → ACKNOWLEDGED; the production path's delivery recorded failed `credential_unbound`; the package revoked (the download, a further delivery and the read refused; the deliveries kept) |
| `hosted-3a5a181-build-test-summary.txt` | the totals of the hosted `ci` run 35032929806 at `3a5a181` (PR #50 → main; build-test job 104595361754): unit 2156/2156, the meta suite 9/9, acceptance 58/58, the integration suite 1016/1016 in 63 files on a fresh database (`phase6-retention-b13` 10/10), the upgrade proof with 0022–0073 (+52 rows, 73 files), C18 612/612 + 44; supply-chain and browser-regression green; C19 lifecycle 35032929929 green |
| `b14-harness.txt` | the B14 harness (`phase6-retention-b14.test.ts`, 5 cases) at the B14 tree on a fresh database (0001–0074) with an isolated vault, an isolated transfer station and the SYNTHETIC HTTPS RECIPIENT spawned on a loopback port with a self-signed certificate: the positive exchange over TLS acknowledged, the receipt's binding (Codex B13-F1) and the controls, the trust and the vetting, the revocation notice — 5/5 on the third run (the first two on harness expectations, stated in the header) |
| `b14-neighbouring-suites.txt` | the suites the B14 changes touch at the B14 tree on a fresh database: `phase6-retention-b11`, `-b11-closure`, `-b12`, `-b13`, `-b14` — 71/71 in five files |
| `b14-int-all-1.txt`, `b14-upgrade-proof.txt`, `b14-unit.txt` | at the B14 tree: the full integration suite on a fresh database (0001–0074); the upgrade proof with 0022–0074 (53 migrations); the unit suite 2156/2156 and the meta suite 9/9, the web build and tests |
| `b14-rehearsal.txt` | the B14 act rehearsed on a restored copy of `eye_demo` (`eye_demo_b14` on :3411 with its own vault copy, key, station — the copy's station row repointed by SQL, a rehearsal-only edit — its own demonstration https recipient with a run-time bearer, and its own Redis): ALL SCENES HELD |
| `act-b14.txt` | the B14 act on the NORDWERK demonstration (2026-09-16T07:36Z): the operator's preparation (the recipient's bearer by reference in the local handoff — never printed; the demonstration https recipient on this host's loopback interface with its certificate under `.eye-local/https-recipient-demo`), the backup, 0074 on `eye_demo`, the restart on the B14 build; the destination declared with the recipient's certificate as its trust anchor; the signed export; the https path through the PRODUCTION egress refused by the vetting on this host (dns_failure; nothing left the process — the positive exchange the harness's, a public recipient the activation step); the station exchange acknowledged; the REVOCATION NOTICE — the station told in the revoke act, the product's copies removed after the commit, the recipient's confirmation collected → acknowledged, a further notice refused by the recipient → mismatched; the state. ALL SCENES HELD |
| `hosted-3b7c44c-build-test-summary.txt` | the totals of the hosted `ci` run 35070312503 at `3b7c44c` (PR #51 → main; build-test job 104710045011): unit 2156/2156, the meta suite 9/9, acceptance 58/58, the integration suite 1021/1021 in 64 files on a fresh database with `phase6-retention-b14` 5/5, the upgrade proof through 0074, C18 612/612 + 44; C19 lifecycle 35070312431 green |
| `b15-harness.txt` | the B15 harness (`phase6-retention-b15.test.ts`, 3 cases) at the B15 tree on a fresh database (0001–0075) with an isolated vault and station and the synthetic https recipient: the relationship closure, the streamed archive, a 272 MB package above the in-memory ceiling — 3/3 |
| `b15-neighbouring-suites.txt` | the six retention harnesses (B11, B11-closure, B12, B13, B14, B15) at the B15 tree on a fresh database — 74/74 (the B11/B13 pins of the package's shape updated to the closure's file and row) |
| `b15-int-all-1.txt`, `b15-upgrade-proof.txt`, `b15-unit.txt` | at the B15 tree: the full integration suite on a fresh database (0001–0075); the upgrade proof with 0022–0075 (54 migrations); the unit suite and the meta suite, the web build and tests |
| `b15-rehearsal.txt` | the B15 act rehearsed on a restored copy of `eye_demo` (`eye_demo_b15` on :3411 with its own vault copy, key and station — the copy's station row repointed by SQL, a rehearsal-only edit — and its own Redis): ALL SCENES HELD |
| `act-b15.txt` | the B15 act on the NORDWERK demonstration: the backup, 0075 on `eye_demo`, the restart on the B15 build; the export of the NORDWERK internal records that carry derived knowledge — links.json named inside the signed chain, the closure read and consistent, the ledger, the product's verification; the customer's verifier and the customer's fetch tool streaming the archive from the stream route through the real HTTP path; the JSON download; the streamed delivery to the station acknowledged. ALL SCENES HELD |
| `hosted-33882a3-build-test-summary.txt` | the totals of the hosted `ci` run 35074350184 at `33882a3` (PR #52; build-test job 104723078501): unit 2156/2156, the meta suite 9/9, acceptance 58/58, the integration suite 1024/1024 in 65 files on a fresh database with `phase6-retention-b15` 3/3, the upgrade proof through 0075, C18 612/612 + 44; C19 lifecycle 35074350249 green |
| `b16-harness.txt` | the B16 harness (`phase6-retention-b16.test.ts`, 6 cases) at the B16 tree on a fresh database (0001–0076) with four isolated vault roots, an isolated station and the synthetic https recipient: the versioned closure, the exchange partner and the signing-key binding, the round trip with the compare tool, the refusals with the evidence preserved, a 272 MB package imported from the station with the sampled memory high-water mark (182.1 MiB above the baseline), held recipients — 6/6 |
| `b16-neighbouring-suites.txt` | the seven retention harnesses (B11, B11-closure, B12, B13, B14, B15, B16) at the B16 tree on a fresh database — 80/80 (the B15 L1 pins moved to the /2 closure; the B15 S2 measurement replaced by the sampled high-water mark) |
| `b16-int-all-1.txt`, `b16-upgrade-proof.txt`, `b16-unit.txt` | at the B16 tree: the full integration suite on a fresh database (0001–0076); the upgrade proof with 0022–0076 (55 migrations; 35 registry rows); the unit suite (with `import-package.test.ts` 28) and the meta suite, the web build and tests |
| `b16-rehearsal.txt` | the B16 act rehearsed on a restored copy of `eye_demo` (`eye_demo_b16` on :3411 with its own vault copy, key, station and Redis — the copy's station row repointed and its signing-key row swapped by SQL, rehearsal-only edits): the first run stopped on the copy's key row (D5 found), the second ALL SCENES HELD |
| `act-b16.txt` | the B16 act on the NORDWERK demonstration: the backup, 0076 on `eye_demo`, the restart on the B16 build; the REL claim corrected C@3 → C@4 with the edge kept on its version; the export E1 with the /2 closure (17 claim versions, 8 edges, 7 entities), the pair check, the counterexample and the rebase failing, delivered to the station and acknowledged; the mirror domain, its personas, intake contract, station and partner; the import from the station verified (sixteen checks), approved, admitted under new ids; the re-export E2 and the customer's round-trip tool: ROUND TRIP OK; held recipients on a mismatched station receipt — ALL SCENES HELD (40 checks) |
| `hosted-f4b2345-build-test-summary.txt` | the totals of the hosted `ci` run 35097020135 at `f4b2345` (PR #53; build-test job 104796797537): unit 2184/2184, the meta suite 9/9, acceptance 58/58, the integration suite 1030/1030 in 66 files on a fresh database with `phase6-retention-b16` 6/6, the upgrade proof through 0076, C18 612/612 + 44; C19 lifecycle 35097020209 green |
| `b17-harness.txt` | the B17 harness (`phase6-retention-b17.test.ts`, 5 cases) at the B17 tree on a fresh database (0001–0077) with the scheduler on, four isolated vault roots and an isolated station: import.admitted and its deliveries, the ObservationRecorded rows, the review gate, the truncation; import.revoked — the propagation executed in the mirror with the walk's reach; the legal hold, the pending path, the fault, the reuse rules; the foreign origin's signed notice; the recipient and the origin's acts — 5/5 |
| `b17-neighbouring-suites.txt` | the eight retention harnesses (B11–B17) and the four subscription harnesses (B6–B9) at the B17 tree on a fresh database — 163/163 (the memory-mappings consumer's digest changed: every subscription registers fresh) |
| `b17-int-all-1.txt`, `b17-upgrade-proof.txt`, `b17-unit.txt` | at the B17 tree: the full integration suite on a fresh database (0001–0077); the upgrade proof with 0022–0077 (56 migrations; 35 registry rows); the unit suite (with the three B17 files: 54) and the meta suite, the web build and tests |
| `b17-rehearsal.txt` | the B17 act rehearsed on a restored copy of `eye_demo` (`eye_demo_b17` on :3411 with its own vault copy, key, station and Redis — the station repoint, the key-row swap and the demo row's reference renamed by SQL, rehearsal-only edits): the first run stopped on a persona name the demonstration already held, the second ALL SCENES HELD |
| `act-b17.txt` | the B17 act on the NORDWERK demonstration: the backup, 0077 on `eye_demo`, the restart on the B17 build; the mirror's seven subscribers and the origin's memory-mappings re-registered; S. Roth's assumption and K. Vogel's twin on B16's admitted import; H. Bergmann's revocation of B16's E1 — the station's SIGNED notice verified by the recipient and acknowledged, the mirror's copies destroyed by the same act, the origin's importer notice acknowledged, the import.revoked deliveries (the twin unverified, retrieval verified); E4 imported afresh under new ids with ONE import.admitted, its deliveries and the ObservationRecorded rows — ALL SCENES HELD (52 checks) |
| `hosted-2d92385-build-test-summary.txt` | the totals of the hosted `ci` run 35115226285 at `2d92385` (PR #54; build-test job 104858824337): unit 2210/2210, the meta suite 9/9, acceptance 58/58, the integration suite 1035/1035 in 67 files on a fresh database with `phase6-retention-b17` 5/5, the upgrade proof through 0077, C18 612/612 + 44; C19 lifecycle 35115226352 green |
| `b18-harness.txt` | the two B18 harnesses at the B18 tree on fresh databases (0001–0078): `phase6-retention-b18.test.ts` (6 cases — B18.1, Codex B17-F1: the crash after a record batch and the FIRST resumed request removing the crashed attempt's bytes; the removal failing with the residue named and the retry correcting the ledger's last word; the hand-off of a copy between two revocations; the crash ordering; a staged-only residue; an unreachable root) and `phase6-interfaces-b18.test.ts` (14 cases — S1 the seven announced events in their transitions' own transactions, S2 the withdrawal → invalidation → reopen chain with the deliveries and the replay, S3 the refusals by family and status, S4 the register 36/14/0) |
| `b18-neighbouring-suites.txt` | the eight retention harnesses (B11–B18), the four subscription harnesses (B6–B9; the twins and decisions digests changed — they register fresh; B6's admits settled), the interfaces harness and the simulation/decision harnesses that reproduce or commit (`phase5-simulations`, `phase5-corrections`, `phase6-replay`, `phase6-decisions`, `phase6-approvals`, `phase5-twins`, `phase6-residual-corrections`) at the B18 tree on a fresh database — 283/283 in 22 files |
| `b18-int-all-1.txt`, `b18-upgrade-proof.txt`, `b18-unit.txt` | at the B18 tree: the full integration suite on a fresh database (0001–0078) — 1055/1055 in 69 files; the upgrade proof with 0022–0078 (57 migrations; 35 registry rows; 78 files; the schema digests equal; 275/275 on the upgraded data); the unit suite (with the B18 files: the withdrawn header, the six event builders, the lifecycle graph builders, the PDP precedence, the mapper order — 2272 = 2210 + 62) and the meta suite 9/9 |
| `b18-browser.txt` | the browser gate at the B18 tree (`pnpm exec playwright test`; the gate's own API on :3401 with the run's generated Ed25519 key, the web on :3000) on a fresh database with isolated vault roots — 46/46: the Phase 0 ten, the Phase 1 sixteen, the B18 twenty (`e2e/phase6-memory.spec.ts` 7; `e2e/phase6-retention.spec.ts` 13 — the tenant acts through the working-domain chooser in both modes); the first run's two spec-side faults stated |
| `b18-rehearsal.txt` | the B18 act rehearsed on a restored copy of `eye_demo` (`eye_demo_b18` on :3411 with its own vault copy, key, station and Redis — B17's rehearsal-only edits) — the second rehearsal, held whole (60 checks); the first stopped in scene 3 on the act's hard-coded twin cut-off, which the product rightly refused |
| `act-b18.txt` | the B18 act on the NORDWERK demonstration: the backup, 0078 on `eye_demo`, the restart on the B18 build (the API had been stopped for the browser gate); the twins and decisions subscriptions of both domains re-registered; B18.1 on the mirror's standing revoked import (`retried`, nothing to remove); K. Vogel's twin version announced; N. Eriksen's 90-day forecast (ForecastIssued@v2) and scenario; two cold records restored before T. Nakamura's twin version 5 and its shocked control and reroute runs (Started/Completed with the resource evidence); L. Brandt's package proposed, approved by S. Okafor, committed; the forecast WITHDRAWN as unfit (the scenario marked, the package noted, the twin version unverified), the reroute reproduced → INVALIDATED, the package REOPENED and re-committed (two commitments, the first untouched), version 1 replayed at its own instant; A. Hoffmann's challenge (ReviewRequested); the sixteen event rows; the register 36/14/0 — ALL SCENES HELD (60 checks) |
| `hosted-4cea858-build-test-summary.txt` | the totals of the hosted `ci` run 35163213469 at `4cea858` (PR #55; build-test job 105018424523): unit 2272/2272, the meta suite 9/9, acceptance 58/58, the integration suite 1055/1055 in 69 files on a fresh database (`phase6-retention-b18` 6/6, `phase6-interfaces-b18` 14/14), the upgrade proof +57 rows / 78 files, C18 612/612 + 44; supply-chain green; browser-regression 46 passed (job 105018424472); C19 lifecycle 35163213457 green |
| `b19-harness.txt` | the B19 harness (`phase6-memory-derived.test.ts`, 6 cases — M1 derived (document) with the lift said and enforced on the deriver, M2 the review and lifecycle gates, M3 telemetry on the REST fixture with a registered series and a warning basis, M4 the record following its basis (corrected; withdrawn by the three paths; re-derived; the withdrawn ground refused), M5 the deletion pause naming the derived records and the human control, M6 the briefing) at the B19 tree on fresh databases (0001–0079), no scheduler — the first run and the re-run after the re-derivation's validity default |
| `b19-neighbouring-suites.txt` | B19's neighbouring set at the B19 tree on a fresh database — the four subscription harnesses (the two B9-F1 closure pins widened to the retrieval's shape), the briefing-memory harness, the eight retention harnesses, the corrections harnesses, the executive requests and the B18 interfaces harness — 245/245 in 20 files |
| `b19-int-all-1.txt`, `b19-upgrade-proof.txt`, `b19-unit.txt` | at the B19 tree: the full integration suite on fresh databases (0001–0079) — 1061/1061 in 70 files, twice; the upgrade proof with 0022–0079 (58 migrations; the registry's 36 rows with MEM@v2; 79 files; the digests equal; 275/275); the unit suite (with `memory-derive` 19 and the PDP describe 4 — 2295 = 2272 + 23) and the meta suite 9/9 |
| `b19-browser.txt` | the browser gate at the B19 tree on a fresh database with isolated vault roots — 48/48: the Phase 0 ten, the Phase 1 sixteen, the B18 twenty, the B19 two (a claim extracted on the gate's own upload by a recorded-fixture method and DERIVED through the page; the refusals) |
| `b19-rehearsal.txt` | the B19 act rehearsed on a restored copy of `eye_demo` (`eye_demo_b19` on :3411 with B17's rehearsal-only edits) — the fourth rehearsal, held whole (32 checks); the first three stopped on the act's own pins and one service default, all recorded in the header |
| `act-b19.txt` | the B19 act on the NORDWERK demonstration: the backup, 0079 on `eye_demo`, the restart on the B19 build; every basis looked up at run time; the telemetry record from the PortWatch transit count and the corridor warning; the queued claim refused; the document record from the NORDWERK relationship and S. Okafor's briefing carrying it; the communication kind stated; the basis corrected in review → DOC marked → re-derived → version 1 replayed; the mirror's imported claim refused; the evidence corrected → the agent's walk marks DOC → the deletion PAUSED naming the record, withdrawn — ALL SCENES HELD (32 checks) |
| `hosted-087736e-build-test-summary.txt` | the totals of the hosted `ci` run 35174994149 at `087736e` (PR #56; build-test job 105054615717): unit 2295/2295, the meta suite 9/9, acceptance 58/58, the integration suite 1061/1061 in 70 files on a fresh database (`phase6-memory-derived` 6/6 beside the B18 harnesses), the upgrade proof +58 rows / 79 files, C18 612/612 + 44; supply-chain green; browser-regression 48 passed (the B19 two on the hosted gate); C19 lifecycle 35174994150 green — the B19 hosted run, bound once in the records commit |
| `b20-harness.txt` | the B20 harness (`phase6-graph-projections-b20.test.ts`, 9 cases — P1 the watermark on every read through paused / resumed / revoked / re-registered, P2 drift → the automatic withdrawal → the labelled last-valid reads and the constrained walk → the rebuild → the re-driven delivery, P3 the poisoned partition and the two held-poison shapes, P4 the missing row (an entity, an edge, an unrebuildable edge, a memory item), P5 the unapproved representation version, P6 the memory workspace (drift, the policy columns, the content tier down, the narrower gate, the operator's withdrawal, the 503), P7 the deletion paused, P9 the operator's acts and the refusals, P8 the search's completeness) at the B20 tree on two fresh databases (0001–0080) with the scheduler on and the seven consumers — the 17 `B20 EVIDENCE` lines (the six V04-T-024/026 items per case) of the first run, then both runs' totals: 9/9 and 9/9 |
| `b20-neighbouring-suites.txt` | B20's neighbouring set at the B20 tree on a fresh database with the scheduler on — the four subscription harnesses and the two repro suites (the ✔ fixtures planting their events beside the rows), `phase6-interfaces-b18`, the retention harnesses B11/B16/B17/B18 (the `noDrift` pins widened to the symmetric check), `phase6-memory-derived`, the briefing/agent/corrections/executive/monitoring suites, `phase3-acceptance` (`:1176` widened) and `phase3-corrections`, `phase6-decisions`, `phase6-propagation-consumer` — 344/344 in 24 files |
| `b20-int-all-1.txt`, `b20-upgrade-proof.txt`, `b20-unit.txt` | at the B20 tree: the full integration suite on a fresh database (0001–0080) — 1070/1070 in 71 files (= 1061 + 9); the upgrade proof with 0022–0080 (59 migrations; the registry's 36 rows — no row in B20; 80 files; the digests equal; 275/275); the unit suite (with `projection-state` 19, `change-events-projection` 8, `memory-content-tier` 7, the PDP describe and the refusals describe — 2338 = 2295 + 43) and the meta suite 9/9 |
| `b20-browser.txt` | the browser gate at the B20 tree on a fresh database with isolated vault roots — 51/51: the Phase 0 ten, the Phase 1 sixteen, the B18 twenty, the B19 two (the memory walk's test 2 with its new line — the retrieval's projection reads `unverified` with C1's first form: the gate has no scheduler and no subscription), the B20 three (`e2e/phase6-projections.spec.ts`: the six unverified rows on the subscriptions page; the domain administrator withdraws `entities_current` with a reason, the search page shows the label with `EYE-DEG-001`, the rebuild restores; the analyst's 403 with the Withdraw button disabled until `#preason` is filled) |
| `b20-rehearsal.txt` | the B20 act rehearsed on a restored copy of `eye_demo` (`eye_demo_b20` on :3411 with B17's rehearsal-only edits) — the third rehearsal, held whole (39 checks); the first stopped on the act's own briefing pin (the sources' degradation attributed to the projection), the second on the act's own scene-1 wait and its lookups against the revoked subscription's rows — both act-side, recorded in the header |
| `act-b20.txt` | the B20 act on the NORDWERK demonstration: the backup FIRST to `.eye-local/backups/` (durable), 0080 on `eye_demo` (the partitions seeded), the restart on the B20 build; the register and the strict check on both domains; the retrieval subscriptions of both domains revoked and registered anew, each replaying the revoked cursor's own event; the search and the neighbourhood `current` at 14359; the administrator's withdrawal of `edges_current` — the neighbourhood served from the log, labelled `EYE-DEG-001` and constrained, the search still current, a second withdrawal idempotent; P. Novák's approved deletion PAUSED at execution `(projection_withdrawn)`; S. Okafor's briefing not degraded by the projection; L. Brandt's memory retrieval unaffected; the rebuild `restored` (0/0/0 — honest), the `projection.rebuilt` event's six deliveries, the paused deletion resolved again and withdrawn, the reads current at 14497 — ALL SCENES HELD (39 checks) |
| `hosted-1f6d04c-build-test-summary.txt` | the totals of the hosted `ci` run 35779940271 at `1f6d04c` (PR #58): attempt 1 preserved (build-test failed on one unrelated B14 https-recipient case, 1069/1070; the supply-chain job on the C15 recheck step; browser 51 passed) and attempt 2 the binding (build-test job 106930091028: unit 2338/2338, the meta suite 9/9, acceptance 58/58, the integration suite 1070/1070 in 71 files with `phase6-graph-projections-b20` 9/9, the upgrade proof +59 rows / 80 files, C18 612/612 + 44; browser-regression job 106930093569 51 passed; the supply-chain job 106930091021 red on the C15 patched-image recheck step alone — the gate itself green); C19 lifecycle 35779940295 green — the B20 hosted run, bound once in the records commit |
| `b21-refute-b20-f1.txt` | B21.1's refute step (2026-09-23T22:08Z): Codex's B20-F1 reproduced at the function boundary with Codex's own idiom — `1f6d04c`'s `fallback.ts` (byte-identical to `13ed40c`'s) under a query double that answers one row the projection lacks and throws SQLSTATE 57014 at its canonical lookup: the raw 57014 escaped with zero savepoints entered; then the corrected reader under the same double raising `ContentTierUnavailable(canonical_versions)` after two savepoints with the second rolled back — 2/2; the throw-away files deleted before the commit |
| `b21-1-harness.txt` | the B21.1 harness (`phase6-graph-projections-b21.test.ts`, 1 case — F1: Codex's four rows through HTTP at each canonical statement (the retrieval's versions read; the derivation; the absent rows' versions, reached by the fault registry's ordinal), `/memory/list` and `/memory/:id/get`, the refused reader's 503, the audit rows `EYE-DEG-001` and never `EYE-INT-001`, the recovery, the briefing composed without its memory items and declared, the `on_degraded` agent stopped with the reason, the plain agent finished, the rebuild) at the B21 tree on two fresh databases (0001–0081), no scheduler, no subscription, the vault roots the file's own — 1/1 and 1/1 with the `B21.1 EVIDENCE F1` line each |
| `b21-2-harness.txt` | the B21.2 harness (`phase6-evidence-degradation-b21.test.ts`, 6 cases — V7 the gate and the canonical refusals before the root check, V1 Class A through the route with the custody row durable beside the audit row `success`/`EYE-INT-001` (the D2.6/0013 reconciliation stated in the header), V2 Class B on the archive root, V3 Class B on the evidence root, V4(a) the series reader's disclosure, V5 the verifier's NULL) at the B21 tree on two fresh databases (0001–0081), the vault roots the file's own, the markers moved aside and restored — 6/6 and 6/6 with the six `B21.2 EVIDENCE` lines each; and the two appended cases from the neighbouring run — V4(c) the extraction under an unreachable evidence root (`phase2-acceptance` 24/24) and V6 the poll under an unreachable archive root (`phase6-retention-b11-archive-poll` 5/5) |
| `b21-3-harness.txt` | the B21.3 harness (`phase6-fitness-b21.test.ts` — T1 ValidateTwin, T2 ForecastFitnessChanged, T3 ScenarioCoherenceFailed, T4 ChallengeSimulation, T5 the register 40/10/0, the seven consumer digests against `13ed40c` and the two rule constants) at the B21 tree on two fresh databases (0001–0081) with the scheduler on and the seven consumers registered fresh — the `DIGESTS_13ED40C` block in the header, the migration's `::text` fix and the harness's two fixes stated, 5/5 and 5/5 with the `B21.3 EVIDENCE` lines; the C15 case (`phase5-corrections` after F6: a domain administrator validates the reconciled version fit → `calibration.count 1`) from the neighbouring run; then, after the rehearsal wedge's fix, the harness with T1.8 (the run route under a planted stream of hour-old capability nonces crossing `ctx.build`'s sweep line; the wedge's signature watched; the six-transaction audit trail) — 6/6 twice and the refuter's 6/6 |
| `b21-neighbouring-suites.txt` | B21's neighbouring set at the B21 tree on fresh databases with the scheduler on and the vault roots exported to a temporary directory — 38 files in five groups (53/53, 239/239, 166/170 on the first run of n3 → the three files re-run green 64/64 after the four pins the design moved, 117/117; the per-file counts inside): the B20 harness (its register pin moved to 40/10/0), the briefing/agent/memory/executive/monitoring suites, the phase-1, phase-2 and phase-4 suites with the nine retention harnesses, the phase-5 twin and simulation suites, the four subscription harnesses, the interfaces harness, `phase6-decisions`, the phase-3 and corrections suites |
| `b21-int-all-1.txt`, `b21-upgrade-proof.txt`, `b21-unit.txt` | at the B21 tree: the full integration suite on a fresh database (0001–0081) after the wedge's fix — 1086/1086 in 74 files (= 1070 + 12 new + 2 appended + 1 + T1.8; the run at the reconciled tree before T1.8 existed 1085/1085 in 74 files, named in the header); the upgrade proof with 0022–0081 (60 migrations; the registry's 36 rows and the 31 roles unchanged; 81 files; the digests equal `d9018019…`; 297/297 before and after; 276/276 on the upgraded data); the unit suite (with `memory-fallback-content-tier` 13, `evidence-retrieval-labels` 3, `phase6-refusals-b21` 13, `phase6-fitness-events-b21` 22 — 2389 = 2338 + 51 in 59 files) and the meta suite 9/9, at the reconciled tree and again after the wedge's fix |
| `b21-browser.txt` | the browser gate at the B21 tree on a fresh database with isolated vault roots (the demonstration API and web stopped for the run) — 51/51 in 44.6 s: the Phase 0 ten, the Phase 1 sixteen, the B18 twenty, the B19 two, the B20 three — as B20 left it; B21 adds no hosted walk (the fitness walk `e2e/phase6-fitness.demo.spec.ts` is a demo spec ignored by the hosted config); the phase-1 evidence walk's served-line pin holds beside the degraded block the evidence page now renders from the flag |
| `b21-rehearsal.txt` | the B21 act rehearsed on a restored copy of `eye_demo` (`eye_demo_b21` on :3411 with the vault COPIED and B18–B20's rehearsal-only edits) — the NINTH rehearsal, held whole (57 checks, 143 s); the eight stops before it recorded in the header: the wedge (a product defect of phase 5's making, fixed — §B21.4), the act's carried versions without a cut-off and the guard's order, the forecast's class read by the rule's order and the act disputing a run of its own, then that run refused in turn by the product's right rules (the version's `known_at`, the unflipped branch, the twin's own shipments, the comparable control); the upheld challenge path and the promotion-to-simulation refusal shown REHEARSAL ONLY; nothing on `eye_demo` touched |
| `act-b21.txt` | the B21 act on the NORDWERK demonstration: the backup FIRST to `.eye-local/backups/` (52,584,453 bytes), 0081 on `eye_demo`, the restart on the B21 build; the register 40/10/0; the forecasts, scenarios and decisions subscriptions of the origin revoked and registered anew from the revoked cursor's own event, the four other kinds left; the honest defaults; the twin's owner refused by the separation of duties and the administrator validating version 5 FIT under the envelope the port computed, the control run carrying the verdict; N. Eriksen's corridor forecast UNFIT (`data_shift`) with its measures, the event and its six deliveries, idempotent on repeat; a scenario on it refused; J. Weber's duplicate-branch scenario ADMITTED failed and routed, the run on its branch refused, retired, the successor passed; the challenge of the act's own hypothetical run, the re-run bound and compared, dismissed, the control promoted fit, the operator refused to promote his own; the demonstration's archive root marker moved aside by the act and restored byte-identical — the cold NORDWERK record metadata-only with its custody and audit rows, no integrity incident, verified again after — ALL SCENES HELD (54 checks, 22.2 s) |
| `hosted-a2303ff-build-test-summary.txt` | the totals of the hosted `ci` run 35982420268 at `a2303ff` (PR #59; one attempt): unit 2389/2389, the meta suite 9/9, acceptance 58/58, the integration suite 1086/1086 in 74 files on a fresh database (the three B21 harnesses), the upgrade proof +60 rows / 81 files, C18 623/623 + 44; supply-chain green on the returned official pins with the recheck's return transition passing; browser-regression 51 passed; C19 lifecycle 35982420259 green — the B21 hosted run, bound once in the records commit |
| `b22-1-nonce-sweep.txt` | B22.1 (0082): `phase6-nonce-sweep-b22.test.ts` 6/6 (N1 two connections — the pre-0082 statement waits 55P03, the 0082 minter does not, a login meanwhile, batches of 500; N2 the authority unchanged; N3.a–d the residual nested paths under crossing nonces) and the CONTROL on the pre-0082 body — all four paths wedge |
| `b22-2-harness.txt` | B22.3 (0083): `phase6-attention-b22.test.ts` 11/11 on `eye_verify_b22_a5`, `_a6` and `_a7` — the policy, the eleven kinds, forecast unfit, scenario incoherent, the warning window, source health (7 markers), observations (no_plan / selected), proposals (PLANTED), quarantine (PLANTED), escalation (due_at moved — stated), policy change + reopen |
| `b22-pins.txt` | the existing pins B22 moved (the register 44/6/0, the eleven kinds, roles 35, migrations 62) — the touched files green on fresh databases |
| `b22-int-all.txt`, `b22-unit.txt`, `b22-acceptance.txt`, `b22-upgrade-proof.txt`, `b22-browser.txt` | at the B22 tree: the full integration suite (run 1 1100/1103 — briefing-memory's timing flake, preserved; run 2 1103/1103 in 76 files), unit 2390/2390 + 9/9, acceptance 58/58, the upgrade proof 0022–0083 PASS, the browser gate 51/51 |
| `b22-rehearsal.txt` | the B22 act rehearsed on `eye_demo_b22` (its own Redis :6392, a vault clone) — the fifth rehearsal held whole; the four act-side stops recorded |
| `act-b22.txt` | the B22 act on the NORDWERK demonstration after the container return and 0082–0083 — ALL SCENES HELD (128 s) |
| `hosted-9f6a77e-build-test-summary.txt` | the totals of the hosted `ci` run 36043290422 at `9f6a77e` (PR #60; one attempt): unit 2390/2390 + 9/9, acceptance 58/58, the integration suite 1103/1103 in 76 files, the upgrade proof through 0083, C18 623 + 44, browser 51, supply-chain green; `567e669`'s failed audit control preserved |
