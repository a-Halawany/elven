# CP-6 B9 — the adversarial review of the candidate c93cdad before its independent review (2026-09-13): six finders (one dimension each), two independent refuters per defect; a finding is CONFIRMED when neither refuter could refute it. The corrections are migration 0067 and the TypeScript of the same commit (PHASE6_REPORT §22.7). 58 agents; the workflow run wf_490be347-15e.

== executive: 7 findings
  CONFIRMED | Analysis request is stranded 'routed' when the agent run is refused after the request write has committed; the 'refused' state and 'request.refused' event are never written | apps/api/src/executive/executive.controller.ts:246
      scenario: POST /executive/requests {kind:'analysis', request_key:'k1', subject:{object_type:'DRM', object_id:<room>, task:'briefing', agent_id:<decision agent>}}. The first pipeline.write (line ~240) commits executive.requests state='routed' and enqueues ExecutiveActionRequested. Then agents.run → executive.open_agent_run raises 'run rejected: a decision agent does not run the task briefing' (0046:138) → 403 to the caller. The same happens when subject.agent_id is absent and activeAgent() finds no active agent (404 at line 268) or the requester lacks agent.read (strategy_owner/decision_authority are not in the agent.read rule, pdp.service.ts:609) → 403. In every case the request row stays state='routed' with no refusal recorded — no port ever writes state='refused' or event 'request.refused' (0066 §9 lines 2312, 2331) — and the published event says it was routed to an agent run. A retry with the s
      refuter: stands — Not refuted; the traced code path matches the claim. In apps/api/src/executive/executive.controller.ts:236-241 `pipeline.write` runs executive.open_request inside its own transaction (pipeline.service.ts:173-187 commits the request row state='routed' and enqueues ExecutiveActionRequested in that transaction) and returns. Only afterwards (lines 243-252) does the controller resolve the agent and call `this.agents.run(...)`, with no try/catch and no compensating write. In agents.service.ts:189-206 
      refuter: stands — Not refuted; the stranded-'routed' scenario is reproducible from the code as written and nothing prevents it.

Chain, verified at c93cdad:
1. apps/api/src/executive/executive.controller.ts:238-243 — the first `pipeline.write` (PipelineService.write, apps/api/src/pipeline/pipeline.service.ts:173-194) runs executive.open_request inside its own transaction and commits: the executive.requests row at state='routed', routed_to='agent.run:briefing', the 'request.opened' ledger event, and the ExecutiveA
  CONFIRMED | Privilege escalation: strategy_owner and decision_authority can trigger an agent run through an analysis request without holding agent.trigger | apps/api/src/policy/pdp.service.ts:955
      scenario: The operator route POST /agents/decision/:agentId/run is gated by 'agent.trigger' (pdp.service.ts:597: platform_admin, domain_admin, decision_owner, executive). The 'executive.request' rule (line 955) also admits strategy_owner and decision_authority. A human strategy_owner posts {kind:'analysis', subject:{object_type:'DPK', object_id:<pkg>, version:N, task:'draft', agent_id:<decision agent>}}: open_request records it, and executive.controller.ts:253 calls agents.run under the agent's own session with no check of the requester's agent.trigger authority. The decision agent drafts option cards on the package at the strategy owner's command, an act the policy refuses them on the direct route. (Without subject.agent_id the same requester hits the agent.read denial after the request is committed — see the stranded-request finding.)
      refuter: stands — Not refuted; the scenario reproduces as described and nothing on the path stops it.

Reproduction: a human holding only strategy_owner (or decision_authority) at DOMAIN scope posts POST /v1/tenants/:t/domains/:d/executive/requests with {kind:'analysis', request_key:'k1', instruction:'draft the options', subject:{object_type:'DPK', object_id:<package in draft>, version:<current>, task:'draft', agent_id:<active decision agent>}}.

What happens, file by file:
- apps/api/src/executive/executive.cont
      refuter: stands — Not refuted; the scenario is exactly what the code does. pdp.service.ts:596-600 limits agent.trigger to platform_admin/domain_admin/decision_owner/executive, while executive.request at pdp.service.ts:955 also admits strategy_owner and decision_authority. executive.controller.ts:243-247 asserts only executive.request (target EXR); validateRequest (requests.service.ts:29-56) only shape-checks subject.task/agent_id; executive.open_request (0066 §9, sql line 2443/2470) asserts only executive.request
  CONFIRMED | fulfil_request does not bind the fulfilling action to the request's kind and accepts any uuid as the answering act | apps/api/migrations/0066_serving_ownership_and_reach.sql:2542
      scenario: assert_authority admits 'executive.request.fulfil', 'executive.request', 'prediction.scenario.declare', 'simulation.run', 'decision.package.declare' and 'decision.package.review' for every request regardless of r.kind/r.routed_to, and p_routed_ref is only checked for NULL. (a) A forecast_owner declares a scenario with payload.requestId = a routed 'decision' request (prediction.controller.ts:431): the decision request becomes state='fulfilled', routed_ref=<scenario id>, fulfilled_by=<forecast owner>, and the decision owner never reviews the package. (b) Any principal with the executive.request.fulfil roles (line 957, no human_gate) can POST /executive/requests/:id/fulfil with routed_ref = any random uuid (executive.controller.ts:289 validates format only) and close any routed request in the domain, the ledger recording an act that does not exist. Neither the requester nor the responsible 
      refuter: stands — Not refuted; the code path is exactly as claimed. apps/api/migrations/0066_serving_ownership_and_reach.sql:2544-2559 executive.fulfil_request: assert_authority is any-of (0022:62-73, `v_action = ANY(p_allowed)`), and the array at :2549 admits 'executive.request.fulfil', 'executive.request', 'prediction.scenario.declare', 'simulation.run', 'decision.package.declare' and 'decision.package.review' without comparing the bound action to r.kind or r.routed_to; the only checks are scope (:2550), p_acto
      refuter: stands — Not refuted; the code matches the claim and nothing elsewhere prevents it.

executive.fulfil_request (apps/api/migrations/0066_serving_ownership_and_reach.sql:2544-2559) does: assert_authority over the union set ['executive.request.fulfil','executive.request','prediction.scenario.declare','simulation.run','decision.package.declare','decision.package.review'] (2549; assert_authority in 0022:62-73 is an any-of membership test on the context's bound action), assert_scope, actor = eye_principal, row
  CONFIRMED | A delegation outlives the lender's membership: is_member honours it after the delegator is removed from the room, and only the lender can revoke it | apps/api/migrations/0066_serving_ownership_and_reach.sql:2429
      scenario: Member A lends standing in room R to B until +30d (open_request checks A is owner/live member only at open time, lines 2487-2490). Next day the room owner removes A via executive.set_membership (op 'remove'); nothing touches executive.delegations (set_membership unchanged since 0044). is_member(R, B) (lines 2429-2435) still returns true: state='active', until_at > now(), no test that from_principal_id is still a member. B reads the room, its briefings, replays, records reviews and composes briefings for 29 more days on a standing A no longer has. withdraw_request (line 2582) requires p_actor = requester, so the room owner cannot revoke it; only A can.
      refuter: stands — Traced and confirmed; the scenario happens exactly as described. (1) apps/api/migrations/0066_serving_ownership_and_reach.sql:2429-2435 — executive.is_member's third disjunct tests only d.room_id, d.to_principal_id, d.state='active', from_at<=now(), until_at>now(); nothing joins from_principal_id back to executive.room_members with removed_at IS NULL or to rooms_current.owner_principal_id. (2) The lender's standing is checked only at open time (0066:2487-2490). (3) executive.set_membership is de
      refuter: stands — Not refuted; the scenario reproduces from the code. executive.open_request (apps/api/migrations/0066_serving_ownership_and_reach.sql:2487-2490) checks the lender's standing only at open time and inserts an 'active' executive.delegations row bounded solely by until_at. executive.set_membership is defined only in 0044 (lines 178-206) and not touched by 0066; its 'remove' branch (0044:201) sets room_members.removed_at and nothing else — no trigger on room_members, no constraint or FK on executive.d
  observation | Concurrent double-submit of the same request_key returns 409 conflict instead of the recorded request | apps/api/migrations/0066_serving_ownership_and_reach.sql:2449
      Two requests with the same (requester, request_key) and the same digest arrive concurrently with different message_ids. Both SELECT ... FOR UPDATE find no row (nothing is locked), both INSERT; the second blocks on xrq_key_once, and after the first commits it fails with 23505. asObservationRefusal maps an unrecognised 23505 to a generic 409 (observation-errors.ts:464), so the second caller receives a conflict rather than the recorded row and must retry. No second effect or event is produced, so exactly-once holds for the effect but not for the documented 'returns the request already recorded' c
  observation | Follow-up owner is not validated as an active principal of the tenant (unlike delegate); subject.agent_id is not validated as a uuid | apps/api/src/executive/requests/requests.service.ts:49
      uuidOrNull('owner') accepts any uuid; open_request (0066 §9 follow_up branch, line ~2521) writes owner_principal_id = p_owner with no identity.principals check, whereas the delegation branch refuses a non-active delegate (line 2491). A follow-up owned by a non-existent or foreign-tenant principal sits on the package's agenda and is briefed with that owner; only the requester can ever complete it. Separately, subject.agent_id (controller line 249) is passed to agents.run without a UUID check, so a malformed agent_id yields a pg 22P02 cast error (500) after the request has been recorded.
  observation | Briefing as-of instant drops withdrawn follow-ups regardless of when they were withdrawn | apps/api/src/executive/briefings/briefing.service.ts:318
      executive.follow_ups records no withdrawn_at; the briefing skips any follow-up with state='withdrawn' even when knownAt precedes the withdrawal, while suppressions (lifted_at) and delegations (revoked_at) are reconstructed as-of knownAt. A briefing composed as-of last Monday for a follow-up withdrawn on Tuesday omits a window that stood then.

== scenario-memory: 9 findings
  CONFIRMED | Memory item content readable under graph.read, bypassing purpose, clearance, audience and the access ledger | apps/api/src/graph/graph.controller.ts:754
      scenario: POST /memory/list (line 754-760) and POST /memory/:itemId/get (line 763-771) run under route action 'graph.read' with GraphCapability.read and return memory.items_current rows via selectAll() (memory.service.ts:169 current(), :206 list()). memory.items_current carries the current version's full content: statement, title, source_ref, related_decision_id, classification. None of the OBJ-15 gates in MemoryService.retrieve (purpose admitted-for/audience_purposes, assertClearance against classification, audience_roles) run, and no memory.item_access row nor memory.retrieved event is written. Concrete: the test at phase6-graph-subscriptions-4.test.ts:335-337 records a 'confidential' item and shows the domain_analyst is refused by /retrieve with no access row; the same analyst holds graph.read (pdp.service.ts:765-790 lists domain_analyst, forecast_agent, resolution_agent, ...) and can read that
      refuter: stands — Traced and confirmed; the claim stands. apps/api/src/graph/graph.controller.ts:754-771 (added by B9: `git diff 3932207 c93cdad` shows the file is 168 pure insertions) route POST /memory/list and POST /memory/:itemId/get through pipeline.consequentialRead under action 'graph.read' with GraphCapability.read, and hand back `this.memory.list(cap, ...)` / `this.memory.current(cap, itemId)` unchanged. memory.service.ts:169 (`current`) and :206 (`list`) do `cap.readMemoryItems().selectAll()`, and graph
      refuter: stands — Not refuted — the scenario is reproducible from the code as written and nothing else stops it.

What I verified:
- apps/api/src/graph/graph.controller.ts:754-760 (`POST /memory/list`) and :763-771 (`POST /memory/:itemId/get`) are new in B9 (git diff 3932207 c93cdad confirms) and run under route action `graph.read` with `GraphCapability.read` through `pipeline.consequentialRead`. `consequentialRead` (apps/api/src/pipeline/pipeline.service.ts:204-241) evaluates policy, writes POL+AUD, then runs th
  CONFIRMED | As-of retrieval enforces the CURRENT version's audience (roles/purposes) on a historical version, while classification is taken from the served version | apps/api/src/graph/memory/memory.service.ts:190
      scenario: retrieve() picks `served` from objects.canonical_objects by asOf (line 187) and checks classification from served (line 194), but audience_purposes (line 190) and audience_roles (line 195) come from `item` = memory.items_current, i.e. the latest version. Scenario: v1 recorded with audience.roles ['executive'] (restricted to executives); the record authority supersedes to v2 with audience.roles [] (or a broader purposes list). A domain_analyst retrieving asOf a time before the supersession passes the role gate (roles.length === 0 from the current row) and is served v1's payload, which v1's own audience never admitted. The inverse also holds: tightening the audience in v2 makes v1 unreadable as-of, so the 'known-at read serves the version current then' replay is not under that version's own audience. Either both checks should read the served version's canonical payload (audience.roles/purp
      refuter: stands — Not refuted — the code path is exactly as claimed and nothing elsewhere prevents the scenario.

Trace (apps/api/src/graph/memory/memory.service.ts, candidate c93cdad):
- L181: `item = await this.current(cap, a.itemId)` reads memory.items_current — the CURRENT projection row.
- L187: `served` is selected from objects.canonical_objects by `recorded_at <= asOf` — the historical version.
- L189: `admittedFor = served['purpose_scope']` (served version).
- L190: `purposes = item['audience_purposes']` 
      refuter: stands — Not refuted; the scenario reproduces exactly as described and nothing else prevents it.

Code (apps/api/src/graph/memory/memory.service.ts, new in B9): retrieve() picks `served` from objects.canonical_objects by asOf (line 187), takes the purpose it was admitted under from `served` (line 189) and the classification from `served` (line 194), but takes `audience_purposes` (line 190) and `audience_roles` (line 195) from `item` = memory.items_current, which memory.record_item overwrites on every sup
  CONFIRMED | review_scenario 'retire' overwrites the named branch record with the loop variable, so the returned/published branch (kind, state_after) can describe a different branch | apps/api/migrations/0066_serving_ownership_and_reach.sql:2114
      scenario: DECLARE has one `b prediction.branches_current%ROWTYPE`. When p_branch_id is given, line 2100 loads it into b. For outcome 'retire', line 2114 `FOR b IN SELECT * FROM prediction.branches_current ... state = 'open' LOOP` reassigns b on every iteration; after the loop b is the LAST open branch. Line 2135 then builds 'branch' as jsonb_build_object('branch_id', p_branch_id, 'kind', b.kind, 'state_after', CASE WHEN ... b.state = 'open' THEN 'closed' ELSE b.state END). Concrete: retire with branch_id = X (kind 'downside', state 'flipped') on a scenario that still has open baseline Y -> the port returns and ScenarioReviewed (scenarios.service.ts:476, `branch: review['branch']`) publishes {branch_id: X, kind: 'baseline', state_after: 'closed'}, although X remains 'flipped' (the retire path only closes open branches) and is a 'downside'. The interface record L7-I05 therefore names a branch with a
      refuter: stands — Confirmed by tracing the code. In prediction.review_scenario (apps/api/migrations/0066_serving_ownership_and_reach.sql, §8) there is a single row variable `b prediction.branches_current%ROWTYPE`; it is loaded with the named branch when p_branch_id is given, then for outcome 'retire' the loop `FOR b IN SELECT * FROM prediction.branches_current x WHERE x.scenario_id = p_scenario_id AND x.state = 'open' LOOP` reassigns `b` on every iteration, so after the loop `b` is the last open branch (with pre-
      refuter: stands — Not refuted. The code is exactly as the reviewer describes. apps/api/migrations/0066_serving_ownership_and_reach.sql:2088 declares a single `b prediction.branches_current%ROWTYPE`; line 2100 loads the named branch into b when p_branch_id is given; line 2114 `FOR b IN SELECT * FROM prediction.branches_current x WHERE x.scenario_id = p_scenario_id AND x.state = 'open' LOOP` reuses that same variable as the query-loop target (in PL/pgSQL a query-loop target is not loop-local; it keeps the last row 
  unjudged | An unparseable asOf on memory retrieve throws RangeError (500) instead of a named refusal | apps/api/src/graph/graph.controller.ts:737
      scenario: `new Date(String(body.payload.asOf)).toISOString()` is evaluated before any validation; for payload.asOf = 'yesterday' or '2024-13-45', new Date(...) is Invalid Date and toISOString() throws RangeError: Invalid time value. The request fails outside the pipeline with a generic error and no EYE_REQ_001 reason, and no pre-handler rejection record (rejectAuthenticatedRequest) is written — unlike the scenario-review controller which validates next_review_by with Date.parse (prediction.controller.ts) before calling the port.
  observation | A withdrawn memory item is still served on a current (asOf = null) retrieval; 'leaves circulation' is only a field in the response | apps/api/src/graph/memory/memory.service.ts:187
      retrieve() never consults item.state. After memory.withdraw_item sets state = 'withdrawn' (migration §3, line ~188 of the section), a POST /retrieve with no asOf returns versions[0] (the last version's full payload) with availability.state = 'withdrawn' and writes an access row. The service doc (line 22) says withdrawal takes the item 'out of circulation' with versions replayable, which reads as: as-of reads replay, a current read is refused (naming 'withdrawn'). As written, any purpose-authorised reader keeps reading a withdrawn record as if current; only the caller's inspection of `availabil
  observation | The writer's envelope purpose becomes an implicit audience purpose of the version, outside the owner's declared audience | apps/api/src/graph/memory/memory.service.ts:191
      write() stamps header.purpose_scope = envelope.purpose_id ?? 'memory' (line 145; graph.controller.ts:676/700). retrieve() then admits any reader whose stated purpose equals served.purpose_scope even when audience.purposes excludes it (line 191: `a.purpose !== admittedFor && !purposes.includes(a.purpose)`). Concrete: the owner records with audience.purposes ['memory']; the record authority supersedes under envelope purpose 'audit' -> v2 is readable under purpose 'audit' by every role in the memory.item.retrieve list, although the owner never admitted that purpose. Not a rule violation as stated
  observation | Refused retrievals leave no trace in the item's own access ledger; the 403 body enumerates the item's admitted purposes and audience roles to the refused reader | apps/api/src/graph/memory/memory.service.ts:192
      A read refused by purpose (line 192), clearance (line 194) or audience roles (line 199) throws inside consequentialReadEvidenced, so only pipeline.recordHandlerFailure writes a generic AUD failure — memory.item_access and memory.item_events get nothing (test line 337 asserts this). That is defensible (the ledger records versions served), but the refusal text discloses `admittedFor`, the full audience_purposes list and the full audience_roles list to a reader who was just refused, i.e. metadata about a possibly restricted record. 'A refusal names its reason' does not require listing the audienc
  observation | scenarios_current.state 'retired' has no projection-rebuild check; branches_current(flipped) stays consistent | apps/api/migrations/0066_serving_ownership_and_reach.sql:2073
      Retirement closes only 'open' branches (line 2114) and emits branch.closed + scenario.retired events; flipped branches keep state 'flipped', so prediction.rebuild_projections (0065:1284-1291, unchanged by 0066) still matches the set of branch.flipped events — consistent. However 0066 adds the 'retired' scenario state and the 'scenario.retired' event without any rebuild row for scenarios_current, so a projection drift (e.g. a scenario row left 'active' after a partial restore) is not detected by the A11 rebuild. The 'closed' branch state from retirement is likewise not derived from branch.close
  observation | §11 trigger and raise_warning are consistent; no legitimate inserter writes warnings without a level | apps/api/migrations/0066_serving_ownership_and_reach.sql:2670
      Checked: the only INSERT into prediction.warnings_current in the live code path is prediction.raise_warning (latest body 0065:1079), which derives level/level_version/urgency via derive_warning_level (absent class resolved by the version's absent_class_rule) and always writes them; no TypeScript inserts directly; rebuild_projections only counts. wrn_level_coherent still allows the all-NULL legacy row and warning_level_immutable (BEFORE UPDATE) leaves NULL-to-NULL updates alone, so acknowledgement/attention/close of the pre-0061 row now succeeds. No defect found on this question; noted so the r

== retention: 7 findings
  unjudged | A partially refused execution commits tombstones and removes bytes under an action that can never be verified, re-resolved, executed or withdrawn | apps/api/src/retention/retention.service.ts:99
      scenario: Action with manifests A and B both 'execute' is resolved and approved. A legal hold is placed on B after approval. Execute: A is tombstoned (savepoint released, execution 'done', locator pushed), B is refused by observation.tombstone_blob (P0R01), so finish_execution is called with 'failed' (retention.service.ts:99) — yet the transaction commits A's tombstone, and retention.controller.ts:110 still removes A's bytes from the vault (locatorsToRemove is returned regardless of refused > 0). The action is now state 'failed' with disposition 'retry' and failure_reason 'resolve the scope again', but every route refuses it: resolve_scope (0066 line 1108) admits only opened/scope_resolved/held/paused, begin_execution requires 'approved', verify_action requires 'executed', withdraw_action (line 1358) excludes 'failed'. A's bytes are gone with no verification row and no DeletionVerified; a later ac
  CONFIRMED | A post-commit byte-removal failure is unrecoverable: the sweeper never removes bytes of tombstoned evidence manifests, and the failure is recorded nowhere durable | apps/api/src/retention/retention.service.ts:103
      scenario: vault.tombstone throws for one locator after the execute transaction committed (EACCES, disk fault). removeBytes swallows it and returns it only in the HTTP body (controller line 111); retention.executions still says 'done' for that item and no residual row is written. The comment claims the bytes are 'orphaned for the sweeper', but sweeper.service.ts:222-232 skips every locator that has a manifest row (only manifest-less files and .tmp- files are touched), so nothing will ever remove them. verify_action then fails (bytes_present true, line 1304) and sets disposition 'retry', but execution cannot be re-run (state 'executed' ≠ 'approved') and verify will fail forever — a tombstone that says deleted over bytes that stay. Related: 'verified_with_residuals' is unreachable — every residual_inventory insert (lines 1148-1189) is 'retained_by_policy', 'pending' is never written, and verify_actio
      refuter: stands — Not refuted; the core of the claim holds on the code as written, with one overstatement noted.

Reproduction path (all at c93cdad):
1. Execute commits the tombstone and the 'done' execution row inside pipeline.write (apps/api/src/retention/retention.controller.ts:107-108; retention.service.ts:79-83). Only afterwards does removeBytes call vault.tombstone (controller:110; service:104-110). vault.tombstone is `rm(full, {force:true})` (apps/api/src/observation/vault/vault.service.ts:236-241): ENOENT
      refuter: stands — Not refuted; every link in the scenario checks out against the code at c93cdad.

1. The failure is possible and swallowed. VaultService.tombstone (apps/api/src/observation/vault/vault.service.ts:237-242) does `rm(full, {force:true})` — force only masks ENOENT; EACCES/EPERM/EIO throw. RetentionService.removeBytes (retention.service.ts:104-110) catches and pushes to `failed`. RetentionController.execute (retention.controller.ts:107-111) calls removeBytes AFTER `pipeline.write` returned (the execut
  CONFIRMED | objects.outbox_declare_floor does not re-check subscriptions' served points at execution; the blocking check exists only at scope resolution | apps/api/migrations/0066_serving_ownership_and_reach.sql:1377
      scenario: log_floor action to_seq=100 resolves with no blocking subscription, is approved. Before execution a consumer registers with backlog_policy 'replay' (0065 line 709: served_from_seq = current retained floor, e.g. 1) or an existing subscription is replayed from the floor (0065 line 590 sets served_from_seq = least(served_from_seq, from+1)). Execute → outbox_declare_floor checks only that an executing log_floor action names (partition, to_seq) and that to_seq lies in (floor, next_seq]; it never consults graph.subscriptions, so the floor moves to 100 above a live subscription served from 1 — exactly what the §4 preamble says the act refuses ('refuses while a subscription's served point or cursor ... lies below the new floor'). The approval digest does not cover subscription state either, so nothing else catches it. The port should re-run the served_from_seq/checkpoint_seq (and unpublished/dea
      refuter: stands — Traced and confirmed at c93cdad. (1) retention.resolve_scope (0066:1178-1183) is the only place graph.subscriptions is consulted; it marks a served_from_seq/checkpoint_seq below to_seq as a 'blocking' scope item and pauses the action. (2) objects.outbox_declare_floor (0066:1367-1385) checks only: caller authority, the tenant partition, that an actions_current row in state 'executing' with kind 'log_floor' names exactly (partition_key, to_seq), and that to_seq lies in (floor, next_seq]; it never 
      refuter: stands — Not refuted; the scenario is reachable and nothing else prevents it.

What the code does (apps/api/migrations/0066_serving_ownership_and_reach.sql):
- retention.resolve_scope (lines 1178-1183) is the only place graph.subscriptions is consulted; it marks a served_from_seq < to_seq (or checkpoint_seq < to_seq-1) subscription as a 'blocking' item and pauses the action. It reads graph.subscriptions without any lock and takes no lock on objects.outbox_partitions.
- objects.outbox_declare_floor (lines
  CONFIRMED | verify_action's floor check is strict equality, so an action whose floor was since moved further by a later action can never verify | apps/api/migrations/0066_serving_ownership_and_reach.sql:1318
      scenario: Action 1 (to_seq 10) executes: floor = 10, not yet verified. Action 2 (to_seq 20) is opened, resolved (floor 10 < 20 ≤ next), approved and executed: floor = 20. Verify action 1: observed retained_from_seq 20 ≠ expected 10 → passed=false → failure_class 'infrastructure', disposition 'retry', state stays 'executed' — and every retry fails identically; action 1 never closes and its DeletionVerified is never published although its own execution record (outbox_declare_floor's returned floor_after = 10) proves the move. The check should accept retained_from_seq >= to_seq (the floor is monotone by the port's own rule at line 1381).
      refuter: stands — Not refuted — the scenario is reachable and nothing in the migration, the service, the policy or the tests prevents it.

Code path (apps/api/migrations/0066_serving_ownership_and_reach.sql):
- Line 1317-1318, retention.verify_action: `SELECT p.retained_from_seq INTO v_floor ...; v_pass := v_floor = (a.selector ->> 'to_seq')::bigint;` — strict equality against the partition's CURRENT floor, not against the action's own execution record.
- Lines 1328-1332: on any failed check the action is set fai
      refuter: stands — Not refuted; the scenario is reachable through the ordinary routes and nothing in the port, the policy, a constraint, or a test stops it.

Traced guards (apps/api/migrations/0066_serving_ownership_and_reach.sql):
- retention.open_action (1041-1057): for log_partition it checks only the selector shape and that partition_key is the caller's tenant; no check for another log_floor action on the same partition in any state.
- retention.resolve_scope, log branch (1160-1197): checks v_to > retained_fro
  observation | The 'let it catch up' route named in the blocking reason does not exist: served_from_seq never advances, so a replay-registered subscription blocks every floor move above its registration point permanently | apps/api/migrations/0066_serving_ownership_and_reach.sql:1183
      resolve_scope blocks on s.served_from_seq < v_to. served_from_seq is set once at registration (0065 line 709/715) and thereafter only moves backwards (0065 line 590: least(...)); a consumer that has applied every row up to next_seq still has served_from_seq at its original floor. The paused action's reason tells the person to 'let it catch up', which changes nothing; the only way to move the floor above that point is to revoke the subscription. The refusal names a remedy that cannot work — the predicate should use the subscription's checkpoint/last-delivered position, or the reason should say 
  observation | begin_execution's approver-never-executes check trusts the caller-supplied p_actor rather than the acting principal | apps/api/migrations/0066_serving_ownership_and_reach.sql:1246
      record_approval binds the approver to public.eye_principal() (line 1220) so the port, not only the policy, enforces who approved. begin_execution (line 1246) compares ap.approver_principal_id to p_actor only; nothing binds p_actor to public.eye_principal(). Today the controller passes principal.principalId, so the separation holds by the calling code, not by the port — contrary to the rule that separation of duties is enforced in the port. A one-line `IF p_actor <> public.eye_principal()` refusal, as in record_approval, closes it.
  observation | The harness assertion for 'the tombstone port refuses a held manifest whatever the caller' does not exercise the hold check | apps/api/test/int/phase6-graph-subscriptions-4.test.ts:466
      The call runs on the superuser connection with no ctx action, so observation.tombstone_blob raises in observation.assert_authority before the legal-hold branch (0066 line 1404) is reached; `.rejects.toThrow()` matches that authority error. The held-manifest refusal (P0R01, and its 409 mapping in B9_REFUSALS) is therefore unverified by this test; asserting on /under a legal hold/ from a governed context with 'retention.action.execute' (or 'observation.sweeper.reconcile') would prove it.

== ports: 12 findings
  CONFIRMED | Relationships consumer re-opens a reassessment the person already closed (decided:kept) on every re-drive of a blocked item | apps/api/src/graph/subscriptions/consumers/relationships.consumer.ts:79
      scenario: A REL claim is corrected v1→v2 to a mention no accepted entity carries. The consumer opens the reassessment (pending), derivation is blocked, the item stays unresolved and the delivery is re-checked on every reconcile tick. The person then keeps the edge (graph.keep_edge_under_reassessment: state stays 'asserted', reassessment_state='reassessed', outcome 'decided:kept'). On the next re-check applyItem sees state='asserted' and claim_version 1 < 2, so it calls cap.openEdgeReassessment again; graph.open_edge_reassessment (0066 line 365-395) finds v_state='reassessed' (not NULL, not 'pending'), takes the ELSE branch and sets reassessment_state='pending', reassessed_at=NULL, reassessment_outcome=NULL — the person's decision is silently undone, and the delivery never terminates because nothing in applyItem treats a decided reassessment (reassessed_at after the cause) as basis.unchanged. The t
      refuter: stands — Not refuted; the scenario is reproducible from the code as written and nothing guards it.

Trace (candidate c93cdad):
1. apps/api/src/graph/subscriptions/consumers/relationships.consumer.ts:64-79 — applyItem only exits early on (a) edge missing, (b) edge.state != 'asserted' (line 68), or (c) claim_version >= to_version (line 74). It never reads reassessment_state, reassessed_at or reassessment_outcome. After the person keeps the edge, the row is state='asserted', claim_version=1 (< 2), so all th
      refuter: stands — Not refuted; the trace confirms the scenario end to end.

1. Re-drive happens: graph.subscription_deliveries_to_reconcile (0064 line 465) selects deliveries with state='unresolved' once last_delivered_at is older than the recheck interval; the dispatcher's tick calls reconcile(..., UNRESOLVED_RECHECK='10 minutes') (subscription-dispatcher.service.ts:85,148) and startup/registration/resume/replay re-drive at once. An unresolved item is not checkpointed as applied (dispatcher :479 "the item does n
  CONFIRMED | Every re-check of a blocked re-derivation appends a duplicate cause (same cause_id) to the edge and writes a duplicate edge.reassessment_opened event | apps/api/migrations/0066_serving_ownership_and_reach.sql:380
      scenario: While the reassessment is still pending, each re-drive of the unresolved relationships delivery (periodic reconcile, UNRESOLVED_RECHECK) calls graph.open_edge_reassessment with the same event_id as cause_id. The 'pending' branch does reassessment_reason := reassessment_reason || '; ' || p_reason and reassessment_causes := reassessment_causes || {same trigger, same cause_id, same reason}, and inserts another edge.reassessment_opened event with already_pending=true. The write commits with the unresolved check (the item's transaction commits, only the item is left open), so reassessment_reason and reassessment_causes grow without bound and the ledger records N openings for one cause. The consumer comment calls this 'idempotent'; it is not — the port needs a cause_id guard or the consumer must not re-open on re-check.
      refuter: stands — Not refuted; the trace confirms the scenario exactly as described.

Path: (1) An unresolved delivery is re-driven by the periodic tick (apps/api/src/graph/subscriptions/subscription-dispatcher.service.ts:85-86, :149, UNRESOLVED_RECHECK = '10 minutes') and on every start/registration/resume/replay (recheck '0'). (2) graph.subscription_item_begin (apps/api/migrations/0066_serving_ownership_and_reach.sql:234-262) returns true for an item that is not in items_applied — an unresolved item was never a
      refuter: stands — Not refuted; the defect is real and reproduces in the candidate's own verification residue.

Code path: apps/api/src/graph/subscriptions/consumers/relationships.consumer.ts:79 calls cap.openEdgeReassessment(..., causeId: event.event_id) unconditionally, BEFORE the readability/derivation checks at lines 81-92 that return `unresolved`. The dispatcher (apps/api/src/graph/subscriptions/subscription-dispatcher.service.ts:478-483) treats an `unresolved` return as a normal effect return: ledger.itemUnr
  CONFIRMED | Ontology refusal (and the review-state refusal) in assert_edge during automatic re-derivation is classified as an infrastructure fault and retried, never surfaced as unresolved work | apps/api/src/graph/subscriptions/subscription-dispatcher.service.ts:492
      scenario: A domain has an active ontology version; a REL claim whose predicate is not declared (or is corrected to an undeclared predicate) is corrected in review. derive.ts does not check the vocabulary, so the consumer calls cap.assertEdge and graph.assert_edge raises SQLSTATE 22023 (0066 line 2004). The PG error is a plain Error (pipeline.service.ts maps only 42501 capability refusals), so the catch at line 492 finishes the delivery 'failed' with failure_class 'infrastructure', disposition 'retry'; the whole item transaction (including the reassessment opening) rolls back, the builder's stated reason never reaches the person, and reconcile re-drives it indefinitely. Same for the 42501 'claim is queued/rejected for review' path (not an HttpException, so not the 403 branch). The consumer should pre-check the active ontology (or map these port refusals) and return unresolved_dependency → human_rev
      refuter: stands — The ontology half of the claim holds on the traced path; only the review-state (42501) half is refuted.

Confirmed path (ontology refusal): apps/api/src/graph/edges/derive.ts checks only review state, endpoint resolution/ambiguity, self-edge and lineage — no vocabulary check. apps/api/src/graph/subscriptions/consumers/relationships.consumer.ts:applyItem opens the reassessment (cap.openEdgeReassessment) and then calls cap.assertEdge with the corrected claim's predicate unchecked. graph.assert_edg
      refuter: stands — The ontology half of the claim reproduces from the code at c93cdad; only the review-state half is refuted.

Confirmed chain (ontology path):
1. apps/api/migrations/0066_serving_ownership_and_reach.sql:1999-2004 — graph.assert_edge raises 'edge rejected: predicate % is not in the domain''s active ontology version ...' with ERRCODE 22023 whenever the domain has an active ontology_versions row and the predicate is not declared in it.
2. apps/api/src/graph/edges/derive.ts:34-84 — deriveEdgeFromClaim
  CONFIRMED | findConflicts' 'rejected' filter is dead: a rejected claim still counts as a contradicting assertion | apps/api/src/intelligence/contradictions/contradiction.service.ts:56
      scenario: intelligence.decide_review (0023 line 786) only updates review_current/review_events; nothing ever updates objects.canonical_objects (no UPDATE exists), so a claim queued then rejected keeps payload.review.state='queued' and lifecycle_state='active'. Claim A (S,P,'10') is extracted below threshold, queued, rejected by a person. Later claim B (S,P,'12') is admitted with high confidence: findConflicts returns A (the check on line 56 never matches), B is queued with reason 'contradiction', a contradiction row A–B is recorded and ContradictionDetected is published against an assertion a person already rejected. Related: a REL claim queued with reason 'contradiction' and then approved keeps payload.review.state='queued', so derive.ts (and the builder) never promote it into the graph.
      refuter: stands — Not refuted; the scenario is reproducible from the code. (1) The only writer of payload.review.state on objects.canonical_objects is the extraction admission (apps/api/src/intelligence/extraction/extraction.service.ts:298-302: 'queued' | 'not_required') and the review CORRECTION path, which admits a new version with review.state='corrected' (review.service.ts:106-110). A 'reject' (and 'approve') decision runs only cap.decideReview (review.service.ts:211-215) → intelligence.decide_review (0023 ~l
      refuter: stands — Not refuted; the scenario is reachable as described and no other check prevents it.

Evidence:
1. apps/api/src/intelligence/contradictions/contradiction.service.ts:42-56 — findConflicts reads only objects.canonical_objects (IntelligenceReads.readCanonicalObjects() is `this.from('objects.canonical_objects')`, intelligence.capabilities.ts:223), filters lifecycle_state in ('active','corrected'), and excludes a row only when payload.review.state === 'rejected'. It never consults intelligence.review_
  CONFIRMED | Contradiction detection is silently bounded to the 2,000 most recent claim versions in the domain | apps/api/src/intelligence/contradictions/contradiction.service.ts:45
      scenario: findConflicts reads canonical_objects of the five claim types ORDER BY recorded_at DESC LIMIT 2000 and builds 'latest' only from that window. In a domain with more than 2,000 claim versions, an older admitted claim about (S,P) is never compared: a new incompatible claim admits with no link, no review case and no event, and nothing records that the window was exceeded. The interface promises linked, never collapsed; here they are simply not detected, with no stated limit.
      refuter: stands — Not refuted. Traced path: apps/api/src/intelligence/contradictions/contradiction.service.ts:42-45 reads objects.canonical_objects with only object_type IN (ENT,EVT,CLM,REL,ASM) and lifecycle_state IN (active,corrected), ORDER BY recorded_at DESC LIMIT 2_000 — no subject/predicate/claim_kind predicate in SQL; those are applied in memory (lines 57-59) only over the fetched window. The window counts every canonical object version of five types in the domain (entities, events, relationships, assessm
      refuter: stands — Not refuted; the code does exactly what the reviewer describes. apps/api/src/intelligence/contradictions/contradiction.service.ts:42-45 reads objects.canonical_objects filtered only by object_type IN (ENT,EVT,CLM,REL,ASM) and lifecycle_state IN (active,corrected), ORDER BY recorded_at DESC LIMIT 2000; the subject/predicate/claim_kind match (lines 57-58) is applied in memory AFTER the window is cut. Nothing in the migration prevents it: 0066 §5 has no DB-side detection (intelligence.record_contra
  CONFIRMED | decide_ontology_proposal enforces proposer≠decider against a caller-supplied p_actor, not the acting principal | apps/api/migrations/0066_serving_ownership_and_reach.sql:1953
      scenario: The SoD check is 'IF v.proposed_by = p_actor' and the ont_sod CHECK compares decided_by to proposed_by, but p_actor is never bound to public.eye_principal() — unlike this migration's own SoD ports (retention.record_approval line 1224, executive.open_request 2445, fulfil/withdraw/complete 2551/2571/2602) and the 0042/0044/0045/0048 convention. Any caller holding graph.ontology.decide (domain_admin also holds graph.ontology.propose) that passes a different actor id records decided_by ≠ proposed_by and activates its own proposal; the port, which is where the product says SoD is enforced, does not detect it. propose_ontology_version (proposed_by) is likewise unbound.
      refuter: stands — Not refuted. Traced path: apps/api/migrations/0066_serving_ownership_and_reach.sql:1941-1985 — graph.decide_ontology_proposal asserts authority (graph.ontology.decide) and scope, then at :1953 checks `IF v.proposed_by = p_actor`, writes `decided_by = p_actor` (:1956, :1978) and stamps the events' actor_principal_id with p_actor; nowhere does it compare p_actor to public.eye_principal(). The ont_sod CHECK (:1844, `decided_by <> proposed_by`) is not an independent guard because both columns are po
      refuter: stands — Not refuted. At c93cdad, apps/api/migrations/0066_serving_ownership_and_reach.sql:1953 checks `IF v.proposed_by = p_actor` and neither decide_ontology_proposal (1941-1985) nor propose_ontology_version (1861-1936) compares p_actor with public.eye_principal(); decided_by/proposed_by and ontology_events.actor_principal_id are written straight from the argument, so the ont_sod CHECK (1836) compares two caller-supplied values. Nothing else stops it: assert_authority checks the context's actions not t
  REFUTED | Adjudicating a contradiction as withdrawn/superseded changes nothing about the assertions, and detection ignores adjudications | apps/api/migrations/0066_serving_ownership_and_reach.sql:1498
      scenario: A (S,P,'10') and B (S,P,'12') are linked; the manager adjudicates 'a_withdrawn'. intelligence.adjudicate_contradiction only stamps the row: A stays lifecycle 'active', its payload unchanged, no review case or canonical version is touched, and findConflicts reads neither intelligence.contradictions nor the adjudication. The next claim C (S,P,'12') is queued for review with reason 'contradiction' and a new row A–C is recorded against the assertion a person declared withdrawn. Likewise when A is corrected to v2 (still contradicting B) a new row (B v1, A v2) is added while (A v1, B v1) stays 'open' referencing a superseded version — two open contradictions for one disagreement, the older never closed by supersession.
      refuter: refuted — The behaviour described is real (intelligence.adjudicate_contradiction at 0066:1498-1509 only stamps the row; ContradictionService.findConflicts at apps/api/src/intelligence/contradictions/contradiction.service.ts:39-65 reads objects.canonical_objects only), but it is the product's stated design, not a defect under its rules.

1. An adjudication is a record about the LINK, not an act on either assertion. The migration header (0066:1424-1430: "incompatible assertions LINKED, never collapsed … eac
      refuter: refuted — The facts the reviewer cites are accurate (intelligence.adjudicate_contradiction at 0066:1498-1509 only stamps state/adjudication on the row; ContradictionService.findConflicts at apps/api/src/intelligence/contradictions/contradiction.service.ts:39-66 reads objects.canonical_objects only), but neither scenario is a defect under this product's own rules — they are the designed behaviour of §5.

1. Adjudication is declared a RECORD, not a mutation of the assertions: 0066:1424-1430 ("incompatible a
  observation | Consumer's re-derived edge is not byte-equal to the builder's run: method_id, run_id and mode come from a different source | apps/api/src/graph/subscriptions/consumers/relationships.consumer.ts:105
      graph.orchestrator.ts:632 asserts methodId: null with runId/mode taken from payload.lineage; the consumer asserts methodId from the intelligence.claim_lineage row and prefers that row's run_id/mode over the payload's. For the same claim version the operator's build and the automatic re-derivation therefore write different edges_current rows (method_id NULL vs set; possibly different run_id/mode). The refusal rules are shared via derive.ts, but the record is not identical as the header claims.
  observation | assert_edge honours only predicate names of the active ontology; per-predicate subject/object types, and removed entity types, are never enforced; the check runs before assert_scope | apps/api/migrations/0066_serving_ownership_and_reach.sql:2004
      propose_ontology_version analyses 'narrowed' predicates by subject_types/object_types and counts entities of removed types, but assert_edge (line 1999-2005) only tests pr->>'predicate' = p_predicate, and decide_ontology_proposal blocks approval only on asserted edges by predicate (removed entity types with live entities are approved). The declared type constraints are decorative. Also the vocabulary check precedes assert_scope, so a principal with graph.edge.assert in its own domain can pass a foreign p_tenant/p_domain and learn from the error text whether that domain's active ontology declare
  observation | The four ontology reviews are self-attested by the decider in the deciding call | apps/api/migrations/0066_serving_ownership_and_reach.sql:1954
      decide merges body.payload.reviews into v.reviews before checking that compatibility='passed' and migration in ('passed','not_required') for a breaking change; the steward can pass {compatibility:'passed', migration:'passed'} in the same request. The only independent gate on a breaking approval is the impacted-edge count.
  observation | rebuild_projections does not verify methods_current.fitness_state although it gates admission | apps/api/migrations/0066_serving_ownership_and_reach.sql:1738
      lock_active_method and transition_method refuse on methods_current.fitness_state='unfit', but the rebuild excludes method.evaluated and checks only lifecycle_state; a drifted fitness_state (e.g. after a restore) would not be reported, while method_evaluations (append-only) holds the truth.
  observation | A challenge may be opened on a superseded claim version; the correction then applies to the latest version, not the challenged one | apps/api/migrations/0066_serving_ownership_and_reach.sql:1523
      request_review accepts any existing (object_id, version). Claim C was corrected v1→v2; a person challenges C v1; the decide route (intelligence.controller.ts:468-471) resolves the latest version (v2) and the correction writes v3 with MemoryCorrected from_version=2. The case says v1 was challenged, the record says v2 was corrected; the port should refuse a non-current version or the decision should act on the case's version.

== fence: 5 findings
  observation | Stale holder still writes identity-session rows and audit events after a take-over (unfenced transactions of a delivery) | apps/api/src/graph/subscriptions/subscription-session.service.ts:36
      Process H passes the fenced receipt (dispatcher :335) for event E, then K takes the domain over (0066 §1 take-over commits). H continues: openRunSession (session.service.ts:36-46) opens a run session and writes identity.agent_session_opened in its own identityDb transaction with no fence; only the next fenced write (the items' resolution, dispatcher :443) is refused. Likewise after each applied item, extendRunSession (session.service.ts:61-69) writes identity.agent_session_extended unfenced, so item N can commit under H, K take over, and H still record a session extension 'extended by delivery
  observation | Race: a fence refusal on a lapsed claim racing the tick's renewal drops the freshly renewed belief and detaches the kept worker — the domain idles up to one tick while the database names this process live | apps/api/src/graph/subscriptions/subscription-dispatcher.service.ts:409
      H's delivery is mid-items when the reconciliation tick is late enough that the DB claim lapses (claimed_until < now). Item k's fence raises P0S01 (0066 :146-148). While that error travels back through pipeline.write (recordHandlerFailure does another DB round trip, pipeline.service.ts:670), the late tick runs reconcile(): the claim port renews the lapsed claim for the same holder without advancing the generation (0066 :79-82), and reconcile sets a fresh serving entry (:271) — the worker is already in the map so :269 is a no-op. Then the job's catch runs lostServing (:402 → :409-413): it delete
  observation | The fence asserts scope only in commit mode — any in-scope commit transaction can hold KEY SHARE on the serving row (delaying take-over/release) and stamp eye.serving_holder | apps/api/migrations/0066_serving_ownership_and_reach.sql:133
      graph.subscription_serving_fence is GRANTed to eye_commit and, outside schedule mode, only calls observation.assert_scope(p_tenant, p_domain) (:136) — no observation.assert_authority on a consumer action, unlike the three ledger ports it protects (:222, :242, :269). The holder and generation are readable by eye_app/eye_commit under the graph_isolation policy (0065 GRANT SELECT on subscription_domain_serving). So any application code path running a commit-context transaction in the tenant/domain can call the fence with the current holder/generation, hold FOR KEY SHARE for the life of its transa
  observation | eye.subscriptions.serving_seconds accepts values below the 60 s reconciliation tick in every runtime — the claim then lapses between renewals in production | apps/api/src/config/config.ts:64
      The schema is z.coerce.number().int().min(5).max(3600).default(150) with EYE_SUBSCRIPTION_SERVING_SECONDS mapped for every runtime; the comment says a value below the tick is 'a test-runtime setting' but nothing enforces eye.runtime.env === 'test'. With EYE_SUBSCRIPTION_SERVING_SECONDS=30 in production: each process's local belief lapses at 30 s − 5 s (dispatcher :271) before the 60 s tick, every job entering handle after that is refused at :373-376 and the worker detached, the DB claim lapses at 30 s and any other process's tick takes the domain over (new generation); on the next tick the ori
  observation | Consumer-side order is not fenced across a hand-over: the stale worker's bounced (older) job is retried after backoff while the new holder may apply a newer event first | apps/api/src/graph/subscriptions/subscription-dispatcher.service.ts:373
      After K's take-over, H's worker keeps fetching until H's next tick or its first fence refusal (both workers consume the same BullMQ queue, scheduler.service.ts:372-378 concurrency 1 each). H fetches J1 (event seq n), its first fence refuses, J1 is moved to delayed with exponential backoff (PROPAGATION_JOB_OPTS, 2 s) and H's worker is detached. Meanwhile K's worker fetches J2 (event seq n+1, e.g. edge.retracted after J1's edge.asserted) and applies it; J1 is applied by K only after the backoff. The fence guarantees exclusivity of effects, not the '(one worker at a time) consumes the domain's or

== records: 12 findings
  CONFIRMED | §22.1 merge table is stale and contradicts plan §8 and the commit message in the same commit (#44 chain, #45 merge) | PHASE6_REPORT.md:1640
      scenario: Rows 7 and 8 read '#44 … pending at the time of writing' and '#45 … `main` — … waits for step 7's chain'. c93cdad was committed 2026-09-12T22:49Z; gh shows #44's chain green by 21:53Z (ci 34720646497, C17 finalize 34721192292, C19 anchor 34721231086) and #45 merged at 22:05Z as e0c5025 with ci 34721784881, C17 finalize 34722387128, C19 anchor 34722425076 all green by 22:19Z. docs/ops/STACK_INTEGRATION_PLAN.md §8 (same commit) already records #44's three run ids and #45's merge commit e0c5025; the commit message claims '#39 → … → #45, each with its C17 archive verified'. Three records of one commit disagree, and no record carries #45's run ids at all.
      refuter: stands — Not refuted; every element of the scenario checks out against the commit and the hosted runs. (1) PHASE6_REPORT.md:1640-1641 at c93cdad reads, row 7 (#44): "pending at the time of writing (`chain-7-44`)" and row 8 (#45): `main` "—", "waits for step 7's chain". (2) docs/ops/STACK_INTEGRATION_PLAN.md:334-335 in the SAME commit records #44 as `cbe1790` with ci 34720646497 / C17 finalize 34721192292 / C19 anchor 34721231086 all green, and #45 as merged at `e0c5025` with the run columns deferred to "
      refuter: stands — Not refuted; the scenario is factually accurate and nothing in the commit prevents or covers it. Verified: c93cdad committed 2026-09-13T01:49:19+03:00 (22:49Z). gh: PR #44 merged 21:40:57Z as cbe1790, its chain green by 21:57Z (ci 34720646497, C17 finalize 34721192292, C19 anchor 34721231086, all `success` on cbe1790); PR #45 merged 22:05:46Z as e0c5025, its chain green by 22:23Z (ci 34721784881, C17 finalize 34722387128, C19 anchor 34722425076, all `success` on e0c5025). Yet PHASE6_REPORT.md §2
  CONFIRMED | AU-DP-0176 verified:local with a harness clause no case exercises and a premise the code does not need | audit/acceptance-units/group-a-observation-intelligence-data.csv:468
      scenario: The condition's third clause — 'a corrected claim whose derivation is unchanged — basis.unchanged, nothing asserted' — has no case: apps/api/test/int/phase6-graph-subscriptions-4.test.ts has only SETUP, RE-DERIVED (l.208) and NOT RE-DERIVABLE (l.254). The code's basis.unchanged (apps/api/src/graph/subscriptions/consumers/relationships.consumer.ts:67–75) fires only when the edge is gone, left the asserted state, or the version no longer applies — never for an unchanged derivation; the RE-DERIVED case corrects only the confidence (an unchanged derivation) and a successor IS asserted, the opposite of the clause. The premise 'a REL claim's edge opened for reassessment by evidence change' / 'pending inferred relationship' is not exercised either: the edge is reassessment 'none' before the correction (the act shows 'reassessment none' too) and the consumer opens the reassessment itself with tr
      refuter: stands — Not refuted; the record defect stands as described. (1) The condition at audit/acceptance-units/group-a-observation-intelligence-data.csv:468 (a row new in B9, absent at 3932207) promises three harness cases; apps/api/test/int/phase6-graph-subscriptions-4.test.ts has only SETUP (l.197), RE-DERIVED (l.208) and NOT RE-DERIVABLE (l.254). No test anywhere exercises the relationships consumer's basis.unchanged (git grep: it appears only in the consumer source). (2) The code cannot produce the third c
      refuter: stands — Not refuted; the claim's facts hold on the code and tests. (1) The unit's condition (audit/acceptance-units/group-a-observation-intelligence-data.csv:468) names three harness clauses; the third, "a corrected claim whose derivation is unchanged — basis.unchanged, nothing asserted", has no case: apps/api/test/int/phase6-graph-subscriptions-4.test.ts's re-derivation describe (l.196–279) has only SETUP (l.197), RE-DERIVED (l.208) and NOT RE-DERIVABLE (l.254), and `grep basis.unchanged` over that fil
  CONFIRMED | AU-EXO-0051 and §22.3 §9 claim simulation/decision requests are fulfilled by the owner's act; only the scenario declaration does that, and the harness never files a simulation request | audit/acceptance-units/group-c-decision-executive-agents-learning-marketplace.csv:145
      scenario: remaining_work says 'scenario/simulation/decision routed to the responsible owner whose act names and fulfils the request' (PHASE6_REPORT.md:1701–1702 says the same). Only prediction.controller.ts:423–432 accepts a requestId and calls executive.fulfil_request in the same write (prediction.capabilities.ts:350); no twin/simulation or decision-review path names a request (grep of src/twin and src/executive/rooms finds none). apps/api/test/int/phase6-executive-requests.test.ts never sends kind 'simulation' and takes 'decision' only to state 'routed' (l.105); fulfilment for those kinds exists only as the generic POST /executive/requests/:id/fulfil (executive.controller.ts:286–292) whose port (0066 executive.fulfil_request) accepts any uuid as routed_ref without checking an act exists or matches the kind. A unit moved to verified:local on a route the harness does not take.
      refuter: stands — Not refuted; every element of the claim holds on c93cdad. (1) Only one act names a request in its own write: apps/api/src/prediction/prediction.controller.ts:423-432 (declareScenario reads payload.requestId and calls cap.fulfilExecutiveRequest → executive.fulfil_request in the same pipeline write, prediction.capabilities.ts:349-351). A grep for requestId/fulfil_request/fulfilRequest across apps/api/src/twin, src/decision, src/executive/rooms and src/prediction finds no simulation-run or decision
      refuter: stands — Not refuted; every element of the claim checks out against the candidate at c93cdad.

1. Only the scenario declaration names and fulfils a request. apps/api/src/prediction/prediction.controller.ts:422-432 reads payload.requestId and calls cap.fulfilExecutiveRequest (prediction.capabilities.ts:349-350 -> executive.fulfil_request) in the same write. A grep of apps/api/src/twin, apps/api/src/decision, apps/api/src/executive/rooms for requestId/request_id/fulfil returns nothing; B9's only change und
  CONFIRMED | AU-MEM-0059 verified:local while only the deletion workflow is exercised and the ports do not distinguish review/archive/customer_export from deletion | audit/acceptance-units/group-b-memory-prediction-twins.csv:60
      scenario: Statement: RetentionActionDue 'initiates archive, review, legal hold, deletion or customer-export workflow'. The harness executes a deletion only; the SCHEDULES case (phase6-graph-subscriptions-4.test.ts:509) evaluates a 'review' schedule to 'opened' and stops. 'legal_hold' is not an action kind (0066:865). retention.resolve_scope marks superseded manifests 'execute' regardless of a.kind (0066:1113–1150) and retention.service.ts execute() tombstones every 'manifest'/'execute' item with no branch on the action kind (l.75–90): an approved and executed 'review', 'archive' or 'customer_export' action deletes bytes exactly as a deletion. The act's declared '24 months … review action due after 730 days' schedule on eye_demo would, once due, approved and executed, remove evidence bytes. The unit's status asserts workflows the harness has not exercised and the code does not implement as distinct
      refuter: stands — Not refuted; the code path is exactly as described and nothing elsewhere prevents it.

Traced path for an evidence action of kind 'review' (or 'archive'/'customer_export'), e.g. one the schedule evaluation opens from the demo's declared "review action due after 730 days" schedule (evidence/cp6/act-b9.txt:90; retention.evaluate_schedules inserts actions_current with kind = s.action_kind and selector {manifest_id, source_id}, 0066:1073–1082):

1. retention.resolve_scope (0066:1111–1150): the only 
      refuter: stands — Not refuted; the code confirms the scenario end to end and nothing stops it.

Reproduction inputs: a domain admin declares a schedule with actionKind 'review' (controller /schedules/declare accepts any of the five kinds; the demo act did exactly this — evidence/cp6/act-b9.txt:90 "a review action due after 730 days"). retention.evaluate_schedules (0066 §4, lines 1062–1096) opens an action with kind = s.action_kind ('review') and selector {manifest_id, source_id}. From there no step distinguishes 
  CONFIRMED | AU-DP-0091 condition still states the pre-B7 gap ('legal_hold column … never read') while the row is verified:local | audit/acceptance-units/group-a-observation-intelligence-data.csv:383
      scenario: The condition field reads 'All profiles; legal_hold column exists and is never read.' — unchanged from the open row (git diff 3932207 c93cdad shows only the status/evidence columns changed). retention.resolve_scope reads m.legal_hold (0066:1137), so the condition contradicts the evidence the same row cites.
      refuter: stands — The claim is factually accurate and cannot be refuted from the code: audit/acceptance-units/group-a-observation-intelligence-data.csv:383 (AU-DP-0091) still carries condition "All profiles; legal_hold column exists and is never read." while its status is verified:local, and `git diff 3932207 c93cdad` on that row shows only status/evidence/remaining_work changed. The same row's evidence cites 0066 §4, and 0066 does read the column: retention.resolve_scope at apps/api/migrations/0066_serving_owner
      refuter: stands — Not refuted. The row at audit/acceptance-units/group-a-observation-intelligence-data.csv:383 is verified as claimed: `git diff 3932207 c93cdad` on that file shows only status (open → verified:local), evidence and remaining_work changed for AU-DP-0091; the condition column still reads "All profiles; legal_hold column exists and is never read." That sentence is false at c93cdad and contradicted by the evidence the same row cites: apps/api/migrations/0066_serving_ownership_and_reach.sql selects bm.
  CONFIRMED | §22.5 names an unreachable act head and claims two effects act-b9.txt does not show | PHASE6_REPORT.md:1741
      scenario: (a) 'head `96c0a76`': that commit is a WIP squashed into c93cdad — `git branch --contains 96c0a76` is empty; it survives only in the local reflog. The act's output also does not match that head's script: at 96c0a76 scene 1 looks for MemoryCorrected/memory_item.superseded (git diff 96c0a76 c93cdad -- scripts/phase6/demo-b9-capabilities.mjs) while act-b9.txt:… shows 'GraphChanged/memory_item.superseded published' — the act ran an uncommitted script. (b) l.1777 '`ScenarioReviewed` per review': the script checks only the retirement's row and prints 'for each review' regardless (scripts/phase6/demo-b9-capabilities.mjs:329–330; act-b9.txt:107). (c) l.1778 '(trigger kind `request`; …)': act-b9.txt:111 shows 'agent.run:briefing … run … finished' and nothing about the trigger kind; only the harness asserts it.
      refuter: stands — Not refuted; every element of the claim checks out against the repository at c93cdad. (a) `git branch -a --contains 96c0a76` is empty; the commit exists only as a reflog entry (HEAD@{4}, a "WIP B9" commit squashed into c93cdad), so PHASE6_REPORT.md:1741 and evidence/cp6/act-b9.txt:1 bind the demonstration to a head no clone of the repository can resolve. The act's own output confirms it ran a script that matches neither committed head: act-b9.txt:50 prints "GraphChanged/memory_item.superseded pu
      refuter: stands — Not refuted — all three sub-claims reproduce against the repository.

(a) Unreachable head and mismatched script. `git branch --contains 96c0a76`, `git tag --contains 96c0a76` and `git for-each-ref --contains 96c0a76` are all empty; the commit exists only in the local reflog (HEAD@{4}, "WIP B9: …"), and the chain 3932207 → a081001 → c93cdad does not include it. PHASE6_REPORT.md:1741 and evidence/cp6/act-b9.txt:1 are the only two mentions of that hash; nothing binds the act to c93cdad or disclose
  SPLIT | §B9 says migration 0066 has 'ten sections'; it has eleven, and the §10 header/bound_in comment say 'six' interfaces while seven rows are bound | audit/CP6_BATCHES.md:677
      scenario: 'Migration 0066 …, one file, ten sections.' — the file carries §1–§11 (grep '^-- §' on apps/api/migrations/0066_serving_ownership_and_reach.sql) and §B9 itself describes §11 further down. In the migration, the §10 header (l.2617) and the COMMENT on bound_in ('0066 for the six of CP-6 B9') say six while seven UPDATEs set bound_in='0066' (L2-I03, L2-I05, L3-I04, L3-I05, L4-I05, L7-I05, L10-I04); the 26/24/0 count itself is correct (19+7 bound, 25−1 partial).
      refuter: stands — Verified at c93cdad, not refutable. (1) `grep '^-- §' apps/api/migrations/0066_serving_ownership_and_reach.sql` returns eleven headers (§1 l.32 … §10 l.2617, §11 l.2653), while audit/CP6_BATCHES.md:677 says "one file, ten sections" and the same §B9 describes §11 at line 766 — the record contradicts itself and the file. (2) In the migration, the §10 header (l.2617: "the six interfaces this migration binds") and the persisted COLUMN COMMENT (l.2621: "0066 for the six of CP-6 B9") say six, while se
      refuter: refuted — The literal observations are accurate but they do not constitute a defect under the review's rules. Verified at c93cdad: the migration carries eleven `-- §N` headers (§1 l.32 … §10 l.2617, §11 l.2653); audit/CP6_BATCHES.md:677 says "ten sections"; the §10 header (l.2617) and the COMMENT on objects.interface_register.bound_in (l.2621) say "six" while seven UPDATEs (l.2622–2641: L2-I03, L2-I05, L3-I04, L3-I05, L4-I05, L7-I05, L10-I04) set bound_in='0066'. However: (1) "six" is not a miscount but a
  observation | AU-MEM-0121: three condition clauses not exercised, and 'bounded' vs the harness's refusal | audit/acceptance-units/group-b-memory-prediction-twins.csv:230
      THE LOG'S FLOOR case (phase6-graph-subscriptions-4.test.ts:470–507): 'shown on the subscriptions status' — the case reads objects.outbox_partitions directly, never the subscriptions status; 'objects.outbox_declare_floor refused … for a partition not the tenant's' — only the no-executing-action refusal is asserted (l.504); 'a replay from sequence 0 bounded to it' / statement 'a replay from below the declared floor is bounded to the floor' — the case asserts a refusal (l.503, rejects.toThrow(/floor|retained/)), which is 0065's rule for a named point.
  observation | AU-MEM-0065 statement clauses with no harness or page: the Enterprise Memory workspace, communications/telemetry-derived records, agent retrieval | audit/acceptance-units/group-b-memory-prediction-twins.csv:66
      Statement: 'retrieved by a purpose-authorized user or agent … through an Enterprise Memory workspace that also holds communications and telemetry-derived records'. The harness (phase6-graph-subscriptions-4.test.ts:281–381) retrieves as a human analyst only, records human-sourced items only, and §22.6 says the memory surface has 'routes and no pages'. Unlike AU-PRD-0064's remaining_work (which names the open browser leg), this row's remaining_work does not name the unexercised clauses.
  observation | §22.2 'generations monotonic across the cases' and §22.4 'unit 2147/2147' are not backed by the cited evidence | PHASE6_REPORT.md:1666
      repro-serving-lifecycle-after.txt prints five case names and no generation values; the test asserts only gens.size === 2 in one case (phase6-repro-serving-lifecycle.test.ts:248), not monotonicity across cases. §22.4 (l.1732) 'unit 2147/2147 (+ the refusal cases)': a local run at c93cdad gives 2148/2148 (one B9 unit case added, phase5-refusals.test.ts); no evidence file records a B9 unit run — 2147 is the 661c2fb hosted figure.
  observation | AU-MEM-0060 'the tombstone port refuses the held manifest … with the hold named' is asserted without checking the message | audit/acceptance-units/group-b-memory-prediction-twins.csv:61
      The only direct call of observation.tombstone_blob on a held manifest is `rejects.toThrow()` with no matcher (phase6-graph-subscriptions-4.test.ts:466); the hold-naming text is checked only as a literal string handed to asObservationRefusal in the unit test (phase5-refusals.test.ts), never as the port's output.
  observation | evidence/cp6/README.md indexes none of the eight B9 evidence files | evidence/cp6/README.md:1
      The index ends at the B8 rows (act-b8.txt, hosted-661c2fb-…); act-b9.txt, b9-int-all-2.txt, b9-int-all-3.txt, b9-run29.txt, b9-run31.txt, repro-serving-lifecycle-before.txt, repro-serving-lifecycle-after.txt and upgrade-0066.txt are cited from §22 but have no index row.

