=== the B17 harness at the B17 tree (apps/api: npx vitest run --config vitest.int.config.ts test/int/phase6-retention-b17.test.ts --reporter=verbose) on a fresh database eye_verify_b17_h6 (0001–0077; four isolated vault roots, an isolated station; EYE_SCHEDULER_ENABLED=true so the outbox publisher and the mirror domain's subscription worker run in process; no https egress, no substitution), 2026-09-16 — S1 import.admitted (the one event, the six deliveries, the ObservationRecorded rows before it in the partition, the review gate on an imported claim, the 300-entity truncation), S2 import.revoked (the origin's revocation executed in the mirror by the same act: edges retracted, entities retired, versions withdrawn with the lineage carried, bytes gone, custody, the receipt acknowledged on the origin ledger; the walk reaching the twin, the forecast, the scenario, the decision, the identifier proposal, the retrieval check; the retry), S3 the legal hold / the pending path / the fault after the graph write / a destroyed copy never reused / a copy another import holds, S4 the foreign origin's signed notice from the station (unsigned, another party's key, another package refused; the partner's and the same party's rotated key verified), S5 the demonstration recipient verifying and refusing, the origin's acts on an importer notice — 5/5 (the earlier runs h1–h5 on harness pins: a twin's claim citation resolves CLM objects — the imported record cited instead; a SQL quote in a fixture; a record's lifecycle is admitted, not active; the error body's dashed code in the pending classification — each fixed) ===

(!) Your Vite config uses features that are unsupported by `configLoader: 'native'`, which is planned to become the default in a future major version of Vite:
  - ESM syntax in a file loaded as CommonJS (vitest.int.config.ts:1:1). Use a `.mjs` extension or set `"type": "module"` in the closest package.json
Set `VITE_CONFIG_NATIVE_IGNORE_WARNING=true` to suppress this warning.

 RUN  v4.1.11 /Users/halawany/work/personal/mohammed/new_project/apps/api

stdout | test/int/phase6-retention-b17.test.ts
[32m✔[0m [33mConsole Ninja[0m extension is connected to [33mNest.js[0m, see [36mhttps://tinyurl.com/2vt8jxzw[0m

 ✓ test/int/phase6-retention-b17.test.ts > S1 · import.admitted (0077 D1, D20; L1-I03; AU-MEM-0030; ES-19-001) > S1 · records A and B (C@1 on B, the edge X, the entities and the identifier) admitted into the mirror → ONE GraphChanged/import.admitted from the graph write with the pinned payload, TWO ObservationRecorded rows ahead of it in the partition, the six deliveries applied (the selectors empty, retrieval verified, mappings nothing), the imported claim not reviewable here and the native one still challengeable; (6) the truncation control: 300 entities and 150 edges → identities cut at IMPORT_EVENT_LIST_MAX, truncated said, the item map whole 2921ms
 ✓ test/int/phase6-retention-b17.test.ts > S2 · import.revoked (0077 D2–D8, D12, D19; ES-08-004, ES-29-005, DP-47-005) > S2 · the mirror builds on the import (a twin bounded by the imported entity citing the imported record — C12 (a twin cites CLM claims; the seeded claim is a REL); an assumption, a decision, a forecast, a scenario, a package resting on it); the origin revokes E1 → the importer notified with a SIGNED notice and its copies destroyed by the same act: the edge retracted, the entities retired (the identifier kept), the claim and the records withdrawn with the lineage carried, the bytes gone, the custody, the import revoked, the receipt acknowledged on the origin ledger; ONE import.revoked with the walk; the six deliveries (the twin unverified, the forecast and scenario marked, the package invalidated, the identifier proposed, retrieval verified); the retry a no-op; the origin's second revoke refused 1217ms
 ✓ test/int/phase6-retention-b17.test.ts > S3 · the legal hold, the pending path, the fault, a destroyed copy never reused, a copy another import holds (0077 D4, D6, D7; C2, C4, C9; AU-MEM-0060) > S3 · (a) a hold on the mirror's copy of C → the origin's revoke answers held (D destroyed, C refused, the receipt false, the notice mismatched, the import revoking); (b) the steward's retry held again (a second notice attempt, no second event); (c) the hold lifted → revoked, acknowledged at attempt 3, a second import.revoked for C alone; (d) the pending path (a domain administrator of the origin) then the fault after the graph write and the second attempt's ONE event; (e) C2 and C9 — a revoked copy never reused, E2 reused retired, the shared record left held_by the other import and destroyed by its revocation 10924ms
 ✓ test/int/phase6-retention-b17.test.ts > S4 · the foreign origin — the signed notice from the station (0077 D8, D9, D13, D14; C7, C8; ES-53-004) > S4 · a package of a foreign tenant and domain, signed by the foreign partner, admitted from its foreign station path; revoke from the origin's record refused (not a domain of this tenant); an unsigned notice, another party's key, another package → refused with nothing destroyed; the foreign partner's signed notice → revoked from the station, the receipt written beside it, the origin not answered here, import.revoked with the station source; check 14's three wordings; (f) the same party's rotated key verifies with rotated_from 2121ms
 ✓ test/int/phase6-retention-b17.test.ts > S5 · the notice as the demonstration recipient sees it; the origin's acts on an importer notice (0077 D8; the B14 collect act) > S5 · (a) a package delivered to the origin's station, acknowledged and revoked → the SIGNED notice; the recipient with --revocation --public-key verifies it, destroys and answers (collected → acknowledged); (b) the notice hand-replaced by an unsigned copy → the recipient keeps its copies and says so (collected → mismatched); (c) the acknowledge route on the importer notice already answered → refused; (d) the notify route for an import that holds no copy → refused 650ms

 Test Files  1 passed (1)
      Tests  5 passed (5)
   Start at  17:54:11
   Duration  19.47s (transform 639ms, setup 17ms, import 1.14s, tests 18.23s, environment 0ms)

