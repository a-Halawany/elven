=== The author's adversarial review of the B10 closure candidate (six find dimensions, two refuters per finding) — 2026-09-13, on the uncommitted candidate over c04f6b1 (38 agents) ===

confirmed: 13; refuted: 3

CONFIRMED [high] B10-F4 is incomplete: /intelligence/claims/:claimId/get serves any canonical object by id whole — a role-restricted MEM version's statement/source/title, and a stored BRF's memory statements, to any holder of intelligence.read
  at apps/api/src/intelligence/intelligence.controller.ts:396
  scenario: getClaim() queries objects.canonical_objects by object_id only (no object_type filter; RLS canonical_read is tenant/domain visibility only, migration 0009:580; the pipeline never checks that route.objectId is of route.objectType) and returns `versions` and `current` as selectAll rows including `payload`. Reproduced on a fresh 0068 database through the governed controllers: knowledge_owner records an item with audience.roles=['knowledge_owner'] (statement 'KNOWLEDGE-OWNERS-ONLY…', source.ref 'KO-ONLY-SOURCE-REF'); a domain_analyst is refused memory.item.retrieve (403); the same analyst calls POST /v1/tenants/T/domains/D/intelligence/claims/<memItemId>/get with purpose 'observation' → 200, current.object_type='MEM', response contains the statement and the source ref. A composer (executive+knowledge_owner) then composes a domain briefing carrying that item; the analyst calls claims/<briefingId>/get → the BRF payload (items[].details.statement) is served whole, bypassing both the memory audience and the room/briefing.read governance the candidate adds in BriefingService.get(). intelligence.read is held by domain_analyst, extraction_agent, resolution_agent, collection_manager, auditor, tenant_admin (pdp.service.ts:213). servedRow() in objects.service only guards the objects routes; this route needs the same MEM/BRF withholding (or an object_type = claim-types filter).
  refuter 1: refuted=False — The claim stands on the code as written; nothing on the path withholds MEM or BRF payloads.  1. Route: apps/api/src/intelligence/intelligence.controller.ts:380-412 (getClaim) runs `cap.readCanonicalObjects().selectAll().where('object_id','=',claimId)` with only an optional `recorded_at <= knownAt` filter — no `object_type` filter — and returns `{ versions, current: versions[last], lineage, cases }` as raw rows, so `payload` is included. `IntelligenceCapability.read().readCanonicalObjects()` is `selectFrom('objects.canonical_objects')` (intelligence.capabilities.ts:180), unfiltered.  2. Authori
  refuter 2: refuted=False — The claim stands; I could not refute it from the code and I reproduced it on the candidate itself.  Code path (candidate = HEAD c04f6b1 + candidate.diff; the diff does not touch apps/api/src/intelligence/intelligence.controller.ts): - getClaim() (intelligence.controller.ts ~L378-411) runs pipeline.consequentialRead with route {action:'intelligence.read', objectType:'CLM', objectId: claimId} and IntelligenceCapability.read, then `cap.readCanonicalObjects().selectAll().where('object_id','=',claimId)` with no object_type filter, and returns `{ versions, current, lineage, cases }` as full rows (pa

CONFIRMED [high] B10-F4 is incomplete: /observation/evidence/:evdId/get (EvidenceService.detail) serves any canonical object by id whole — MEM version payloads and stored BRF payloads — to any holder of observation.read
  at apps/api/src/observation/vault/evidence.service.ts:91
  scenario: detail() selects objects.canonical_objects where object_id = evdId with no object_type filter and returns `evidence: evd` (the full row with payload) plus `versionHistory`; every follow-on lookup tolerates a non-EVD payload (obs_object_id/manifest_id undefined → skipped), so the call succeeds. Reproduced on a fresh 0068 database: the domain_analyst refused memory.item.retrieve (403) calls POST /v1/tenants/T/domains/D/observation/evidence/<memItemId>/get (action observation.read.evidence, purpose 'observation') → 200, evidence.object_type='MEM', response contains 'KNOWLEDGE-OWNERS-ONLY' and 'KO-ONLY-SOURCE-REF'; with the composed briefingId in place of the item id the BRF payload with the restricted statement is returned the same way. observation.read is held by domain_analyst, collection_manager, collection_agent, auditor, domain_admin, tenant_admin (pdp.service.ts). The candidate's F4 closure (servedRow in objects.service) does not reach this route.
  refuter 1: refuted=False — Every link of the scenario is concrete in the candidate's code. (1) apps/api/src/observation/observation.controller.ts:392-407 wires POST /v1/tenants/:t/domains/:d/observation/evidence/:evdId/get through pipeline.consequentialRead with action 'observation.read.evidence' and objectType 'EVD' declared by the route; pipeline.service.ts:327-364 (resolveAndEvaluate) feeds that declaration to the PDP and never reads the object, and consequentialRead (204-244) verifies nothing about the object's type. (2) pdp.service.ts:188-200: the prefix rule 'observation.read' (matched via startsWith at line 1221)
  refuter 2: refuted=False — Verified from the code the candidate diff leaves unchanged (HEAD apps/api/src/observation/vault/evidence.service.ts, detail() at lines 87-107):  1. detail() queries `cap.readCanonicalObjects().selectAll().where('object_id','=',evdId).orderBy('object_version','desc').limit(1)` with NO object_type predicate, and returns `evidence: evd` — the whole row including `payload`. The versionHistory query (lines 162-167) likewise has no type filter (it maps header fields only, so no payload there, but the main row is served whole).  2. The route (observation.controller.ts:392-406) is `POST /evidence/:evd

CONFIRMED [low] A composer-authored narrative that cites a memory item withheld from the reader is served unchanged beside the withheld item
  at apps/api/src/executive/briefings/briefing.service.ts:603
  scenario: get() withholds a memory item's title/statement/source for a reader outside the cited version's audience but returns `...b` with `narrative` and `narrative_cites` untouched. compose() only requires narrative cites to name included items (line 421-423). Inputs: the composer (executive+knowledge_owner) composes with narrative 'Per the ceiling review: the premium ceiling is revised to a quarter after the February review' and narrativeCites ['memory:<restricted>@1']; the plain executive reads the briefing → items[restricted] is 'memory: withheld' and items_withheld=1, yet the narrative carrying the restricted content and the cite naming the withheld item are delivered in the same response. The closure text requires the non-audience reader to receive none of the restricted statement through the complete briefing response; a narrative citing a withheld item should be withheld (or at least the cite flagged) for that reader.
  refuter 1: refuted=True — Mechanically the description is accurate — get() at apps/api/src/executive/briefings/briefing.service.ts:603 spreads `...b`, so the stored `narrative` and `narrative_cites` columns (0044, executive.briefings) are served as stored while the memory item is withheld. But that is not a disclosure path for the memory version's statement/source/title, and the "wrong output" rests on a rule that exists nowhere in the code.  1. No code derives the narrative from item content. The only producer is the human composer's request body (apps/api/src/executive/executive.controller.ts:124: `narrative: typeof 
  refuter 2: refuted=False — The scenario is fully concrete from the code and it stands, within bounds. (1) Composition: apps/api/src/executive/executive.controller.ts:109-124 accepts `narrative`/`narrativeCites` from a human composer; briefing.service.ts compose() lines 420-423 only require each cite to be an `item_id` of an included item, and the memory item R@1 IS included for a composer holding `knowledge_owner` (line 292); executive.compose_briefing (0044 §3, lines 301-312) applies the same check and stores `narrative`/`narrative_cites` on executive.briefings. The migration's own contract is "a narrative cites the it

CONFIRMED [medium] Withdrawn items now consume the 200 memory candidate slots (regression vs 0cee439): the slice runs before the withdrawal filter
  at apps/api/src/executive/briefings/briefing.service.ts:280
  scenario: compose() builds `candidates` as the 200 oldest latest-at-cutoff MEM versions (`sort(asc recorded_at).slice(0, 200)` at line 280) and only THEN skips items in `withdrawnByCutoff` (line 284) and those failing purpose/clearance/role. At 0cee439 the SQL read was `items_current where state='active' and recorded_at<=knownAt order by recorded_at limit 200`, so withdrawn items never occupied a slot. Inputs: a domain with 200 items recorded early and withdrawn by knownAt, plus 5 active items recorded later, all with purposes ['briefing'], internal, no roles; composer executive+knowledge_owner; knownAt after all of them. Expected (and 0cee439's) output: the 5 active items as `memory` items with 5 access rows. Candidate output: the 200 oldest candidates are exactly the withdrawn ones, every one is `continue`d, the 5 active items are never reached — zero memory items, zero accesses, nothing in the response says a bound was hit. More generally, every withdrawn item at the head of the ordering displaces one live item, so any long-lived domain with ≥200 items serves fewer memory items than 0cee439 did.
  refuter 1: refuted=False — The defect is concrete in the code. In the candidate apps/api/src/executive/briefings/briefing.service.ts (lines 268-284), `candidates` is `[...latestAtCutoff.values()].sort(asc recorded_at, then object_id).slice(0, 200)` computed BEFORE the loop, and `if (withdrawnByCutoff.has(itemId)) continue;` is the first statement INSIDE the loop, so items withdrawn by the cutoff occupy slots in the 200. At 0cee439 (line 263) the read was `readMemoryItems().where('state','=','active').where('recorded_at','<=',knownAt).orderBy('recorded_at').limit(200)` — the active filter ran in SQL before the limit, so 
  refuter 2: refuted=False — Confirmed from the code. apps/api/src/executive/briefings/briefing.service.ts line 280: `const candidates = [...latestAtCutoff.values()].sort(asc recorded_at, then object_id).slice(0, 200)`; the withdrawal exclusion `if (withdrawnByCutoff.has(itemId)) continue;` is at line 284, inside the loop over the already-sliced 200, as are the missing-projection skip and the purpose/clearance/role checks. At 0cee439 (git show 0cee439:.../briefing.service.ts) the read was `readMemoryItems().selectAll().where('state','=','active').where('recorded_at','<=',knownAt).orderBy('recorded_at').limit(200)` — the s

CONFIRMED [low] The 2000-version window is taken from the NEWEST versions while the 200 slice prefers the OLDEST: a heavy domain silently drops its oldest items, and the unordered boundary can change the digest
  at apps/api/src/executive/briefings/briefing.service.ts:270
  scenario: The candidate read is `MEM where recorded_at<=knownAt order by recorded_at desc limit 2000` (line 270) — no secondary order — then `latestAtCutoff` per item, then `sort(asc).slice(0,200)` (line 280). Inputs: a domain with 2,300 MEM versions recorded by knownAt (e.g. 1,900 items each superseded once plus 400 single-version items), the 300 oldest single-version items all in the composer's audience. Output: those 300 items' only versions fall outside the 2000 newest rows, so they are absent from `latestAtCutoff` and from the briefing — although the slice rule at line 280 says the OLDEST 200 are the ones served; the composition carries the 200 oldest of the remainder instead, with no truncation signal. At 0cee439 the read was bounded on items (200 oldest active), not on versions, so nothing older was lost to a version window. Second effect: with several versions sharing the ms-precision `recorded_at` at the 2000th row (bulk recording writes the API-clock `now` per request; ties are plausible), Postgres returns an arbitrary subset, so two compositions with the same inputs (same knownAt, same prior) can hold different candidate sets and therefore different content digests — the B10-F2 property breaks at scale.
  refuter 1: refuted=False — I could not refute it; the structural mismatch is exactly as the code reads. At apps/api/src/executive/briefings/briefing.service.ts:269-270 the candidate fetches `objects.canonical_objects` MEM rows with `recorded_at <= knownAt`, `orderBy('recorded_at','desc').limit(2000)` and no secondary key; line 271-272 reduces to the highest object_version per object_id; line 280 sorts those ASCENDING by (recorded_at, object_id) and slices 200. So the pre-fetch keeps the NEWEST 2000 versions while the selection rule serves the OLDEST 200 items — any item whose latest-by-cutoff version lies outside the ne
  refuter 2: refuted=False — The claim is concrete from the code and stands, with two corrections. (1) apps/api/src/executive/briefings/briefing.service.ts:270-271 reads `object_type='MEM' and recorded_at <= knownAt order by recorded_at desc limit 2000` (no secondary key), collapses to the highest object_version per item, then :280 sorts ascending by (recorded_at, object_id) and slices 200. The read is one domain's (RLS eye_row_visible; a SECURITY DEFINER SQL function, so not inlined — the plan is a seq scan + top-N heapsort over an append-only table). Nothing bounds MEM versions per domain (memory.record_item in 0066 has

CONFIRMED [low] `memory_accesses` is bound to the briefing by the client-supplied, non-unique envelope correlation_id, so accesses of other requests can be reported as the composition's
  at apps/api/src/executive/briefings/briefing.service.ts:600
  scenario: get() reports `memory_accesses` as `memory.item_access where correlation_id = briefing.correlation_id` (lines 599–600). The correlation id is taken from the request envelope (pipeline/http.ts line 121–125) and nothing enforces uniqueness per request; a correlation id is designed to span a causal chain (the agent run reuses one across its open-run/compose/close-run requests, agents.service.ts lines 200–229). Inputs: a client sends `memory.item.retrieve` on item X under purpose 'memory' with correlation C, then `briefing.compose` with the same correlation C (a legitimate correlated flow), then reads the briefing. Output: the briefing's `memory_accesses` lists the direct retrieval (purpose 'memory', reader = the person) beside the composition's accesses, presented as 'the composition's accesses'; two compositions issued under one correlation id each report both compositions' access rows. The content digest is unaffected; the misreport is in the access provenance the B10-F2 closure now relies on.
  refuter 1: refuted=False — Against the candidate diff the scenario is concrete at every step and nothing in the code refutes it.  1. The correlation id is client-supplied and never made unique. EnvelopeGuard (apps/api/src/pipeline/http.ts:121-125) copies envelope.correlation_id verbatim into req.eyeCorrelationId; validateEnvelope (packages/contracts/src/envelope.ts:109) only checks uuid format. No table constrains it: executive.briefings.correlation_id (0044_executive_rooms_and_briefings.sql:107), memory.item_access.correlation_id (0066_serving_ownership_and_reach.sql:479, index only on (item_id, accessed_at)), ctx.oper
  refuter 2: refuted=False — The claim is concrete and confirmed from the code. (1) The correlation id is client-supplied: pipeline/http.ts:121-125 copies envelope.correlation_id verbatim; packages/contracts/src/envelope.ts validates only `format: uuid`; nothing in pipeline.service.ts (no idempotency or replay gate) or in the schema (memory.item_access in 0066 line 467, executive.briefings in 0044 line 81 — neither has a unique constraint on correlation_id) makes it unique per request. (2) Two independent writers put rows on memory.item_access under that id: graph.controller.ts:747 (memory.item.retrieve, purpose = envelop

CONFIRMED [medium] memory_accesses is keyed on the client-supplied correlation_id alone, which is a workflow key (not unique per composition): get() can report another composition's (or a direct retrieval's) accesses as this briefing's, and the briefing→access binding AU-MEM-0065's audit relied on (access ids in the snapshot) is now ambiguous
  at apps/api/src/executive/briefings/briefing.service.ts:600
  scenario: The envelope contract carries message_id as the per-message identity and correlation_id as a correlation key; nothing validates or indexes correlation_id as unique (packages/contracts/src/envelope.ts; pipeline/http.ts falls back to newId() only when absent), and the codebase itself uses ONE correlation id across a multi-write workflow (executive.controller.ts request route: agent.trigger, executive.request, the agent run's every write and executive.request.fulfil all under envelope.correlation_id; agents.service.ts run(): open, room.read, decision.read, decision.monitor, briefing.compose, close under a.correlationId). A client that follows the same convention — e.g. an operator console composing the room briefing and then the domain briefing of one morning review under one correlation id, or a compose retried after a timeout whose first attempt committed, or a person retrieving item X via memory.item.retrieve under the workflow's correlation id and then composing — produces two memory.item_access rows sets with the same correlation_id. get(briefing A) then runs `readMemoryAccess().where('correlation_id','=', b.correlation_id)` with no narrowing by reader_principal_id = composed_by, purpose_id, read_as_of = known_at or the snapshot's cited (item_id, version) pairs, and returns memory_accesses containing briefing B's rows (item ids, versions, reader, purpose, read_as_of of items A never cited — including items B's room members alone may see the identity of) and the direct retri
  refuter 1: refuted=False — The claim holds against the candidate diff. Facts from the code:  1. Binding is correlation_id alone. get() (apps/api/src/executive/briefings/briefing.service.ts:599-600) runs `cap.readMemoryAccess().where('correlation_id','=', String(b['correlation_id']))` with no narrowing by reader, purpose, read_as_of, item/version, or briefing id. memory.item_access (migration 0066:467-484) has no briefing column; its only RLS is tenant/domain isolation (memory_isolation policy), so every same-domain row with that correlation id is returned. executive.briefings.correlation_id (0044:106) carries no uniquen
  refuter 2: refuted=False — The claim is concrete from the candidate diff. Candidate get() (apps/api/src/executive/briefings/briefing.service.ts, diff lines 175-176) selects memory.item_access rows with the single predicate correlation_id = executive.briefings.correlation_id and reports them as memory_accesses; there is no narrowing by reader_principal_id, purpose_id, read_as_of, or the snapshot's cited (item_id, version) pairs. Nothing makes that key unique per composition: executive.briefings.correlation_id (0044) and memory.item_access.correlation_id (0066) are uuid NOT NULL with no unique index; memory.item_access RL

CONFIRMED [high] B10-F4 remains open: /intelligence/claims/:claimId/get serves every version of ANY object id whole (MEM statement/source, BRF items with memory statements)
  at apps/api/src/intelligence/intelligence.controller.ts:396
  scenario: getClaim reads objects.canonical_objects by object_id only (no object_type filter) and returns `versions` and `current` with payload; the PDP takes objectType 'CLM' from the route and never checks the row's type. Inputs: the harness's domain_analyst (holds intelligence.read; refused memory.item.retrieve on the knowledge_owner-only item with 403) posts /v1/tenants/T/domains/D/intelligence/claims/<memoryItemId>/get with purpose 'observation'. Output: every MEM version's payload — title 'Ceiling review', statement 'KNOWLEDGE-OWNERS-ONLY: …', source ref 'KO-ONLY-SOURCE-REF' — with no audience-role, classification, purpose or access-ledger check. The same call with <briefingId> returns the BRF payload whole: items[].details.statement of every memory item the composer read, without room membership or the briefing's purpose/clearance checks. servedRow() in objects.service.ts closes only the objects route; this by-id read is the same class of disclosure.
  refuter 1: refuted=True — The claim was accurate against the snapshot the reviewers were given (candidate.diff, 13:00:15, on HEAD c04f6b1) but is refuted by the code now in the working tree, and the closing edit is covered by the harness on the real database.  What was concrete at the snapshot (every link verified in code): HEAD's getClaim (apps/api/src/intelligence/intelligence.controller.ts) filtered canonical_objects by object_id only and returned selectAll() rows as `versions`/`current`; the PDP evaluates the route's DECLARED objectType 'CLM' (pipeline.service.ts:355) and never the row's type, and no bound-object c
  refuter 2: refuted=False — The claim stands against the candidate as diffed. Verified from the code: (1) the candidate diff does not touch apps/api/src/intelligence/intelligence.controller.ts, so getClaim is at HEAD, where lines 396-397 read cap.readCanonicalObjects().selectAll().where('object_id', '=', claimId) with no object_type filter and return { versions, current, lineage, cases } spread into the response beside receipt; versions is selectAll, so each row's payload is served. (2) The PDP is pure: pipeline.service.ts resolveAndEvaluate builds PolicyInput from the route's declared objectType 'CLM' and calls the sync

CONFIRMED [high] B10-F4 remains open: /observation/evidence/:evdId/get serves a MEM or BRF object whole (detail and all versions) to any observation.read holder
  at apps/api/src/observation/vault/evidence.service.ts:91
  scenario: EvidenceService.detail() selects the latest row `where object_id = evdId` with no object_type filter and returns it as `evidence` plus `versions` (selectAll, every version, payload included). Inputs: a collection_manager, collection_agent or domain_analyst (observation.read roles; none of them in a memory version's audience roles, and none a room member) posts /observation/evidence/<memoryItemId>/get or /observation/evidence/<briefingId>/get. Output: the role-restricted memory statement/source/title (every version) or the stored briefing's items with their memory statements — under action 'observation.read.evidence' with objectType 'EVD' asserted by the route, never verified against the row.
  refuter 1: refuted=True — The claimed disclosure path is closed in the actual uncommitted candidate. apps/api/src/observation/vault/evidence.service.ts detail() (called by POST /observation/evidence/:evdId/get, observation.controller.ts:392-405) now filters `.where('object_type' as never, '=', 'EVD' as never)` on both the head-row query (lines 92-98) and the version-history query (lines 165-171), under a `// B10-F4` comment at line 91. For a MEM or BRF object id the head query returns undefined and the route answers 404 EYE_STA_001 'no authorized evidence object matches' before any payload, custody, manifest or version
  refuter 2: refuted=False — The claim stands against the candidate. The candidate diff (scratchpad/b10r/candidate.diff, snapshotted 13:00:15) does not touch apps/api/src/observation/vault/evidence.service.ts, so the candidate carries HEAD's detail(): `readCanonicalObjects().selectAll().where('object_id','=',evdId).orderBy('object_version','desc').limit(1)` (HEAD lines 90–95) with no object_type term, and the same for the versions query (HEAD 162–167). Every link of the scenario is concrete in code: (1) the route apps/api/src/observation/observation.controller.ts:392–405 asserts objectType 'EVD' under action observation.r

CONFIRMED [medium] A withheld memory item's content can still reach the reader through the briefing's narrative, which get() serves whole
  at apps/api/src/executive/briefings/briefing.service.ts:603
  scenario: compose() accepts a narrative that cites included items (a memory item's item_id 'memory:<id>@<v>' is a valid cite) and stores it beside the content; get() returns `...b` unchanged, so `narrative` and `narrative_cites` are served to every reader regardless of the audience check applied to items. Inputs: the composer (executive + knowledge_owner) composes with narrative 'Ceiling review: the premium ceiling is revised to a quarter after the February review' citing memory:<restricted>@1 (or the Model Gateway adds such a narrative summarising the items). The plain executive reads the briefing: the item is reported withheld (items_withheld = 1, title 'memory: withheld', details stripped) while `narrative` carries the restricted statement verbatim — the complete response holds the content Codex's B10-F1 closure requires to be absent. The harness composes without a narrative, so it cannot detect this. Withhold (or mark) the narrative when any of its cites is withheld for this reader.
  refuter 1: refuted=False — Against the candidate diff as captured (scratchpad candidate.diff, 13:00) the scenario is concrete: compose() accepts a composer-supplied free-text narrative whose cites are validated only against items' item_id (push() forms a memory item's item_id as `memory:<uuid>@<v>`, so a cite of a role-restricted memory version is valid; migration 0044 compose_briefing applies the same check and stores narrative/narrative_cites on executive.briefings). get() in the diff reads the row with selectAll() and returns `{ ...b, items: served, items_withheld, ... }`, so `narrative` and `narrative_cites` reach e
  refuter 2: refuted=False — Every premise is concrete in the captured candidate. (1) apps/api/src/executive/executive.controller.ts:109-124 accepts `narrative`/`narrativeCites` from the request body on the human compose route; briefing.service.ts compose() at 418-425 validates only `cites ⊆ items.map(i => i.item_id)`, and `push()` (191-194) builds `item_id` as `${kind}:${id}@${version}`, so `memory:<uuid>@1` is a valid cite whenever the composer's roles admitted the item. executive.compose_briefing (migrations 0044/0048/0069) enforces the same cite rule and stores `narrative text` and `narrative_cites jsonb` on executive

CONFIRMED [low] memory_accesses is bound by the client-supplied, non-unique envelope correlation_id, so a briefing read can report accesses that are not this composition's
  at apps/api/src/executive/briefings/briefing.service.ts:599
  scenario: get() selects memory.item_access `where correlation_id = b.correlation_id`; the correlation id is taken from the request envelope (pipeline/http.ts) with no uniqueness or replay check, and memory.record_access stamps every access (compositions and direct memory.item.retrieve reads) with the caller's correlation id. Inputs: a client composes briefing A and briefing B (different knownAt) under the same envelope.correlation_id, or retrieves item X directly (memory.item.retrieve) under the correlation id it used to compose A. Output: get(A).memory_accesses lists B's access rows (or the direct retrieval's) as A's composition accesses — the B10-F2 'bound to the briefing' report is wrong for that briefing. Binding by briefing_id (or by the composition's own event/access ids recorded outside the content) would be exact.
  refuter 1: refuted=False — Every step of the scenario is concrete in the candidate diff and the code it depends on; nothing refutes it.  1. The lookup: candidate diff lines 175-176 (briefing.service.ts get()) read `cap.readMemoryAccess()` = `memory.item_access` (executive.capabilities.ts, `this.from('memory.item_access')`) with the sole predicate `correlation_id = String(b['correlation_id'])`, ordered by accessed_at. No predicate on briefing, reader, purpose, read_as_of or item. RLS on memory.item_access (migration 0066 lines 484-497) isolates by tenant and domain only.  2. The bound value is client-supplied and nothing
  refuter 2: refuted=True — The claim is accurate for the snapshot at scratchpad/b10r/candidate.diff (13:00; worktree.diff too) but does not hold against the actual uncommitted code in the repository, which the author has already moved past.  What I verified for the claim's premises (they are all true, so the defect WAS concrete in the 13:00 snapshot): - apps/api/src/pipeline/http.ts:121-125 takes envelope.correlation_id as-is from the client (newId() only when absent); packages/contracts/src/envelope.ts:109 validates it as format 'uuid' only; no uniqueness or replay check exists anywhere in the pipeline. - memory.record

CONFIRMED [high] intelligence claims/:id/get serves any canonical object by id — a MEM version (statement, source) and a BRF's stored items reach an intelligence.read holder outside the audience
  at apps/api/src/intelligence/intelligence.controller.ts:396
  scenario: The route queries readCanonicalObjects().selectAll().where('object_id','=',claimId) with no object_type filter and returns { versions, current } whole; the route's objectType 'CLM' is only a declared policy string (the pipeline never checks the stored type; RLS on canonical_objects is tenant/domain only). Verified on a fresh 0068 database: a domain_analyst refused memory.item.retrieve on a knowledge_owner-only item (403) calls POST .../intelligence/claims/<memItemId>/get with envelope action intelligence.read / object_type CLM and receives payload.statement 'KNOWLEDGE-OWNERS-ONLY…' and payload.source.ref whole, every version. The same query serves a BRF object's stored payload (items with memory statements, narrative) to any intelligence.read holder (domain_analyst, extraction_agent, collection_manager, …) who is neither a room member nor in the cited versions' audience. B10-F4's servedRow only wraps ObjectsService reads; this by-id read is the same class of generic read and stays open, so F1 and F4 are not closed at the boundary.
  refuter 1: refuted=False — The claim stands against the candidate diff as reviewed. The candidate's B10-F4 closure wraps only ObjectsService (servedRow in apps/api/src/objects/objects.service.ts); apps/api/src/intelligence/intelligence.controller.ts is not in candidate.diff, and at HEAD (lines 396-397) the claims/:claimId/get handler runs cap.readCanonicalObjects().selectAll().where('object_id','=',claimId) with no object_type predicate and returns { versions, current, lineage, cases } whole (selectAll includes payload). IntelligenceCapabilityImpl.readCanonicalObjects() is tx.selectFrom('objects.canonical_objects') (the
  refuter 2: refuted=True — The claim was argued against a stale snapshot. The file /private/tmp/.../scratchpad/b10r/candidate.diff was written at 13:00:15 and omits four working-tree changes made afterwards: apps/api/src/intelligence/intelligence.controller.ts (13:14:01), apps/api/src/observation/vault/evidence.service.ts (13:14:01), migration apps/api/migrations/0069_b10_closure_accesses_and_port_owned_types.sql (13:15:52) and the harness apps/api/test/int/phase6-briefing-memory.test.ts (13:19:07). The actual uncommitted candidate (git diff vs HEAD c04f6b1) closes the route the claim names.  At HEAD the route indeed re

CONFIRMED [low] memory_accesses is keyed by the client-supplied envelope correlation_id, which is not unique per request; a reused correlation id attributes another request's ledger rows (including a direct retrieval of an item outside the briefing) to this briefing
  at apps/api/src/executive/briefings/briefing.service.ts:599
  scenario: correlation_id is taken from the request envelope (packages/contracts/src/envelope.ts only requires format uuid; pipeline.service.ts enforces no uniqueness). A client composes briefing B under correlation C, then retrieves memory item Z (a role-restricted item that is NOT in B) under memory.item.retrieve with the same correlation C (graph.controller.ts records that access with envelope.correlation_id). A later briefing.read of B returns memory_accesses containing {item_id: Z, version, purpose, reader, ...} — an access B never made, reported as the composition's; the same happens for two compositions sent under one correlation id. Older briefings and the harness are unaffected (executive.briefings.correlation_id and memory.item_access.correlation_id are NOT NULL and the harness mints a fresh uuidv7 per request), so this is an attribution defect at the boundary, not a regression; identity-level fields only (item id, version, purpose, reader), no statement/source/title.
  refuter 1: refuted=False — The claim is concrete against the candidate diff. (1) correlation_id is client-supplied and nothing makes it unique: packages/contracts/src/envelope.ts declares it only as { type: 'string', format: 'uuid' }; apps/api/src/pipeline/http.ts and pipeline.service.ts perform no replay/freshness check (ctx.issue_commit / ctx.issue_evidence only require it to be present, migrations 0011/0013/0014); the only indexes on correlation_id are non-unique (0003_policy.sql:39, 0004_audit.sql:36). (2) The retrieve route records its access under that id: apps/api/src/graph/graph.controller.ts:747 calls cap.recor
  refuter 2: refuted=True — The claim describes code that exists only in the stale candidate.diff snapshot (captured 13:00:15), not in the uncommitted candidate on disk. The working tree's briefing.service.ts (13:16:37), executive.capabilities.ts (13:17:26) and the new migration apps/api/migrations/0069_b10_closure_accesses_and_port_owned_types.sql (13:15:52) post-date the snapshot and replace the correlation-id lookup entirely.  Concretely, in the actual candidate: 1. `readMemoryAccess()` is gone from `ExecutiveReads` — `grep -rn readMemoryAccess apps/api/src/` returns nothing. The only memory.item_access reader left in

REFUTED Memory items are the only item kind not windowed by the prior's known_at (`since`); every windowed briefing re-lists all standing items and they count against max_items
  - The mechanism described is real (apps/api/src/executive/briefings/briefing.service.ts:269-280 reads MEM versions by `recorded_at <= knownAt` with no `since` term, and line 413 counts every item, memory included, against `lim.maxItems`), but it is not a defect of this candidate under the review's rul
  - The observation is accurate but it is not a defect of this candidate under the review's rules; it is the specified, tested design. (1) Not a regression: at the base commit 0cee439 the memory read was already unwindowed (`readMemoryItems().where('state','=','active').where('recorded_at','<=',knownAt)

REFUTED observation evidence/:id/get serves any canonical object by id — a MEM version and a BRF payload reach an observation.read holder outside the audience
  - The scenario cannot be made concrete from the actual uncommitted code in the repository.  1. Working-tree state. /Users/halawany/work/personal/mohammed/new_project/apps/api/src/observation/vault/evidence.service.ts is modified and uncommitted (git status ' M', mtime 13:14:01). Its detail() now filte
  - The claim describes a state the candidate no longer has. The scratchpad candidate.diff (mtime 13:00:15) is a stale snapshot: it omits apps/api/src/observation/vault/evidence.service.ts, which the working tree modified at 13:14:01 (git diff vs HEAD c04f6b1: +4 lines). In the current uncommitted candi

REFUTED The generic write objects.correct admits a new MEM (or BRF) version: an analyst rewrites a knowledge-owner-only item's current reading, unlocks it for themselves, and bricks the record authority's next supersession
  - The claim does not stand against the candidate under review; it describes the pre-candidate state (a "fresh 0068 database") and misses that the uncommitted candidate includes migration 0069, which closes exactly this path.  What the reviewer got right about the pre-candidate state: observation.canon
  - The claim is refuted against the candidate as it stands: the candidate closes exactly this path, and the reviewer verified it on a database that does not carry the candidate's migration.  1. The candidate is larger than the five-file diff excerpt. `git status` on phase6-b10 shows the untracked migra
