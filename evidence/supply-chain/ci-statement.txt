# CI execution statement — Phase 0 invariant-remediation gate — TRUTHFUL DISCLOSURE

This is a LOCAL-ONLY Phase 0 gate. HOSTED CI EXECUTION IS NOT CLAIMED: this
repository has no GitHub remote configured, so no hosted GitHub Actions run
exists for GATE_CANDIDATE_SHA (see evidence/git-metadata.txt) or any commit.

.github/workflows/ci.yml defines the gate pipeline as THREE jobs: build-test,
browser-regression, supply-chain. For this gate, LOCAL-EQUIVALENT evidence was
produced by running the SAME commands the workflow runs, on this machine,
against the SAME digest-pinned images and generated ephemeral secrets. Raw
command / timestamp / tool-version / exit-code logs: evidence/test-runs.txt.

Job -> local-equivalent evidence:

build-test:
  - pnpm boundaries                → ✔ 85 modules, 222 deps
  - pnpm typecheck                 → ✔ 4 workspaces
  - pnpm build                     → ✔
  - pnpm test (unit + conformance) → contracts 118, tokens 3, api 14
  - pnpm db:migrate                → migrations 0001–0008 on a virgin volume
  - pnpm --filter @eye/api test:int    → 38/38
  - pnpm --filter @eye/api test:accept → 34/34

browser-regression:
  - pnpm exec playwright test      → 10/10 (Chrome stable)

supply-chain:
  - gitleaks                       → gitleaks-report.json / gitleaks-stdout.txt (exit 0)
  - node scripts/generate-sbom.mjs → sbom.cdx.json (280 components; fails on empty)
                                     + licenses-prod.json / licenses-dev.json
                                     + reconciliation.txt (RECONCILED)
  - pnpm audit --audit-level high  → pnpm-audit-human.txt (0 known vulnerabilities)
  - node scripts/license-inventory.mjs → 0 allowlist violations
  - trivy fs (labeled filesystem scan)     → trivy-fs.txt (exit 0)
  - trivy image <postgres pinned digest>   → trivy-image-postgres18.txt (exit 0)
  - trivy image <redis pinned digest>      → trivy-image-redis8.txt (exit 0)
  - scripts/verify-images.sh (digest consistency + blocking image scans)
                                           → verify-images.txt (PASS)

The exact image digests scanned are the ones pinned in docker-compose.yml and
recorded in conformance.manifest.json. CVE-level dispositions (owner, date,
expiry, external/customer-data prohibition) live in .trivyignore. Tools:
pnpm audit, gitleaks 0.73-era, trivy 0.73.0 (exact versions in test-runs.txt).
Hosted-CI execution becomes verifiable as soon as a remote is configured; until
then CI claims are limited to workflow-definition content + this local evidence.
