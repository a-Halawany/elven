# Trivy evidence — Phase 0 invariant-remediation gate (R8)
#
# HONEST STATEMENT: this project builds NO application container images (no
# Dockerfile exists; the API/web run as node processes). Trivy runs in two
# correctly-labeled modes:
#   (a) `trivy fs`    — the repository filesystem            → trivy-fs.txt
#   (b) `trivy image` — the EXACT pinned Compose image digests, blocking at
#       HIGH/CRITICAL                                        → trivy-image-*.txt
#
# Images are pinned by immutable DIGEST in docker-compose.yml and recorded in
# conformance.manifest.json. The alpine variants were chosen for a clean
# HIGH/CRITICAL OS baseline.

trivy version: 0.73.0
trivy fs exit:                      0
trivy image postgres (pinned):      0   (postgres:18-alpine @sha256:9a8afca5...de15)
trivy image redis    (pinned):      0   (redis:8-alpine     @sha256:978f0e01...6241)

# SUPERSEDED / HISTORICAL: the previous debian-based postgres:18 and redis:8
# tags carried 55 and 22 HIGH/CRITICAL OS-package findings respectively. Those
# are historical and no longer the deployed images.

# CVE-level dispositions (.trivyignore) — LOCAL-ONLY, dated, owner-attributed:
# postgres:18-alpine:
#   - c-ares CVE-2026-33630 (HIGH, OS pkg; fix not yet in any published build;
#     DoS-class; PostgreSQL bound to loopback)
#   - 15 Go-stdlib CVEs compiled into the upstream gosu 1.19 binary shipped by
#     EVERY current official postgres image; gosu runs once at container start
#     to drop root and none of the findings are reachable in that use; no
#     patched official build exists.
# redis:8-alpine: clean at HIGH/CRITICAL — no dispositions.
#
# Owner: founding-engineer. Disposition date: 2026-08-05. Expiry: 2026-11-05
# (or next monthly ADR-P0-01 patch-cadence re-pin, whichever is earlier).
# PROHIBITED: external deployment / real customer data under these dispositions;
# before any such use the images must be re-pinned to patched builds and
# re-scanned as a blocking release gate.

# pnpm-audit disposition
# Zero known vulnerabilities after the sharp>=0.35.0 / postcss>=8.5.23 workspace
# overrides (pnpm-audit-human.txt, pnpm-audit.json). trivy fs is clean.
