{
  "schema": "eye/c19/trust-policy",
  "version": 3,
  "_comment": [
    "C19 TRUST POLICY v3. v1 pinned GitHub's rotating OIDC JWKS leaves as an offline root, which was",
    "wrong twice over. v2 fixed the anchor but left the IDENTITY POLICY able to FAIL OPEN: fields were",
    "only checked when the caller happened to supply an expected value, so an identity carrying a wrong",
    "source SHA, a wrong workflow digest and a wrong run URI verified with zero findings when those",
    "expectations were absent - which is exactly how the real CLI called it.",
    "v3 makes every delivery-standing field MANDATORY. A missing expectation is a refusal, never a skip."
  ],
  "tuf": {
    "rootFile": "tuf-root.json",
    "trustedRootFile": "trusted-root.json",
    "trustedRootSha256": "736cbf298e93aca1803195fa15ed91553071a916e466901c721d5a71e8f157e1",
    "minimumVersions": {
      "root": 1,
      "timestamp": 1,
      "snapshot": 1,
      "targets": 1
    }
  },
  "algorithms": [
    "ES256"
  ],
  "identity": {
    "issuer": "https://token.actions.githubusercontent.com",
    "subjectAlternativeName": "https://github.com/a-Halawany/elven/.github/workflows/c19-anchor.yml@refs/heads/main",
    "repository": "a-Halawany/elven",
    "repositoryOwner": "a-Halawany",
    "ref": "refs/heads/main",
    "workflowRef": "a-Halawany/elven/.github/workflows/c19-anchor.yml@refs/heads/main",
    "runnerEnvironment": "github-hosted",
    "repositoryId": "1326601788",
    "repositoryOwnerId": "84006518",
    "signerEventName": "workflow_run"
  },
  "signers": {
    "sigstore-fulcio": {
      "kind": "sigstore-keyless",
      "deliveryCapable": true,
      "trustMaterial": "tuf",
      "description": "Fulcio-issued short-lived certificate bound to a GitHub Actions OIDC identity, with the signature recorded in Rekor. The trust root is the TUF-authenticated Sigstore material, not any single rotating key."
    },
    "local-dev": {
      "kind": "ed25519-local",
      "deliveryCapable": false,
      "keys": {},
      "description": "NON-DELIVERY test seam only. Controlled by the evidence process, therefore NOT independent. Hosted delivery standing refuses it by construction."
    }
  },
  "revoked": [],
  "requiredIdentityFields": [
    "subjectAlternativeName",
    "issuer",
    "sourceRepositoryUri",
    "sourceRepositoryIdentifier",
    "sourceRepositoryOwnerUri",
    "sourceRepositoryOwnerIdentifier",
    "sourceRepositoryRef",
    "sourceRepositoryDigest",
    "buildConfigUri",
    "buildConfigDigest",
    "buildTrigger",
    "runInvocationUri",
    "runnerEnvironment"
  ],
  "_requiredComment": [
    "Every field here MUST be present in the certificate AND matched against an expected value.",
    "If the caller cannot supply an expectation, verification FAILS - it does not skip the check."
  ],
  "_fixture": "NOT the production policy: it trusts a fixture CA generated by make-sigstore-fixture.mjs"
}
